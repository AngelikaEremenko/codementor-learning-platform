# -*- coding: utf-8 -*-
"""Модуль 7: Связные списки."""

# ============================================================================
# ТЕОРИЯ 1 — Что такое связный список
# ============================================================================

THEORY_1 = """\
# Связные списки: ещё один способ хранить последовательность

## Зачем ещё одна структура

Мы уже знаем `vector`, динамический массив, который умеет расти. Он
быстрый при доступе по индексу (`v[i]` за O(1)), но не очень удобен, когда
нужно вставить элемент в середину: чтобы освободить место под новый
элемент, приходится **двигать** все следующие элементы вправо.

**Связный список** устроен совсем иначе. Элементы не лежат в памяти
подряд. Каждый элемент (его называют **узлом**) хранит своё значение и
указатель на **следующий** узел. Из этих ссылок и собирается
последовательность.

Аналогия. Представь поиск сокровищ: первая записка в почтовом ящике, и в
ней написано «следующая записка под подушкой». Под подушкой следующая
записка с адресом следующей. И так далее. Сами записки разбросаны по
квартире как угодно. Чтобы пройти всю цепочку, нужно начать с первой и
следовать указаниям. Связный список устроен точно так же.

Графически это выглядит так:

```
[1] -> [2] -> [3] -> nullptr
```

Стрелочка означает «здесь хранится адрес следующего узла». В конце списка
вместо адреса лежит специальное значение `nullptr`, что означает «дальше
ничего нет».

## Узел

В C++ узел это обычная структура с полем-значением и полем-указателем на
такой же узел.

```cpp-run
// hint: создаём список вручную и обходим его
#include <iostream>
using namespace std;

struct Node {
    int value;     // значение, которое хранится в узле
    Node* next;    // указатель на следующий узел (или nullptr)
};

int main() {
    // создаём три узла и связываем их в цепочку 1 -> 2 -> 3
    Node* a = new Node{1, nullptr};
    Node* b = new Node{2, nullptr};
    Node* c = new Node{3, nullptr};
    a->next = b;
    b->next = c;
    // поэкспериментируй: добавь четвёртый узел, поменяй порядок ссылок

    // обход списка от начала до конца
    for (Node* cur = a; cur != nullptr; cur = cur->next) {
        cout << cur->value << ' ';
    }
    cout << endl;

    // обязательно убираем за собой
    delete a;
    delete b;
    delete c;
    return 0;
}
```

В коде сразу несколько новых вещей, на которых стоит остановиться:

- `Node*` это **указатель** на узел. По сути, адрес ячейки памяти, где лежит сам узел.
- `new Node{1, nullptr}` создаёт новый узел в специальной области памяти (она называется «куча») и возвращает указатель на него.
- `cur->value` это короткая запись для `(*cur).value`, то есть «значение узла, на который указывает `cur`». Оператор `->` это «доступ к полю через указатель».
- `delete a` освобождает память, которую мы заняли через `new`. Без этого память считается «занятой» до конца программы. Если делать `new` и забывать `delete`, говорят, что произошла **утечка памяти**.

Обход списка это маленький, но очень характерный шаблон:

```cpp
for (Node* cur = head; cur != nullptr; cur = cur->next) { ... }
```

Тут мы заводим «бегунок» `cur`, начинаем с головы списка и на каждой
итерации перепрыгиваем к следующему узлу. Цикл заканчивается, когда `cur`
становится `nullptr`, то есть мы прошли весь список.

## Чем хорош и чем плох список

| Операция                                              | Vector                  | Связный список |
|-------------------------------------------------------|-------------------------|----------------|
| Доступ к k-му элементу                                | O(1)                    | O(n), нужно дойти от головы |
| Вставка/удаление в середине (есть указатель на узел)  | O(n) со сдвигом хвоста  | O(1) |
| Памяти на один элемент                                | только значение         | значение + указатель |

Главная мысль: у двух структур разные сильные и слабые стороны. Вектор
быстро отвечает на «дай элемент номер k», но дорого вставляет в
середину. Список наоборот: дойти до середины долго, но **уже находясь** в
нужном месте, вставить или удалить можно мгновенно.

В алгоритмических задачах список чаще нужен как **концепт**, на котором
держатся более сложные структуры: деревья, очереди с приоритетом, графы.
Понимание узлов и указателей здесь это фундамент, без которого тяжело
идти дальше.

[mini-task:0]

## Итоги

- Связный список это узлы, и каждый узел знает только своего следующего соседа.
- Доступ к k-му элементу выполняется за O(n). Но если у нас уже есть указатель на конкретный узел, вставить или удалить рядом с ним можно за O(1).
- В C++ узлы создаются через `new` и обязаны освобождаться через `delete`. Иначе будет утечка памяти.
- Указатели и оператор `->` это основа основ. Они встретятся ещё много раз в деревьях и графах.
"""

QUIZ_1 = [
    {
        'question': 'Какая сложность доступа к k-му элементу односвязного списка?',
        'a': 'O(1)', 'b': 'O(log n)', 'c': 'O(n)', 'd': 'O(n²)',
        'correct': 'c',
    },
    {
        'question': 'Что хранит узел односвязного списка?',
        'a': 'Только значение',
        'b': 'Значение и указатель на следующий узел',
        'c': 'Значение, указатели на следующий и предыдущий',
        'd': 'Целый массив значений',
        'correct': 'b',
    },
    {
        'question': 'Что означает Node* next = nullptr;?',
        'a': 'Указатель ни на что не указывает (конец списка)',
        'b': 'Создан новый узел',
        'c': 'Это значение узла',
        'd': 'Узел удалён',
        'correct': 'a',
    },
    {
        'question': 'Зачем после new обязательно вызывать delete?',
        'a': 'Чтобы программа работала быстрее',
        'b': 'Чтобы освободить память, иначе будет утечка',
        'c': 'delete обнуляет значение',
        'd': 'Это не обязательно',
        'correct': 'b',
    },
    {
        'question': 'В чём преимущество связного списка перед вектором?',
        'a': 'Быстрый доступ по индексу',
        'b': 'Быстрая вставка/удаление в середине, если есть указатель на нужный узел',
        'c': 'Меньшая память',
        'd': 'Автоматическая сортировка',
        'correct': 'b',
    },
]

MINI_TASKS_1 = [
    {
        'title': 'Подсчитайте узлы',
        'description': (
            'В стартовом коде уже создан список из нескольких узлов. '
            'Допишите программу так, чтобы она вывела количество узлов в нём.'
        ),
        'starter_code': (
            '#include <iostream>\n'
            'using namespace std;\n\n'
            'struct Node { int value; Node* next; };\n\n'
            'int main() {\n'
            '    Node* head = new Node{10, new Node{20, new Node{30, new Node{40, nullptr}}}};\n'
            '    // Ваш код: посчитайте, сколько узлов в списке\n'
            '\n'
            '    return 0;\n'
            '}\n'
        ),
        'tests': [{'input': '', 'expected': '4'}],
        'hint': 'Двигайтесь по next от head, пока не увидите конец, и считайте шаги.',
    },
]

# ============================================================================
# ТЕОРИЯ 2 — std::list и типичные приёмы
# ============================================================================

THEORY_2 = """\
# std::list и типичные приёмы

## std::list: готовый связный список

Писать связный список руками каждый раз неудобно. Поэтому в стандартной
библиотеке C++ есть готовый `std::list`. Это **двусвязный** список: каждый
узел знает не только своего следующего соседа, но и предыдущего. Графически
это можно представить так:

```
nullptr <- [10] <-> [20] <-> [30] -> nullptr
```

Подключается через заголовок `<list>`. У этой структуры удобный API,
похожий на вектор, но с другим набором операций.

```cpp-run
// hint: list умеет добавлять в любой конец
#include <iostream>
#include <list>
using namespace std;

int main() {
    list<int> lst = {10, 20, 30};
    // поэкспериментируй: попробуй pop_front(), pop_back(), reverse()
    lst.push_back(40);     // в конец: [10, 20, 30, 40]
    lst.push_front(5);     // в начало: [5, 10, 20, 30, 40]

    for (int x : lst) cout << x << ' ';
    cout << endl;

    cout << "Размер: " << lst.size() << endl;
    return 0;
}
```

Самые ходовые методы:

| Метод            | Что делает                                  |
|------------------|----------------------------------------------|
| `push_back(x)`   | добавить `x` в конец                        |
| `push_front(x)`  | добавить `x` в начало                       |
| `pop_back()`     | удалить последний элемент                   |
| `pop_front()`    | удалить первый элемент                      |
| `size()`         | количество элементов                        |
| `reverse()`      | развернуть список на месте за O(n)          |
| `remove(x)`      | удалить **все** элементы со значением `x`   |

Важное различие с вектором: у `std::list` **нет** индексного доступа
`lst[i]`. И это сделано намеренно: такой доступ всё равно работал бы за
O(n), и иллюзия «дешёвой операции» только запутывала бы. Если нужно
обращаться по индексу, бери вектор.

[mini-task:0]

## Два указателя с разной скоростью

«Метод двух указателей» из модуля массивов в чистом виде на список не
переносится: мы не можем прыгать по индексам туда-сюда. Зато прекрасно
работает другая идея: **два указателя, идущих с разной скоростью**.

Аналогия. Представь беговую дорожку и двух бегунов: одного черепаху, она
бежит медленно, и одного зайца, он бежит вдвое быстрее. Стартуют оба
одновременно. Когда заяц добежит до конца, черепаха пройдёт ровно
половину пути. Это и есть классический способ найти середину списка за
один проход.

```cpp-run
// hint: алгоритм «заяц и черепаха» находит середину за один проход
#include <iostream>
#include <list>
using namespace std;

int main() {
    list<int> lst = {10, 20, 30, 40, 50, 60, 70};
    // поэкспериментируй: поменяй количество элементов и посмотри, как смещается середина
    auto slow = lst.begin();
    auto fast = lst.begin();

    while (fast != lst.end() && next(fast) != lst.end()) {
        ++slow;                  // черепаха делает один шаг
        ++fast;
        ++fast;                  // заяц делает два шага
    }
    cout << "Серединный элемент: " << *slow << endl;
    return 0;
}
```

`slow` шагает по одному узлу, `fast` сразу по два. Когда `fast`
дойдёт до конца списка (или до предпоследнего узла), `slow` окажется
ровно в середине. Не нужно знать длину списка заранее, а сложность
получается линейная.

Эту же идею (с небольшой модификацией) используют для:

- Поиска цикла в списке (алгоритм Флойда). Если в списке есть цикл, заяц рано или поздно «догонит» черепаху изнутри. Если цикла нет, заяц добежит до `nullptr`.
- Поиска k-го узла с конца: сначала «быстрый» уходит вперёд ровно на `k` узлов, потом оба идут синхронно. Когда быстрый дойдёт до конца, медленный окажется на k-м узле с конца.

[mini-task:1]

## Когда выбрать list, а когда vector

В абсолютном большинстве учебных и боевых задач подходит **vector**. Он
быстрее по доступу по индексу, компактнее в памяти, лучше дружит с кешем
процессора.

`std::list` стоит брать только тогда, когда:

- Ты часто вставляешь и удаляешь элементы **в середине** и при этом уже держишь итератор на нужный узел.
- Структура алгоритма натурально требует двусвязности (например, LRU-кэш, очередь с быстрым удалением произвольного элемента).

Если просто хранишь последовательность и обращаешься по индексу, бери
`vector`. Не «потому что список устарел», а потому что у `vector`
правильные характеристики для такой задачи.

## Итоги

- `std::list` это готовый двусвязный список из стандартной библиотеки. Подключается через `<list>`.
- Поддерживает добавление и удаление с обоих концов за O(1), но не поддерживает индексный доступ.
- Метод «два указателя с разной скоростью» позволяет находить середину списка и циклы за один проход, не зная длины.
- В большинстве задач `vector` лучше. `list` нужен в специфических случаях, когда важна вставка/удаление в середине по уже известному итератору.
"""

QUIZ_2 = [
    {
        'question': 'Чем std::list отличается от std::vector?',
        'a': 'Это то же самое',
        'b': 'list — это двусвязный список с O(1) на вставку/удаление по итератору, но O(n) на доступ по индексу',
        'c': 'list быстрее по любым операциям',
        'd': 'list хранит пары ключ-значение',
        'correct': 'b',
    },
    {
        'question': 'Что делает lst.remove(7)?',
        'a': 'Удаляет элемент с индексом 7',
        'b': 'Удаляет все элементы со значением 7',
        'c': 'Удаляет последний элемент 7',
        'd': 'Бросает исключение',
        'correct': 'b',
    },
    {
        'question': 'Какой подход не работает для std::list?',
        'a': 'Range-based for', 'b': 'lst.size()', 'c': 'lst[i]', 'd': 'lst.reverse()',
        'correct': 'c',
    },
    {
        'question': 'Как обнаружить цикл в односвязном списке за O(n) и O(1) памяти?',
        'a': 'Сохранить все узлы в std::set',
        'b': 'Двумя указателями: один идёт на шаг, другой на два; они встретятся, если есть цикл',
        'c': 'Развернуть список',
        'd': 'Это невозможно',
        'correct': 'b',
        'explanation': 'Это алгоритм Флойда «заяц и черепаха».',
    },
    {
        'question': 'Что обычно эффективнее, если нужен и доступ по индексу, и обход всей последовательности?',
        'a': 'std::list', 'b': 'std::vector', 'c': 'std::stack', 'd': 'std::queue',
        'correct': 'b',
    },
]

MINI_TASKS_2 = [
    {
        'title': 'Сумма списка',
        'description': (
            'Прочитайте `n` и `n` целых чисел. Сохраните их в `std::list<int>`, '
            'затем выведите их сумму.'
        ),
        'starter_code': (
            '#include <iostream>\n'
            '#include <list>\n'
            'using namespace std;\n\n'
            'int main() {\n'
            '    int n;\n'
            '    cin >> n;\n'
            '    list<int> lst;\n'
            '    // Ваш код\n'
            '\n'
            '    return 0;\n'
            '}\n'
        ),
        'tests': [
            {'input': '5\n1 2 3 4 5', 'expected': '15'},
            {'input': '1\n42', 'expected': '42'},
            {'input': '4\n-1 -2 -3 -4', 'expected': '-10'},
        ],
        'hint': 'Range-based for прекрасно работает и со std::list.',
    },
    {
        'title': 'Удалить значение',
        'description': (
            'Прочитайте `n`, `n` целых чисел и значение `x`. Удалите все вхождения '
            '`x` из списка и выведите оставшиеся элементы через пробел. '
            'Если ничего не осталось, выведите пустую строку.\n\n'
            '## Пример\n```\n6\n1 2 3 2 4 2\n2\n```\n```\n1 3 4\n```'
        ),
        'starter_code': (
            '#include <iostream>\n'
            '#include <list>\n'
            'using namespace std;\n\n'
            'int main() {\n'
            '    int n;\n'
            '    cin >> n;\n'
            '    list<int> lst;\n'
            '    for (int i = 0; i < n; ++i) { int v; cin >> v; lst.push_back(v); }\n'
            '    int x;\n'
            '    cin >> x;\n'
            '    // Ваш код\n'
            '\n'
            '    return 0;\n'
            '}\n'
        ),
        'tests': [
            {'input': '6\n1 2 3 2 4 2\n2', 'expected': '1 3 4'},
            {'input': '3\n1 2 3\n5', 'expected': '1 2 3'},
            {'input': '4\n7 7 7 7\n7', 'expected': ''},
        ],
        'hint': 'У std::list для этого есть готовый метод.',
    },
]

# ============================================================================
MODULE = {
    'title': 'Связные списки',
    'description': (
        'Узлы и связи между ними. Чем связный список отличается от массива, '
        'какие у него плюсы и минусы. Типичные приёмы работы со связными списками. '
        'Базовый материал для понимания деревьев и графов в следующих модулях.'
    ),
    'concepts': (
        'struct Node { int value; Node* next; }; указатели Node*, оператор ->, '
        'nullptr, обход списка через for (Node* cur = head; cur != nullptr; cur = cur->next); '
        'оператор new для создания узла и delete для освобождения памяти; '
        'std::list<int>: push_back, push_front, pop_back, pop_front, size, '
        'reverse(), remove(value), range-based for; '
        'метод «заяц и черепаха» (два указателя с разной скоростью) '
        'для поиска середины списка и k-го узла с конца'
    ),
    'order': 7,
    'theories': [
        {'title': 'Связный список как структура данных', 'content': THEORY_1, 'order': 0,
         'mini_tasks': MINI_TASKS_1, 'quiz': QUIZ_1},
        {'title': 'std::list и типичные приёмы', 'content': THEORY_2, 'order': 1,
         'mini_tasks': MINI_TASKS_2, 'quiz': QUIZ_2},
    ],
    'tasks': [
        # Уровень 1 — обход std::list
        {
            'title': 'Сумма списка',
            'description': (
                'Прочитайте `n` и `n` целых чисел. Используя `std::list<int>`, '
                'выведите их сумму.\n\n'
                '## Пример\n```\n5\n1 2 3 4 5\n```\n```\n15\n```'
            ),
            'difficulty': 1,
            'tests': [
                {'input': '5\n1 2 3 4 5', 'expected': '15'},
                {'input': '1\n42', 'expected': '42'},
                {'input': '4\n-1 -2 -3 -4', 'expected': '-10'},
            ],
            'hint': 'std::list поддерживает range-based for: for (int x : lst) sum += x;',
            'tags': ['linked-list'],
            'order': 0,
            'variations': [
                {
                    'title': 'Произведение списка',
                    'description': (
                        'Прочитайте `n` и `n` целых чисел. Используя `std::list<int>`, '
                        'выведите их произведение.\n\n'
                        '## Пример\n```\n4\n1 2 3 4\n```\n```\n24\n```'
                    ),
                    'tests': [
                        {'input': '4\n1 2 3 4', 'expected': '24'},
                        {'input': '3\n5 6 0', 'expected': '0'},
                        {'input': '1\n7', 'expected': '7'},
                        {'input': '3\n-2 3 -4', 'expected': '24'},
                    ],
                    'hint': 'То же что и для суммы, только умножение, а начальное значение равно единице.',
                    'tags': ['linked-list'],
                },
            ],
        },

        # Уровень 2 — модификация списка
        {
            'title': 'Развернуть список',
            'description': (
                'Прочитайте `n` и `n` целых чисел. Сохраните их в `std::list<int>`, '
                'затем разверните и выведите через пробел.\n\n'
                '## Пример\n```\n5\n1 2 3 4 5\n```\n```\n5 4 3 2 1\n```'
            ),
            'difficulty': 2,
            'tests': [
                {'input': '5\n1 2 3 4 5', 'expected': '5 4 3 2 1'},
                {'input': '1\n7', 'expected': '7'},
                {'input': '3\n-1 0 1', 'expected': '1 0 -1'},
            ],
            'hint': 'У std::list есть метод reverse(), он работает за O(n) и не аллоцирует память.',
            'tags': ['linked-list', 'pointers'],
            'order': 1,
            'variations': [
                {
                    'title': 'Удалить все вхождения значения',
                    'description': (
                        'Прочитайте `n`, `n` чисел, затем значение `x`. Удалите все '
                        'вхождения `x` из списка и выведите оставшиеся элементы через пробел. '
                        'Если ничего не осталось, выведите пустую строку.\n\n'
                        '## Пример\n```\n6\n1 2 3 2 4 2\n2\n```\n```\n1 3 4\n```'
                    ),
                    'tests': [
                        {'input': '6\n1 2 3 2 4 2\n2', 'expected': '1 3 4'},
                        {'input': '3\n1 2 3\n5', 'expected': '1 2 3'},
                        {'input': '4\n7 7 7 7\n7', 'expected': ''},
                    ],
                    'hint': 'У std::list есть метод remove(value), который удаляет все элементы равные value за O(n).',
                    'tags': ['linked-list'],
                },
            ],
        },

        # Уровень 3 — два указателя
        {
            'title': 'Найти середину списка',
            'description': (
                'Прочитайте `n` и `n` чисел. Выведите элемент, стоящий ровно посередине '
                'списка (для нечётного n берётся средний элемент; для чётного берётся нижний из двух средних, '
                'то есть с индексом n/2 - 1, считая с 0).\n\n'
                'Реализуйте двумя указателями за один проход.\n\n'
                '## Пример\n```\n5\n10 20 30 40 50\n```\n```\n30\n```'
            ),
            'difficulty': 3,
            'tests': [
                {'input': '5\n10 20 30 40 50', 'expected': '30'},
                {'input': '6\n1 2 3 4 5 6', 'expected': '3'},
                {'input': '1\n42', 'expected': '42'},
                {'input': '2\n10 20', 'expected': '10'},
            ],
            'hint': 'Идея «заяц и черепаха»: один указатель идёт на шаг, другой на два. Когда быстрый дойдёт до конца, медленный окажется в середине.',
            'tags': ['linked-list', 'two-pointers'],
            'order': 2,
            'variations': [
                {
                    'title': 'k-й узел с конца',
                    'description': (
                        'Прочитайте `n`, `n` целых чисел и значение `k` (1 ≤ k ≤ n). '
                        'Выведите элемент списка, который стоит на k-м месте **с конца**.\n\n'
                        '## Пример\n```\n5\n10 20 30 40 50\n2\n```\n```\n40\n```'
                    ),
                    'tests': [
                        {'input': '5\n10 20 30 40 50\n2', 'expected': '40'},
                        {'input': '5\n10 20 30 40 50\n1', 'expected': '50'},
                        {'input': '5\n10 20 30 40 50\n5', 'expected': '10'},
                        {'input': '3\n7 8 9\n3', 'expected': '7'},
                    ],
                    'hint': 'Два указателя: сначала «быстрый» уходит вперёд на k шагов, потом оба идут одновременно. Когда быстрый дойдёт до конца, медленный окажется на нужном месте.',
                    'tags': ['linked-list', 'two-pointers'],
                },
            ],
        },
    ],
}
