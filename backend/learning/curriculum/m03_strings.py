# -*- coding: utf-8 -*-
"""Модуль 3: Строки."""

# ============================================================================
# ТЕОРИЯ 1 — std::string и базовые операции
# ============================================================================

THEORY_1 = """\
# Строки: текст внутри программы

## Что такое строка

Строка это последовательность символов: одно слово, одна фраза или целый
абзац. В C++ для работы со строками есть удобный тип `std::string` из
заголовочного файла `<string>`. Снаружи строка выглядит как «текст», а
внутри это, по сути, тот же массив, только из отдельных символов.

Аналогия. Представь магнитные буквы на холодильнике, выстроенные в ряд:
«П», «Р», «И», «В», «Е», «Т». Каждая буква занимает свою клеточку, у неё
есть номер слева направо. Любую букву можно вытащить, заменить или
посмотреть. Строка в программе устроена так же.

```cpp-run
// hint: создаём и печатаем строку
#include <iostream>
#include <string>
using namespace std;

int main() {
    // поэкспериментируй: поменяй текст в кавычках, добавь ещё одну строку
    string greeting = "Привет";
    cout << greeting << endl;
    cout << "Длина: " << greeting.size() << endl;
    return 0;
}
```

Несколько мелочей, которые сразу полезно запомнить:

- Текст в строке окружают **двойными** кавычками `"..."`. Одинарные кавычки в C++ означают другое: одиночный символ типа `char`, например `'A'`. Это две разные вещи, и компилятор их строго различает.
- Метод `.size()` возвращает длину строки в символах (для ASCII-букв это и есть видимое число знаков).
- Заголовок `<string>` нужно подключить отдельно. Без него строки работать не будут.

## Чтение строки с клавиатуры

Здесь есть тонкость, которая часто удивляет новичков. В C++ для чтения
строки есть **два разных** способа, и они ведут себя по-разному:

- `cin >> s;` читает **одно слово** до ближайшего пробела или перевода строки.
- `getline(cin, s);` читает **всю строку целиком** до перевода строки, включая пробелы.

```cpp-run
// hint: введи фразу с пробелами и посмотри, что прочитается
#include <iostream>
#include <string>
using namespace std;

int main() {
    string word;
    cin >> word;       // прочтётся только первое слово
    cout << "Первое слово: " << word << endl;
    return 0;
}
```

Если запустить пример и ввести «привет мир», переменная `word` получит
только «привет». Чтобы прочитать всю фразу целиком вместе с пробелами,
заменяй `cin >> word;` на `getline(cin, word);`. Это базовое правило, и
оно действительно часто становится причиной первых багов в задачах на
строки.

## Длина и доступ к символам

Со строкой можно работать как с массивом. У каждого символа есть свой
индекс, который начинается с нуля. Длину строки возвращает метод `.size()`
(а ещё есть синоним `.length()`, делает то же самое).

```cpp-run
// hint: каждый символ адресуется по индексу, как в массиве
#include <iostream>
#include <string>
using namespace std;

int main() {
    string s = "hello";
    // поэкспериментируй: поменяй индекс, попробуй s[10] (там мусор!)
    cout << s[0] << endl;     // 'h', первый символ
    cout << s[4] << endl;     // 'o', последний символ
    cout << s.size() << endl; // 5, длина строки

    s[0] = 'H';               // меняем символ в первой ячейке
    cout << s << endl;        // получим "Hello"
    return 0;
}
```

Как и у обычного массива, выходить за границы строки нельзя. Если
обратиться к `s[100]` у строки длины 5, программа может вывести мусор
или упасть. Правило простое: допустимые индексы от 0 до `s.size() − 1`.

## Перебор символов

Часто нужно пройти по всем символам и что-нибудь посчитать или поменять.
Подойдёт обычный цикл `for` через индекс или короткий range-based for из
прошлого модуля:

```cpp-run
// hint: считаем, сколько в строке букв 'a'
#include <iostream>
#include <string>
using namespace std;

int main() {
    string s = "abacaba";
    int count_a = 0;
    // поэкспериментируй: поменяй искомую букву или строку
    for (char c : s) {
        if (c == 'a') ++count_a;
    }
    cout << "Букв 'a': " << count_a << endl;
    return 0;
}
```

Запись `for (char c : s)` читается как «для каждого символа `c` из строки
`s`». На каждой итерации в переменную `c` попадает очередной символ. Если
понадобится позиция символа, удобнее обычный цикл через `int i`.

[mini-task:0]

## Объединение и сравнение строк

Строки в C++ можно складывать оператором `+`. Такое склеивание называется
**конкатенацией** (от слова concatenate, «соединять в цепочку»):

```cpp-run
// hint: склеиваем строки в одну
#include <iostream>
#include <string>
using namespace std;

int main() {
    string first = "Code";
    string second = "Mentor";
    // поэкспериментируй: добавь между ними пробел, попробуй first + second + "!"
    string full = first + second;       // "CodeMentor"
    full += "!";                        // "CodeMentor!"
    cout << full << endl;
    return 0;
}
```

Сравнивать строки можно стандартными операторами `==`, `!=`, `<`, `>`.
Сравнение работает **лексикографически**, то есть в словарном порядке: как
если бы ты раскладывал слова по алфавиту в словаре. Например, `"abc" <
"abd"`, потому что в третьем символе `'c'` идёт раньше `'d'`. И `"ab" <
"abc"`, потому что более короткая строка считается «меньшей», если она
является началом более длинной.

[mini-task:1]

## Итоги

- `std::string` находится в заголовке `<string>`. Текст обрамляется двойными кавычками.
- `cin >> s` читает одно слово, `getline(cin, s)` читает всю строку с пробелами.
- К символам можно обращаться по индексу как у массива. Длина возвращается через `.size()`.
- `+` склеивает строки, `==`, `<`, `>` сравнивают их по словарному порядку.
- Многие приёмы из работы с массивами (цикл, накопитель, два указателя) точно так же работают и со строками.
"""

QUIZ_1 = [
    {
        'question': 'В чём разница между cin >> s и getline(cin, s)?',
        'a': 'getline быстрее',
        'b': 'cin >> читает одно слово до пробела, а getline читает целую строку',
        'c': 'getline работает только с числами',
        'd': 'Различий нет',
        'correct': 'b',
    },
    {
        'question': 'Какие кавычки используются для строки?',
        'a': 'Одинарные \'...\'', 'b': 'Двойные "..."', 'c': 'Обратные `...`', 'd': 'Без кавычек',
        'correct': 'b',
        'explanation': 'Одинарные кавычки используются для одиночного символа (тип char).',
    },
    {
        'question': 'Какой метод вернёт длину строки std::string?',
        'a': 's.length() или s.size()',
        'b': 'len(s)',
        'c': 'sizeof(s)',
        'd': 'count(s)',
        'correct': 'a',
    },
    {
        'question': 'Что выведет cout << ("abc" < "abd"); ?',
        'a': '0', 'b': '1', 'c': 'abc', 'd': 'Ошибка',
        'correct': 'b',
        'explanation': 'true преобразуется в 1; "abc" действительно идёт раньше "abd" в словаре.',
    },
    {
        'question': 'Какой оператор используется для объединения двух строк?',
        'a': 'concat()', 'b': '+', 'c': '&', 'd': '<<',
        'correct': 'b',
    },
]

MINI_TASKS_1 = [
    {
        'title': 'Подсчёт символа',
        'description': (
            'Прочитайте строку `s` (без пробелов) и символ `c`. Выведите, '
            'сколько раз символ `c` встречается в `s`.\n\n'
            '## Пример\n```\nbanana\na\n```\n```\n3\n```'
        ),
        'starter_code': (
            '#include <iostream>\n'
            '#include <string>\n'
            'using namespace std;\n\n'
            'int main() {\n'
            '    string s;\n'
            '    char c;\n'
            '    cin >> s >> c;\n'
            '    // Ваш код\n'
            '\n'
            '    return 0;\n'
            '}\n'
        ),
        'tests': [
            {'input': 'banana\na', 'expected': '3'},
            {'input': 'hello\nl', 'expected': '2'},
            {'input': 'abc\nz', 'expected': '0'},
        ],
        'hint': 'Перебери символы строки и считай совпадения с искомым.',
    },
    {
        'title': 'Соедини две строки',
        'description': (
            'Прочитайте две строки `a` и `b` (по одной на строке). Выведите их '
            'объединение, разделённое пробелом.\n\n'
            '## Пример\n```\nHello\nWorld\n```\n```\nHello World\n```'
        ),
        'starter_code': (
            '#include <iostream>\n'
            '#include <string>\n'
            'using namespace std;\n\n'
            'int main() {\n'
            '    string a, b;\n'
            '    cin >> a >> b;\n'
            '    // Ваш код\n'
            '\n'
            '    return 0;\n'
            '}\n'
        ),
        'tests': [
            {'input': 'Hello\nWorld', 'expected': 'Hello World'},
            {'input': 'foo\nbar', 'expected': 'foo bar'},
        ],
        'hint': 'Подумайте, как из двух строк собрать одну с разделителем.',
    },
]

# ============================================================================
# ТЕОРИЯ 2 — Манипуляции со строками
# ============================================================================

THEORY_2 = """\
# Манипуляции со строками

В реальных задачах со строкой обычно нужно что-то делать: достать кусочек,
найти в ней слово, перевести в нижний регистр, развернуть. К счастью, у
`std::string` для этого есть готовые методы, которые экономят много
времени.

## Подстрока

Метод `s.substr(pos, len)` возвращает **подстроку**: кусок исходной строки
длиной `len` символов, начинающийся с позиции `pos`.

Аналогия. Представь, что текст это длинная полоса бумаги, а `substr`
ножницы, которыми ты вырезаешь нужный кусочек, не трогая оригинал.

```cpp-run
// hint: вырезаем разные куски из одной строки
#include <iostream>
#include <string>
using namespace std;

int main() {
    string s = "Hello, World!";
    // поэкспериментируй: поменяй pos и len, попробуй s.substr(2)
    cout << s.substr(7, 5) << endl;   // "World"
    cout << s.substr(0, 5) << endl;   // "Hello"
    cout << s.substr(7)    << endl;   // "World!"  (без len берётся всё до конца)
    return 0;
}
```

Важный момент: `substr` возвращает **новую** строку, исходная `s` не
меняется. Если хочешь сохранить результат, присвой его в новую переменную:
`string part = s.substr(7, 5);`.

## Поиск подстроки

Метод `s.find(t)` возвращает индекс первого вхождения подстроки `t` в
строке `s`. Если ничего не нашлось, возвращается специальное значение
`string::npos`. Это очень большое число, которое используется как знак
«ничего не найдено».

```cpp-run
// hint: ищем подстроку, проверяем результат через string::npos
#include <iostream>
#include <string>
using namespace std;

int main() {
    string s = "abracadabra";
    // поэкспериментируй: ищи "abra", "xyz", "a", меняй строку s
    size_t i = s.find("cada");
    if (i != string::npos) {
        cout << "Нашли на позиции " << i << endl;
    } else {
        cout << "Нет" << endl;
    }
    return 0;
}
```

Тип `size_t` это просто беззнаковый целый тип. Можно использовать и `int`,
но тогда могут возникать предупреждения от компилятора. Главное правило:
после `find` обязательно сравнивай результат с `string::npos`, прежде чем
использовать его как индекс.

## Преобразование регистра

В C++ у строки нет «волшебного» метода `toLowerCase()`, как в некоторых
других языках. Регистр меняют посимвольно через функции `tolower(c)` и
`toupper(c)` из заголовка `<cctype>`.

```cpp-run
// hint: переводим всю строку в нижний регистр
#include <iostream>
#include <string>
#include <cctype>
using namespace std;

int main() {
    string s = "Hello WORLD";
    // поэкспериментируй: попробуй toupper, поменяй текст
    for (int i = 0; i < (int)s.size(); ++i) {
        s[i] = tolower(s[i]);
    }
    cout << s << endl;
    return 0;
}
```

Маленькое предупреждение: `tolower` и `toupper` хорошо работают только с
латинскими буквами (английским алфавитом). Для русских букв нужны другие
функции и настройки, которые зависят от операционной системы. В учебных
задачах ты обычно работаешь с латиницей, и переживать об этом не нужно.

[mini-task:0]

## Разворот строки

Поскольку строка по своей сути это массив символов, её легко перевернуть
задом наперёд. Самый удобный способ это вызвать готовую функцию
`std::reverse` из заголовка `<algorithm>`:

```cpp-run
// hint: разворачиваем строку одной строкой кода
#include <iostream>
#include <string>
#include <algorithm>
using namespace std;

int main() {
    string s = "abcde";
    // поэкспериментируй: поменяй строку, попробуй reverse у пустой строки
    reverse(s.begin(), s.end());
    cout << s << endl;     // получим "edcba"
    return 0;
}
```

Запись `s.begin()` и `s.end()` означает «начало строки» и «конец строки».
Функция `reverse` принимает любой такой диапазон и переворачивает его на
месте, прямо в исходной строке. Это та же `s`, просто содержимое уже
другое.

Развернуть строку можно и вручную через два указателя: пока левый меньше
правого, меняем местами `s[l]` и `s[r]`, потом сдвигаем оба к центру. Этот
приём пригодится в задаче про палиндром.

[mini-task:1]

## Когда строка ведёт себя как массив

Большинство приёмов из прошлого модуля переносятся на строки без
изменений: посчитать сумму чего-то, найти максимальный или минимальный
символ, использовать два указателя для проверки палиндрома, накопить
ответ в одном проходе. Если задача звучит как «пройди по символам и что-то
посчитай», скорее всего, решение это знакомый цикл с накопителем.

## Итоги

- `s.substr(pos, len)` возвращает новую подстроку, исходная строка не меняется.
- `s.find(t)` ищет подстроку и возвращает индекс или `string::npos`, если не нашлось.
- `tolower(c)` и `toupper(c)` из `<cctype>` меняют регистр одного символа. К строке применяются в цикле.
- `reverse(s.begin(), s.end())` из `<algorithm>` переворачивает строку на месте.
- Со строкой почти всегда можно работать как с массивом символов. Те же шаблоны кода, тот же подход.
"""

QUIZ_2 = [
    {
        'question': 'Что вернёт s.substr(2, 3) для строки s = "abcdef"?',
        'a': '"abc"', 'b': '"bcd"', 'c': '"cde"', 'd': '"def"',
        'correct': 'c',
        'explanation': 'Берём 3 символа начиная с индекса 2: c, d, e.',
    },
    {
        'question': 'Что возвращает s.find("xyz"), если такой подстроки нет?',
        'a': '-1', 'b': '0', 'c': 'string::npos', 'd': 'NULL',
        'correct': 'c',
    },
    {
        'question': 'Какая функция переводит символ в нижний регистр?',
        'a': 'lower(c)', 'b': 'tolower(c)', 'c': 'c.toLower()', 'd': 'c - 32',
        'correct': 'b',
    },
    {
        'question': 'Что произойдёт после reverse(s.begin(), s.end()) для s = "abc"?',
        'a': 's станет "cba"',
        'b': 'Создастся новая строка "cba", s не изменится',
        'c': 'Будет ошибка',
        'd': 's станет пустой',
        'correct': 'a',
        'explanation': 'reverse меняет содержимое строки на месте.',
    },
    {
        'question': 'Какой подход не работает для строки std::string?',
        'a': 'Перебор через range-based for',
        'b': 'Доступ через s[i]',
        'c': 'Складывание через s + t',
        'd': 'Превращение в число через приведение типа (int)s',
        'correct': 'd',
        'explanation': 'Просто привести строку к int нельзя, нужны функции stoi/stoll.',
    },
]

MINI_TASKS_2 = [
    {
        'title': 'Привести к нижнему регистру',
        'description': (
            'Прочитайте слово (без пробелов) из латинских букв в произвольном регистре '
            'и выведите его в нижнем регистре.\n\n'
            '## Пример\n```\nHelloWORLD\n```\n```\nhelloworld\n```'
        ),
        'starter_code': (
            '#include <iostream>\n'
            '#include <string>\n'
            '#include <cctype>\n'
            'using namespace std;\n\n'
            'int main() {\n'
            '    string s;\n'
            '    cin >> s;\n'
            '    // Ваш код\n'
            '\n'
            '    return 0;\n'
            '}\n'
        ),
        'tests': [
            {'input': 'HelloWORLD', 'expected': 'helloworld'},
            {'input': 'abc', 'expected': 'abc'},
            {'input': 'XYZ', 'expected': 'xyz'},
        ],
        'hint': 'Понадобится функция из `<cctype>`, применённая к каждому символу.',
    },
    {
        'title': 'Палиндром',
        'description': (
            'Прочитайте строку. Выведите `YES`, если она читается одинаково в обе '
            'стороны, иначе `NO`.\n\n'
            '## Пример\n```\nlevel\n```\n```\nYES\n```'
        ),
        'starter_code': (
            '#include <iostream>\n'
            '#include <string>\n'
            'using namespace std;\n\n'
            'int main() {\n'
            '    string s;\n'
            '    cin >> s;\n'
            '    // Ваш код\n'
            '\n'
            '    return 0;\n'
            '}\n'
        ),
        'tests': [
            {'input': 'level', 'expected': 'YES'},
            {'input': 'hello', 'expected': 'NO'},
            {'input': 'a', 'expected': 'YES'},
            {'input': 'abcba', 'expected': 'YES'},
            {'input': 'ab', 'expected': 'NO'},
        ],
        'hint': 'Это очень похоже на метод двух указателей из прошлого модуля.',
    },
]

# ============================================================================
MODULE = {
    'title': 'Строки',
    'description': (
        'Работа с текстом: чтение строк целиком, обработка отдельных символов, '
        'поиск подстрок и смена регистра. Большинство приёмов из работы с массивами '
        'применимы и к строкам.'
    ),
    'concepts': (
        'std::string, чтение через cin >> (одно слово) и getline(cin, s) (вся строка); '
        's.size() / s.length(), s[i], изменение символа s[i] = c; '
        'конкатенация через + и +=, сравнение строк ==, !=, <, >; '
        's.substr(pos, len), s.find(...), string::npos; '
        'std::reverse(s.begin(), s.end()) из <algorithm>; '
        'функции tolower, toupper, isdigit, isalpha из <cctype>; '
        'метод двух указателей для проверки палиндрома'
    ),
    'order': 3,
    'theories': [
        {'title': 'Знакомство со строками', 'content': THEORY_1, 'order': 0,
         'mini_tasks': MINI_TASKS_1, 'quiz': QUIZ_1},
        {'title': 'Манипуляции со строками', 'content': THEORY_2, 'order': 1,
         'mini_tasks': MINI_TASKS_2, 'quiz': QUIZ_2},
    ],
    'tasks': [
        # Уровень 1 — длина и перебор
        {
            'title': 'Длина строки',
            'description': (
                'Прочитайте строку (без пробелов) и выведите её длину.\n\n'
                '## Пример\n```\nhello\n```\n```\n5\n```'
            ),
            'difficulty': 1,
            'tests': [
                {'input': 'hello', 'expected': '5'},
                {'input': 'a', 'expected': '1'},
                {'input': 'codementor', 'expected': '10'},
            ],
            'hint': 'У std::string есть метод size() (или length()).',
            'tags': ['string'],
            'order': 0,
            'variations': [
                {
                    'title': 'Количество цифр в строке',
                    'description': (
                        'Прочитайте строку (без пробелов) и выведите, сколько в ней цифр '
                        '(символов от 0 до 9).\n\n'
                        '## Пример\n```\nabc12de3\n```\n```\n3\n```'
                    ),
                    'tests': [
                        {'input': 'abc12de3', 'expected': '3'},
                        {'input': 'abcdef', 'expected': '0'},
                        {'input': '12345', 'expected': '5'},
                        {'input': 'a1b2c3', 'expected': '3'},
                    ],
                    'hint': 'Перебери символы и проверяй их через `isdigit(c)` из `<cctype>` или сравнением с диапазоном `\'0\'..\'9\'`.',
                    'tags': ['string', 'loops'],
                },
            ],
        },

        # Уровень 2 — манипуляции с символами
        {
            'title': 'Развернуть строку',
            'description': (
                'Прочитайте строку и выведите её в обратном порядке.\n\n'
                '## Пример\n```\nhello\n```\n```\nolleh\n```'
            ),
            'difficulty': 2,
            'tests': [
                {'input': 'hello', 'expected': 'olleh'},
                {'input': 'a', 'expected': 'a'},
                {'input': 'abcd', 'expected': 'dcba'},
            ],
            'hint': 'Используй std::reverse(s.begin(), s.end()) или цикл с конца.',
            'tags': ['string'],
            'order': 1,
            'variations': [
                {
                    'title': 'Привести к нижнему регистру',
                    'description': (
                        'Прочитайте строку из латинских букв (без пробелов) и выведите её, '
                        'приведя все символы к нижнему регистру.\n\n'
                        '## Пример\n```\nHelloWORLD\n```\n```\nhelloworld\n```'
                    ),
                    'tests': [
                        {'input': 'HelloWORLD', 'expected': 'helloworld'},
                        {'input': 'abc', 'expected': 'abc'},
                        {'input': 'XYZ', 'expected': 'xyz'},
                        {'input': 'CodeMentor', 'expected': 'codementor'},
                    ],
                    'hint': 'Каждому символу можно применить `tolower(c)` из `<cctype>`.',
                    'tags': ['string', 'loops'],
                },
            ],
        },

        # Уровень 3 — два указателя / посимвольная замена
        {
            'title': 'Палиндром',
            'description': (
                'Прочитайте строку. Определите, является ли она палиндромом '
                '(читается одинаково в обе стороны). Выведите `да` или `нет`.\n\n'
                '## Пример\n```\nlevel\n```\n```\nда\n```'
            ),
            'difficulty': 3,
            'tests': [
                {'input': 'level', 'expected': 'да'},
                {'input': 'hello', 'expected': 'нет'},
                {'input': 'a', 'expected': 'да'},
                {'input': 'abcba', 'expected': 'да'},
                {'input': 'ab', 'expected': 'нет'},
            ],
            'hint': 'Сравнивай i-й символ с (n-1-i)-м, пока i < n/2.',
            'tags': ['string', 'two-pointers'],
            'order': 2,
            'variations': [
                {
                    'title': 'Замена символа в строке',
                    'description': (
                        'Прочитайте строку `s`, символ `a` и символ `b` (каждое на отдельной строке). '
                        'Замените все вхождения `a` на `b` и выведите результат.\n\n'
                        '## Пример\n```\nhello\nl\nL\n```\n```\nheLLo\n```'
                    ),
                    'tests': [
                        {'input': 'hello\nl\nL', 'expected': 'heLLo'},
                        {'input': 'aaa\na\nb', 'expected': 'bbb'},
                        {'input': 'abc\nz\nx', 'expected': 'abc'},
                    ],
                    'hint': 'Пройди по строке циклом и заменяй каждый s[i] == a на b.',
                    'tags': ['string'],
                },
            ],
        },
    ],
}
