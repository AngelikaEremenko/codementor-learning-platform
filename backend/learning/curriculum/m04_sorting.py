# -*- coding: utf-8 -*-
"""Модуль 4: Сортировки."""

# ============================================================================
# ТЕОРИЯ 1 — Зачем сортировать и как это делает std::sort
# ============================================================================

THEORY_1 = """\
# Сортировки: зачем и как

## Что значит «отсортировать массив»

Сортировка это упорядочивание элементов по какому-то правилу: чаще всего
от меньшего к большему, иногда наоборот. После сортировки массив выглядит
аккуратно, и с ним становится в разы удобнее работать.

Аналогия. Представь полку с книгами, расставленными в случайном порядке.
Чтобы найти нужную, придётся читать корешок каждой подряд. А если книги
стоят по алфавиту, нужную ты находишь почти сразу. С данными в программе
происходит ровно то же самое.

Зачем сортировать массив:

- Медиану (значение «по серединке») можно достать за один шаг: это элемент с индексом `n / 2`.
- `k` самых маленьких или самых больших чисел это просто первые или последние `k` элементов отсортированного массива.
- Дубликаты, если они есть, оказываются стоящими рядом. Достаточно один раз пройти по массиву и сравнить каждый элемент с предыдущим.
- Двоичный поиск (про него будет следующий модуль) вообще работает только на отсортированных массивах.

## std::sort: рабочая лошадка

В стандартной библиотеке C++ уже есть быстрая сортировка. Не нужно ничего
писать руками: подключаешь заголовок `<algorithm>` и зовёшь функцию `sort`.

```cpp-run
// hint: одна строка, и массив отсортирован
#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int main() {
    vector<int> v = {3, 1, 4, 1, 5, 9, 2, 6};
    // поэкспериментируй: поменяй элементы или отсортируй кусок массива
    sort(v.begin(), v.end());

    for (int x : v) cout << x << ' ';
    cout << endl;
    return 0;
}
```

Что значат `v.begin()` и `v.end()`. Первый указывает на начало массива
(первый элемент), второй на позицию **сразу после** последнего элемента.
Эта пара задаёт диапазон, который нужно отсортировать. Так устроены
почти все алгоритмы в стандартной библиотеке: они принимают начало и
«конец, который не включается».

Можно отсортировать не весь массив, а только его кусок. Например,
`sort(v.begin() + 2, v.begin() + 5)` отсортирует элементы с индексами 2,
3, 4. Это пригодится в задачах посложнее.

## Сортировка по убыванию

По умолчанию `sort` сортирует по возрастанию (от меньшего к большему).
Если нужно наоборот, передаём третьим аргументом готовый «компаратор»
`greater<int>()`:

```cpp-run
// hint: тот же sort, но с третьим аргументом
#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int main() {
    vector<int> v = {3, 1, 4, 1, 5};
    // поэкспериментируй: уберi greater<int>(), посмотри разницу
    sort(v.begin(), v.end(), greater<int>());

    for (int x : v) cout << x << ' ';
    cout << endl;
    return 0;
}
```

Запись `greater<int>()` это «функция сравнения, которая считает большие
числа меньшими». Звучит запутанно, но в работе достаточно запомнить шаблон
и использовать его.

## Сложность

У `std::sort` сложность **O(n log n)**. На пальцах это значит: для
миллиона элементов нужно около 20 миллионов операций, примерно секунда
работы. Это очень быстро по меркам алгоритмов.

Для сравнения: простые сортировки (пузырьковая, выбором), о которых пойдёт
речь в следующей теории, работают за O(n²). Для миллиона элементов это
уже **триллион** операций, то есть тысячи секунд. Поэтому в реальной
работе используют `std::sort`, а простые сортировки учат для понимания
устройства.

Линейного прохода O(n) при сортировке не бывает: чтобы упорядочить массив,
по нему всё равно нужно пройти много раз. O(n log n) это математически
доказанный минимум для общих сортировок сравнением.

[mini-task:0]

## Когда стандартный sort не подходит

Иногда в реальных задачах нужно отсортировать не числа, а структуры
(например, людей по возрасту, потом по имени). Иногда направление
сортировки зависит от пользовательского ввода. Почти всегда это решается
передачей в `std::sort` своей функции сравнения, и сам принцип работы
остаётся тем же. До таких случаев в этом курсе мы дойдём чуть позже.

## Итоги

- `sort(v.begin(), v.end())` базовый способ упорядочить массив по возрастанию.
- Сложность O(n log n) считается «быстрой». Для большинства учебных и боевых задач этого хватает.
- Третий аргумент `greater<int>()` меняет порядок на убывание.
- После сортировки многие задачи (медиана, поиск дубликатов, k-й по величине, двоичный поиск) решаются почти мгновенно.
"""

QUIZ_1 = [
    {
        'question': 'Какой заголовочный файл нужен для std::sort?',
        'a': '<sort>', 'b': '<algorithm>', 'c': '<vector>', 'd': '<numeric>',
        'correct': 'b',
    },
    {
        'question': 'Какая сложность std::sort?',
        'a': 'O(1)', 'b': 'O(n)', 'c': 'O(n log n)', 'd': 'O(n²)',
        'correct': 'c',
    },
    {
        'question': 'Что обозначает v.end() в sort(v.begin(), v.end())?',
        'a': 'Последний элемент',
        'b': 'Позицию ПОСЛЕ последнего элемента',
        'c': 'Конец массива в физической памяти',
        'd': 'Размер вектора',
        'correct': 'b',
        'explanation': 'Полуинтервал [begin, end) является стандартным соглашением в STL.',
    },
    {
        'question': 'Как отсортировать вектор v по убыванию?',
        'a': 'sort(v.begin(), v.end(), reverse)',
        'b': 'sort(v.begin(), v.end(), greater<int>())',
        'c': 'sort_descending(v)',
        'd': 'sort(v.end(), v.begin())',
        'correct': 'b',
    },
    {
        'question': 'Зачем сортировать массив, если можно просто перебрать?',
        'a': 'Только ради красоты',
        'b': 'Многие задачи (поиск k-го, медиана, бинарный поиск, дубликаты) после сортировки решаются проще',
        'c': 'Иначе массив занимает больше памяти',
        'd': 'sort снимает ограничения на размер',
        'correct': 'b',
    },
]

MINI_TASKS_1 = [
    {
        'title': 'Отсортируйте по возрастанию',
        'description': (
            'Прочитайте `n`, затем `n` целых чисел. Выведите их в порядке '
            'неубывания через пробел.\n\n'
            '## Пример\n```\n5\n3 1 4 1 5\n```\n```\n1 1 3 4 5\n```'
        ),
        'starter_code': (
            '#include <iostream>\n'
            '#include <vector>\n'
            '#include <algorithm>\n'
            'using namespace std;\n\n'
            'int main() {\n'
            '    int n;\n'
            '    cin >> n;\n'
            '    vector<int> v(n);\n'
            '    for (int i = 0; i < n; ++i) cin >> v[i];\n'
            '    // Ваш код\n'
            '\n'
            '    return 0;\n'
            '}\n'
        ),
        'tests': [
            {'input': '5\n3 1 4 1 5', 'expected': '1 1 3 4 5'},
            {'input': '1\n42', 'expected': '42'},
            {'input': '4\n-1 -3 -2 0', 'expected': '-3 -2 -1 0'},
        ],
        'hint': 'Готовая функция в <algorithm> сделает почти всё за вас.',
    },
]

# ============================================================================
# ТЕОРИЯ 2 — Простые сортировки за O(n²)
# ============================================================================

THEORY_2 = """\
# Простые сортировки: пузырьковая и сортировка выбором

`std::sort` это прекрасно, но всегда полезно понимать, как сортировка
устроена изнутри. Иначе она остаётся «магией», а магии в программировании
не бывает. В этой теории разберём две самые простые сортировки. Они
медленные (работают за O(n²)), зато идеи у них настолько прозрачные, что
можно проследить весь алгоритм глазами на маленьком массиве.

## Пузырьковая сортировка

Идея: проходим по массиву от начала до конца и сравниваем каждую пару
соседних элементов. Если они стоят «неправильно» (левый больше правого),
меняем их местами. Большие числа на каждом проходе сдвигаются вправо, как
пузырьки воздуха всплывают в стакане воды. Отсюда и название.

Аналогия. Представь шеренгу людей разного роста, которых попросили
выстроиться по возрастанию. Каждый смотрит на соседа справа: если сосед
ниже, они меняются местами. После одного прохода самый высокий человек
точно окажется в самом конце. Ещё один проход, и второй по высоте
займёт свою позицию. И так далее.

```cpp-run
// hint: посмотри, как меняется массив; добавь cout внутри цикла
#include <iostream>
#include <vector>
using namespace std;

int main() {
    vector<int> a = {5, 1, 4, 2, 8};
    int n = a.size();
    // поэкспериментируй: добавь cout вместе с состоянием массива после каждого прохода

    for (int i = 0; i < n - 1; ++i) {
        for (int j = 0; j < n - 1 - i; ++j) {
            if (a[j] > a[j + 1]) {
                swap(a[j], a[j + 1]);   // меняем соседей местами
            }
        }
    }

    for (int x : a) cout << x << ' ';
    cout << endl;
    return 0;
}
```

Почему внутренний цикл идёт до `n - 1 - i`, а не до `n - 1`. После первого
прохода в самом конце уже точно стоит максимум массива. После второго в
конце стоят два самых больших. После `i`-го прохода последние `i`
элементов уже на своих местах, и трогать их бессмысленно. Эта маленькая
оптимизация ускоряет алгоритм почти в два раза.

`std::swap(a, b)` это стандартная функция, которая обменивает значения
двух переменных. Если бы её не было, пришлось бы писать через временную
переменную: `int t = a; a = b; b = t;`. Под капотом `swap` делает то же
самое.

[mini-task:0]

## Сортировка выбором

Идея: на каждом шаге ищем самый маленький из ещё не разложенных элементов
и ставим его на правильное место.

Аналогия. Представь, что ты сортируешь карты в руке. Берёшь оставшуюся
кучку, ищешь в ней младшую карту и кладёшь её первой в готовый ряд. Из
оставшейся кучки снова ищешь младшую и кладёшь второй. И так пока кучка не
закончится.

```cpp-run
// hint: каждый шаг ищет минимум среди оставшихся и ставит на позицию i
#include <iostream>
#include <vector>
using namespace std;

int main() {
    vector<int> a = {5, 1, 4, 2, 8};
    int n = a.size();
    // поэкспериментируй: поменяй массив, попробуй уже отсортированный

    for (int i = 0; i < n; ++i) {
        int min_idx = i;                          // считаем, что минимум на позиции i
        for (int j = i + 1; j < n; ++j) {
            if (a[j] < a[min_idx]) min_idx = j;   // нашли что-то меньше, запомнили
        }
        swap(a[i], a[min_idx]);                   // ставим найденный минимум на позицию i
    }

    for (int x : a) cout << x << ' ';
    cout << endl;
    return 0;
}
```

Обрати внимание: мы не двигаем элементы по одному, а сразу делаем
**один** обмен на каждом шаге. Это часто экономит операции по сравнению
с пузырьковой, хотя сложность у обеих остаётся одинаковой.

[mini-task:1]

## Что важно понять

- Обе сортировки работают за O(n²). Это означает: для 100 элементов 10 тысяч операций (мгновенно), для 1000 уже миллион (терпимо), для 100 000 это 10 миллиардов (программа задумается надолго).
- В реальной жизни эти сортировки не используют, потому что есть `std::sort`. Но они показывают, что у задачи сортировки нет «магии»: можно собрать алгоритм пошагово, шаг за шагом, и он будет работать.
- Идея «пройти массив и поменять соседей, если они стоят не так» встречается в задачах в самых неожиданных местах. Например, это базовая операция в сортировках слиянием, в сортировке циклов и в нескольких алгоритмах на графах.

## Итоги

- Пузырьковая сортировка: проходим по массиву много раз, меняем местами соседей, если они стоят в неправильном порядке.
- Сортировка выбором: на каждом шаге находим минимум среди оставшихся и ставим его на текущую позицию.
- Обе работают за O(n²). Для больших данных это медленно, но идея очень прозрачная и важная для понимания того, как вообще устроены алгоритмы.
"""

QUIZ_2 = [
    {
        'question': 'Почему пузырьковая сортировка называется именно так?',
        'a': 'Из-за пузырей в реализации',
        'b': 'Большие элементы «всплывают» в конец массива',
        'c': 'Сложность похожа на квадрат',
        'd': 'Просто красивое название',
        'correct': 'b',
    },
    {
        'question': 'Какая сложность пузырьковой и сортировки выбором?',
        'a': 'O(1)', 'b': 'O(log n)', 'c': 'O(n)', 'd': 'O(n²)',
        'correct': 'd',
    },
    {
        'question': 'Что делает std::swap(a, b)?',
        'a': 'Складывает a и b',
        'b': 'Обменивает значения переменных',
        'c': 'Сравнивает a и b',
        'd': 'Удаляет одно из значений',
        'correct': 'b',
    },
    {
        'question': 'Что находит сортировка выбором на i-м шаге?',
        'a': 'Среднее значение',
        'b': 'Минимум в оставшейся части массива',
        'c': 'Максимум во всём массиве',
        'd': 'Количество перестановок',
        'correct': 'b',
    },
    {
        'question': 'Почему после k проходов пузырьковой можно сократить внутренний цикл?',
        'a': 'Чтобы код был короче',
        'b': 'Последние k элементов уже стоят на своих местах',
        'c': 'Это требование стандарта',
        'd': 'Никакой причины',
        'correct': 'b',
    },
]

MINI_TASKS_2 = [
    {
        'title': 'Пузырёк руками',
        'description': (
            'Прочитайте `n` и `n` целых чисел. Отсортируйте массив **сами**, '
            'не используя `std::sort`, и выведите его по неубыванию.\n\n'
            'Вам не запрещено использовать `swap`.'
        ),
        'starter_code': (
            '#include <iostream>\n'
            '#include <vector>\n'
            'using namespace std;\n\n'
            'int main() {\n'
            '    int n;\n'
            '    cin >> n;\n'
            '    vector<int> a(n);\n'
            '    for (int i = 0; i < n; ++i) cin >> a[i];\n'
            '    // Ваш код\n'
            '\n'
            '    for (int x : a) cout << x << \' \';\n'
            '    return 0;\n'
            '}\n'
        ),
        'tests': [
            {'input': '5\n3 1 4 1 5', 'expected': '1 1 3 4 5'},
            {'input': '4\n4 3 2 1', 'expected': '1 2 3 4'},
            {'input': '1\n7', 'expected': '7'},
        ],
        'hint': 'Понадобятся два вложенных цикла и условный обмен соседних элементов.',
    },
    {
        'title': 'Минимум в начало',
        'description': (
            'Прочитайте `n` и `n` целых чисел. **За один проход** найдите '
            'минимальный элемент и поменяйте его местами с первым. Выведите '
            'получившийся массив через пробел.\n\n'
            '## Пример\n```\n5\n3 1 4 5 1\n```\n```\n1 3 4 5 1\n```'
        ),
        'starter_code': (
            '#include <iostream>\n'
            '#include <vector>\n'
            'using namespace std;\n\n'
            'int main() {\n'
            '    int n;\n'
            '    cin >> n;\n'
            '    vector<int> a(n);\n'
            '    for (int i = 0; i < n; ++i) cin >> a[i];\n'
            '    // Ваш код\n'
            '\n'
            '    for (int x : a) cout << x << \' \';\n'
            '    return 0;\n'
            '}\n'
        ),
        'tests': [
            {'input': '5\n3 1 4 5 1', 'expected': '1 3 4 5 1'},
            {'input': '3\n10 20 30', 'expected': '10 20 30'},
            {'input': '4\n5 4 3 2', 'expected': '2 4 3 5'},
        ],
        'hint': 'Это первая итерация сортировки выбором. Запоминайте индекс минимума.',
    },
]

# ============================================================================
MODULE = {
    'title': 'Сортировки',
    'description': (
        'Зачем сортировать массивы и как это делается. На практике используется '
        'встроенная быстрая сортировка, а классические алгоритмы (пузырёк и сортировка '
        'выбором) разбираются для понимания того, как сортировка устроена изнутри.'
    ),
    'concepts': (
        'std::sort(v.begin(), v.end()) из <algorithm>; '
        'сортировка по убыванию через std::sort(v.begin(), v.end(), greater<int>()); '
        'std::reverse для разворота; std::swap(a, b) для обмена значений; '
        'пузырьковая сортировка (два вложенных цикла, обмен соседей); '
        'сортировка выбором (поиск минимума в оставшейся части и обмен)'
    ),
    'order': 4,
    'theories': [
        {'title': 'std::sort и зачем сортировать', 'content': THEORY_1, 'order': 0,
         'mini_tasks': MINI_TASKS_1, 'quiz': QUIZ_1},
        {'title': 'Простые сортировки руками', 'content': THEORY_2, 'order': 1,
         'mini_tasks': MINI_TASKS_2, 'quiz': QUIZ_2},
    ],
    'tasks': [
        # Уровень 1 — использование std::sort
        {
            'title': 'Сортировка по возрастанию',
            'description': (
                'Прочитайте `n` и `n` целых чисел. Выведите их по возрастанию через пробел.\n\n'
                '## Пример\n```\n5\n3 1 4 1 5\n```\n```\n1 1 3 4 5\n```'
            ),
            'difficulty': 1,
            'tests': [
                {'input': '5\n3 1 4 1 5', 'expected': '1 1 3 4 5'},
                {'input': '1\n42', 'expected': '42'},
                {'input': '4\n-1 -3 -2 0', 'expected': '-3 -2 -1 0'},
            ],
            'hint': 'std::sort(v.begin(), v.end()) даёт самое короткое решение.',
            'tags': ['sorting', 'vector'],
            'order': 0,
            'variations': [
                {
                    'title': 'Сортировка по убыванию',
                    'description': (
                        'Прочитайте `n` и `n` чисел. Выведите их по убыванию через пробел.\n\n'
                        '## Пример\n```\n5\n3 1 4 1 5\n```\n```\n5 4 3 1 1\n```'
                    ),
                    'tests': [
                        {'input': '5\n3 1 4 1 5', 'expected': '5 4 3 1 1'},
                        {'input': '3\n10 -10 0', 'expected': '10 0 -10'},
                        {'input': '1\n42', 'expected': '42'},
                    ],
                    'hint': 'Можно передать в `sort` третьим аргументом `greater<int>()` либо сначала отсортировать обычным образом и потом развернуть.',
                    'tags': ['sorting', 'vector'],
                },
            ],
        },

        # Уровень 2 — своя сортировка
        {
            'title': 'Пузырьковая сортировка',
            'description': (
                'Прочитайте `n` и `n` чисел. Отсортируйте их **пузырьковой сортировкой** '
                '(не используя `std::sort`) и выведите по возрастанию.\n\n'
                '## Пример\n```\n5\n3 1 4 1 5\n```\n```\n1 1 3 4 5\n```'
            ),
            'difficulty': 2,
            'tests': [
                {'input': '5\n3 1 4 1 5', 'expected': '1 1 3 4 5'},
                {'input': '4\n4 3 2 1', 'expected': '1 2 3 4'},
                {'input': '1\n7', 'expected': '7'},
            ],
            'hint': 'Внешний цикл i от 0 до n-1, внутренний j от 0 до n-1-i; меняем соседей если стоят неправильно.',
            'tags': ['sorting', 'bubble', 'array'],
            'order': 1,
            'variations': [
                {
                    'title': 'Сортировка выбором',
                    'description': (
                        'Прочитайте `n` и `n` чисел. Отсортируйте их **сортировкой выбором** '
                        '(не используя `std::sort`) и выведите по возрастанию.\n\n'
                        'Идея: на i-м шаге найти минимум в оставшейся части и поставить его на позицию i.\n\n'
                        '## Пример\n```\n5\n3 1 4 1 5\n```\n```\n1 1 3 4 5\n```'
                    ),
                    'tests': [
                        {'input': '5\n3 1 4 1 5', 'expected': '1 1 3 4 5'},
                        {'input': '4\n4 3 2 1', 'expected': '1 2 3 4'},
                        {'input': '3\n-1 0 -2', 'expected': '-2 -1 0'},
                    ],
                    'hint': 'На каждом шаге запоминайте индекс минимума в оставшейся части и меняйте его местами с текущим элементом.',
                    'tags': ['sorting', 'selection', 'array'],
                },
            ],
        },

        # Уровень 3 — сортировка как инструмент
        {
            'title': 'k-й по величине элемент',
            'description': (
                'Прочитайте `n` и `k`, затем `n` чисел. Выведите k-е по величине число '
                '(где 1 соответствует наибольшему, а n соответствует наименьшему).\n\n'
                '## Пример\n```\n5 2\n3 1 4 1 5\n```\n```\n4\n```'
            ),
            'difficulty': 3,
            'tests': [
                {'input': '5 2\n3 1 4 1 5', 'expected': '4'},
                {'input': '5 1\n3 1 4 1 5', 'expected': '5'},
                {'input': '5 5\n3 1 4 1 5', 'expected': '1'},
                {'input': '3 2\n10 20 30', 'expected': '20'},
            ],
            'hint': 'Отсортируй по убыванию и возьми элемент с индексом k-1.',
            'tags': ['sorting', 'vector'],
            'order': 2,
            'variations': [
                {
                    'title': 'Уникальные значения по возрастанию',
                    'description': (
                        'Прочитайте `n` и `n` целых чисел. Выведите уникальные значения '
                        'в порядке возрастания через пробел.\n\n'
                        '## Пример\n```\n7\n3 1 4 1 5 9 5\n```\n```\n1 3 4 5 9\n```'
                    ),
                    'tests': [
                        {'input': '7\n3 1 4 1 5 9 5', 'expected': '1 3 4 5 9'},
                        {'input': '5\n1 1 1 1 1', 'expected': '1'},
                        {'input': '4\n4 3 2 1', 'expected': '1 2 3 4'},
                    ],
                    'hint': 'После сортировки одинаковые элементы стоят рядом. Обходите массив и выводите элемент, только если он отличается от предыдущего.',
                    'tags': ['sorting', 'vector'],
                },
            ],
        },
    ],
}
