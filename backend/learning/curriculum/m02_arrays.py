# -*- coding: utf-8 -*-
"""Модуль 2: Массивы и std::vector."""

# ============================================================================
# ТЕОРИЯ 1 — Массив как структура данных
# ============================================================================

THEORY_1 = """\
# Массивы: как хранить много значений сразу

## Зачем нужен массив?

В предыдущем модуле мы хранили данные в отдельных переменных: `int a`,
`int b`, `int c`. Это нормально, пока чисел три или пять. А если нужно
сохранить оценки пятидесяти студентов? Писать `int a1, a2, ..., a50;`
неудобно, а ещё неудобнее потом обращаться к этим переменным в цикле,
потому что у обычной переменной нет «номера», по которому к ней можно
прийти.

Для таких ситуаций в C++ есть **массив**. Массив это коробка из ячеек
одного типа, лежащих в памяти строго подряд. У каждой ячейки есть свой
порядковый номер, который называется **индекс**. Зная индекс, можно за
одно действие прочитать или поменять любое значение.

Представь себе шкаф с пронумерованными ящичками: 0, 1, 2, 3, 4. Чтобы
достать содержимое нужного ящичка, ты не перебираешь все по очереди, а
сразу подходишь к нужному номеру. Массив устроен точно так же.

## Статический массив

Самая простая форма это **статический массив** заранее известного размера.
Слово «статический» означает, что длина задаётся один раз и больше не
меняется.

```cpp-run
// hint: массив из пяти чисел
#include <iostream>
using namespace std;

int main() {
    // поэкспериментируй: поменяй значения в фигурных скобках,
    // обратись к a[1] или a[4], попробуй присвоить a[0] = -7
    int a[5] = {3, 1, 4, 1, 5};
    cout << a[0] << endl;     // самый первый элемент
    cout << a[2] << endl;     // третий по счёту (с нуля!)
    a[2] = 99;                // поменяли значение в третьей ячейке
    cout << a[2] << endl;
    return 0;
}
```

Запись `int a[5]` читается так: «массив из пяти целых чисел с именем `a`».
А `a[2]` означает «значение в ячейке с индексом 2».

Несколько правил, которые поначалу легко забыть:

- Индексы в C++ начинаются с **нуля**, а не с единицы. То есть `a[0]` это
  первый элемент, `a[1]` второй, и так далее. У массива длины 5 индексы
  идут от 0 до 4 включительно, никаких `a[5]` там уже нет.
- Размер фиксируется в момент объявления и не меняется во время работы
  программы. Нельзя «дописать» шестой элемент, если объявлено пять.
- Если обратиться к несуществующему индексу (например, `a[10]` у массива
  длины 5), компилятор молча пропустит это, а во время запуска программа
  может вывести мусор, испортить чужие данные или упасть. Эта ошибка
  называется **выходом за границы массива** и считается одной из самых
  коварных в C++.

## Перебор массива

Чтобы что-то сделать со всеми элементами, обычно используют цикл `for`.
Идея простая: переменная-счётчик `i` пробегает все индексы от 0 до
размера минус один, а тело цикла обращается к ячейке `a[i]`.

```cpp-run
// hint: считаем сумму всех элементов массива
#include <iostream>
using namespace std;

int main() {
    int a[5] = {10, 20, 30, 40, 50};
    int sum = 0;
    // поэкспериментируй: поменяй числа или верхнюю границу 5 на 3
    for (int i = 0; i < 5; ++i) {
        sum += a[i];          // прибавляем очередной элемент к сумме
    }
    cout << "Сумма: " << sum << endl;
    return 0;
}
```

Обрати внимание на форму условия: `i < 5`, а не `i <= 5`. Если бы стояло
`<=`, цикл зашёл бы в несуществующую ячейку `a[5]` и получил бы мусор.
Поэтому в работе с массивами условие почти всегда выглядит как
`i < размер`.

Сама схема «завести переменную-накопитель и пополнять её в цикле» очень
важная. С её помощью считают сумму, произведение, количество подходящих
элементов и многое другое. Запомни этот шаблон, он встретится ещё много
раз.

[mini-task:0]

## Главное о статических массивах

- `тип имя[размер];` объявляет массив фиксированной длины.
- `имя[i]` это обращение к ячейке с индексом `i`. Индексы идут от `0` до `размер − 1`.
- Размер задаётся один раз и не меняется во время работы программы.
- За границы массива вылезать категорически нельзя. Если индекс может прийти от пользователя, всегда проверяй его перед обращением.

Статический массив отлично подходит для простых задач, где количество
элементов известно заранее. Но в алгоритмах часто бывает наоборот: размер
становится известен только во время работы, или элементы нужно добавлять
по одному. Для таких случаев в стандартной библиотеке есть удобный
динамический массив `std::vector`, о котором мы поговорим в следующей
теории.
"""

QUIZ_1 = [
    {
        'question': 'С какого индекса начинается нумерация элементов массива в C++?',
        'a': 'С 1', 'b': 'С 0', 'c': 'С -1', 'd': 'Зависит от типа',
        'correct': 'b',
        'explanation': 'В C++ индексы массива всегда начинаются с нуля.',
    },
    {
        'question': 'Какой индекс у последнего элемента массива int a[10];?',
        'a': '10', 'b': '11', 'c': '9', 'd': '0',
        'correct': 'c',
        'explanation': 'Длина 10 → индексы от 0 до 9.',
    },
    {
        'question': 'Что произойдёт при обращении a[100] в массиве из 5 элементов?',
        'a': 'Программа вернёт 0',
        'b': 'Будет ошибка компиляции',
        'c': 'Поведение не определено: может быть мусор, падение или ничего',
        'd': 'Массив автоматически расширится',
        'correct': 'c',
        'explanation': 'Выход за границы массива — типичная причина ошибок.',
    },
    {
        'question': 'Как объявить статический массив из 7 чисел типа int?',
        'a': 'int a(7);', 'b': 'int a[7];', 'c': 'array<int> a(7);', 'd': 'int[7] a;',
        'correct': 'b',
    },
    {
        'question': 'Какой цикл правильно проходит все элементы массива int a[n];?',
        'a': 'for (int i = 0; i <= n; ++i)',
        'b': 'for (int i = 1; i <= n; ++i)',
        'c': 'for (int i = 0; i < n; ++i)',
        'd': 'for (int i = 1; i < n; ++i)',
        'correct': 'c',
        'explanation': 'Индексы валидны от 0 до n-1, поэтому условие i < n.',
    },
]

MINI_TASKS_1 = [
    {
        'title': 'Сумма пяти чисел',
        'description': (
            'В стартовом коде уже объявлен массив из пяти целых чисел. '
            'Допишите программу так, чтобы она вывела их сумму.'
        ),
        'starter_code': (
            '#include <iostream>\n'
            'using namespace std;\n\n'
            'int main() {\n'
            '    int a[5] = {7, 2, 9, 4, 1};\n'
            '    // Ваш код\n'
            '\n'
            '    return 0;\n'
            '}\n'
        ),
        'tests': [{'input': '', 'expected': '23'}],
        'hint': 'Понадобится цикл и переменная-аккумулятор, в которой накапливается результат.',
    },
]

# ============================================================================
# ТЕОРИЯ 2 — std::vector
# ============================================================================

THEORY_2 = """\
# std::vector: массив, который умеет расти

## Чем vector лучше статического массива

`std::vector` это **динамический** массив из стандартной библиотеки C++.
Главное отличие от обычного массива в том, что ему не нужно заранее знать
размер. Он умеет расти и уменьшаться по мере необходимости. При этом
доступ к элементу по индексу остаётся таким же мгновенным, как у обычного
массива (сложность O(1)).

Бытовая аналогия. Статический массив это пенал на пять карандашей: больше
не вставишь, даже если очень захочется. `std::vector` это рюкзак: пока
вещи помещаются, кладёшь сколько угодно, рюкзак сам подбирает размер.

Чтобы пользоваться вектором, нужно подключить заголовок `<vector>`:

```cpp-run
// hint: vector умеет добавлять элементы
#include <iostream>
#include <vector>
using namespace std;

int main() {
    // поэкспериментируй: добавь ещё push_back, попробуй v.pop_back()
    vector<int> v;            // пустой вектор
    v.push_back(10);          // содержимое: [10]
    v.push_back(20);          // содержимое: [10, 20]
    v.push_back(30);          // содержимое: [10, 20, 30]

    cout << "Размер: " << v.size() << endl;
    cout << "Первый: " << v[0] << endl;
    cout << "Последний: " << v.back() << endl;
    return 0;
}
```

Запись `vector<int>` читается как «вектор целых чисел». В угловых скобках
указывается тип элементов: можно `vector<double>`, `vector<string>` и так
далее. Метод `push_back` добавляет новый элемент в самый конец вектора.

## Создание и инициализация

У вектора есть несколько способов создания на старте. Полезно сразу
выбрать тот, что лучше подходит к задаче.

```cpp-run
// hint: четыре разных способа создать вектор
#include <iostream>
#include <vector>
using namespace std;

int main() {
    vector<int> a;                    // пустой, размер 0
    vector<int> b(5);                 // 5 нулей: [0, 0, 0, 0, 0]
    vector<int> c(5, 7);              // 5 семёрок: [7, 7, 7, 7, 7]
    vector<int> d = {3, 1, 4, 1, 5};  // конкретные значения

    // поэкспериментируй: поменяй 5 на 10, поменяй 7 на -1
    cout << b[0] << ' ' << c[0] << ' ' << d[2] << endl;
    cout << "Размер a: " << a.size() << endl;
    cout << "Размер b: " << b.size() << endl;
    return 0;
}
```

Запомни три формы: `vector<int> v(n)` создаёт вектор из `n` нулей,
`vector<int> v(n, x)` создаёт `n` копий значения `x`, фигурные скобки с
конкретными значениями работают так же, как у обычного массива.

## Полезные методы

| Метод            | Что делает                                  |
|------------------|----------------------------------------------|
| `v.size()`       | возвращает количество элементов              |
| `v.empty()`      | `true`, если вектор пуст                     |
| `v.push_back(x)` | добавить `x` в конец                         |
| `v.pop_back()`   | удалить последний элемент                    |
| `v.front()`      | первый элемент                               |
| `v.back()`       | последний элемент                            |
| `v.clear()`      | удалить все элементы (размер станет 0)       |

Все эти методы вызываются через точку: сначала имя вектора, потом точка,
потом метод со скобками. Это общий способ обращения к функциям, которые
«живут внутри» объекта.

## Перебор вектора

Самый универсальный способ это обычный `for` через индекс. Он удобен,
когда нужно знать положение элемента (например, сравнивать соседей):

```cpp
for (int i = 0; i < (int)v.size(); ++i) {
    cout << v[i] << ' ';
}
```

Маленькая деталь: `v.size()` возвращает беззнаковое число, поэтому в
сравнении его принято приводить к `int` через `(int)v.size()`. Иначе
компилятор может выдать предупреждение.

Когда индексы не нужны, есть более короткая запись, которая называется
**range-based for**. Читается как «для каждого `x` из `v`»:

```cpp-run
// hint: распечатать все элементы вектора
#include <iostream>
#include <vector>
using namespace std;

int main() {
    vector<int> v = {10, 20, 30, 40};
    // поэкспериментируй: добавь push_back, поменяй значения, замени int x на int& x
    for (int x : v) {
        cout << x << ' ';
    }
    cout << endl;
    return 0;
}
```

Правило выбора простое. Нужны только значения и порядок не важен,
например, печать или поиск максимума, то берёшь range-based for. Нужны
сами индексы (сравнить `v[i]` с `v[i+1]`, поменять элемент через
присваивание), бери обычный `for` через `i`.

[mini-task:0]

## Чтение вектора с клавиатуры

В задачах очень часто встречается такой шаблон: сначала читаем число `n`
(сколько элементов будет), потом сами эти `n` элементов. Удобно сразу
создать вектор нужного размера и просто заполнить его в цикле.

```cpp-run
// hint: введи 3, потом три числа через пробел или Enter
#include <iostream>
#include <vector>
using namespace std;

int main() {
    int n;
    cin >> n;
    vector<int> v(n);                 // вектор сразу нужного размера, заполнен нулями
    for (int i = 0; i < n; ++i) {
        cin >> v[i];                  // читаем i-й элемент с клавиатуры
    }
    cout << "Прочитано " << n << " элементов." << endl;
    cout << "Первый: " << v[0] << ", последний: " << v.back() << endl;
    return 0;
}
```

Зачем создавать вектор сразу размера `n`, а не пустой? Так читается чуть
проще: индексы от 0 до `n − 1` уже существуют, и в них можно прямо писать
через `v[i] = ...`. Альтернативный вариант со `push_back` тоже работает,
просто чуть длиннее.

[mini-task:1]

## Итоги

- `std::vector` это динамический массив. Доступ по индексу за O(1), как у обычного.
- Размер вектора узнаётся через `.size()` и меняется через `push_back` и `pop_back`.
- Способы создания: пустой, заданного размера (нулями), заданного размера с одинаковым значением, через фигурные скобки.
- Чтение `n` элементов с клавиатуры это базовый шаблон, который встретится почти в каждой задаче.
- Range-based for короче, обычный `for` через индекс удобнее, когда нужны соседи или модификация.
"""

QUIZ_2 = [
    {
        'question': 'Какой заголовочный файл нужен для std::vector?',
        'a': '<array>', 'b': '<list>', 'c': '<vector>', 'd': '<map>',
        'correct': 'c',
    },
    {
        'question': 'Что вернёт v.size() после двух вызовов push_back?',
        'a': '0', 'b': '1', 'c': '2', 'd': 'Зависит от типа',
        'correct': 'c',
    },
    {
        'question': 'Как создать вектор из 100 элементов, каждый равен 7?',
        'a': 'vector<int> v[100] = 7;',
        'b': 'vector<int> v(100, 7);',
        'c': 'vector<int> v = 7 * 100;',
        'd': 'vector<int> v{0:100, 7};',
        'correct': 'b',
    },
    {
        'question': 'Сложность доступа v[i] у std::vector?',
        'a': 'O(1)', 'b': 'O(log n)', 'c': 'O(n)', 'd': 'Зависит от размера',
        'correct': 'a',
    },
    {
        'question': 'Что произойдёт при v.push_back(x), если внутренний буфер заполнен?',
        'a': 'Будет ошибка',
        'b': 'Вектор выделит новый, более крупный буфер и скопирует элементы',
        'c': 'Старый элемент будет удалён',
        'd': 'Элемент потеряется',
        'correct': 'b',
        'explanation': 'Это и есть «динамичность» вектора: он сам растёт по мере необходимости.',
    },
    {
        'question': 'Какой синтаксис правильный для прохода по элементам вектора без использования индексов?',
        'a': 'for (int x : v)', 'b': 'foreach (v)', 'c': 'for x in v', 'd': 'while (v.next())',
        'correct': 'a',
    },
]

MINI_TASKS_2 = [
    {
        'title': 'Удвойте элементы',
        'description': (
            'В стартовом коде уже создан вектор из нескольких чисел. '
            'Выведите через пробел значения, в которых каждый элемент удвоен.\n\n'
            '## Пример\n'
            'Если внутри лежит `{3, 5, 1}`, должно вывестись `6 10 2`.'
        ),
        'starter_code': (
            '#include <iostream>\n'
            '#include <vector>\n'
            'using namespace std;\n\n'
            'int main() {\n'
            '    vector<int> v = {3, 5, 1, 4, 9};\n'
            '    // Ваш код\n'
            '\n'
            '    return 0;\n'
            '}\n'
        ),
        'tests': [{'input': '', 'expected': '6 10 2 8 18'}],
        'hint': 'Достаточно перебрать вектор и для каждого элемента вывести нужное значение.',
    },
    {
        'title': 'Сумма n элементов',
        'description': (
            'Прочитайте число `n`, затем `n` целых чисел. Выведите их сумму.\n\n'
            '## Пример\n```\n4\n10 20 30 40\n```\n```\n100\n```'
        ),
        'starter_code': (
            '#include <iostream>\n'
            '#include <vector>\n'
            'using namespace std;\n\n'
            'int main() {\n'
            '    int n;\n'
            '    cin >> n;\n'
            '    // Ваш код\n'
            '\n'
            '    return 0;\n'
            '}\n'
        ),
        'tests': [
            {'input': '4\n10 20 30 40', 'expected': '100'},
            {'input': '1\n42', 'expected': '42'},
            {'input': '5\n-1 -2 -3 -4 -5', 'expected': '-15'},
        ],
        'hint': 'Создайте вектор размера n, прочитайте элементы циклом, а потом просуммируйте.',
    },
]

# ============================================================================
# ТЕОРИЯ 3 — Поиск максимума и метод двух указателей
# ============================================================================

THEORY_3 = """\
# Базовые алгоритмы на массивах

Когда умеешь хранить и перебирать элементы, открываются десятки полезных
приёмов. В этом уроке разберём два самых частых: поиск максимума за один
проход и метод двух указателей.

## Поиск максимума

Аналогия. Представь, что ты идёшь по магазину и хочешь запомнить самую
дорогую вещь. Ты не сравниваешь всё со всем (это было бы очень долго). Ты
просто держишь в голове «самое дорогое из того, что я уже видел» и
обновляешь это число, если встречаешь что-то ещё дороже. К концу обхода в
голове у тебя ответ.

```cpp-run
// hint: ищем максимум в векторе
#include <iostream>
#include <vector>
using namespace std;

int main() {
    vector<int> v = {3, 1, 4, 1, 5, 9, 2, 6};
    // поэкспериментируй: поменяй элементы, попробуй массив из одного числа
    int best = v[0];                  // считаем первый элемент «лучшим на сейчас»
    for (int i = 1; i < (int)v.size(); ++i) {
        if (v[i] > best) {
            best = v[i];              // нашли число побольше: обновляем рекорд
        }
    }
    cout << "Максимум: " << best << endl;
    return 0;
}
```

Заметь две детали:

- Стартуем цикл с `i = 1`, а не с `i = 0`. Потому что элемент с индексом 0 мы уже взяли как стартовый «рекорд» в `best`, нет смысла сравнивать его с самим собой.
- Инициализировать `best` нулём не стоит. Если массив состоит из отрицательных чисел, например `{-7, -3, -10}`, то нулевой `best` ошибочно останется ответом, хотя такого числа в массиве нет. Поэтому правильнее всегда брать первый элемент.

В стандартной библиотеке для поиска максимума уже есть готовая функция:

```cpp
#include <algorithm>
int m = *max_element(v.begin(), v.end());
```

Эта запись работает, и в реальном коде её часто используют. Но в учебных
задачах полезно уметь писать руками: цикл с обновлением лучшего значения
это базовый приём, на котором держатся многие алгоритмы.

[mini-task:0]

## Метод двух указателей

Часто встречается такая задача: «найди в массиве пару элементов с заданным
свойством». Самое прямолинейное решение это два вложенных цикла, которые
перебирают все пары. Сложность такого решения O(n²), и при больших `n` оно
работает медленно.

Если массив **отсортирован**, появляется красивый приём, который называется
**методом двух указателей**. Идея: заводим два индекса, один в начале
массива, другой в конце, и двигаем их навстречу друг другу. Получается
сложность O(n) вместо O(n²).

Аналогия. Представь прайс-лист, отсортированный по цене от дешёвых к
дорогим. Ты хочешь найти два товара, сумма цен которых равна, скажем, 9.
Если ты ткнул в самый дешёвый и самый дорогой и получил сумму больше 9,
значит, дорогой товар «слишком тяжёлый», его нужно заменить на тот, что
стоит чуть дешевле. Если сумма меньше 9, наоборот, дешёвый товар «слишком
лёгкий», берём вместо него следующий повыше. Так шаг за шагом обе границы
сходятся к ответу или встречаются в середине.

```cpp-run
// hint: ищем в отсортированном массиве пару с суммой 9
#include <iostream>
#include <vector>
using namespace std;

int main() {
    vector<int> v = {1, 3, 4, 5, 7};   // массив отсортирован по возрастанию
    int s = 9;
    // поэкспериментируй: поменяй s на 8, 10, 100, поменяй массив
    int l = 0, r = (int)v.size() - 1;
    bool found = false;
    while (l < r) {
        int sum = v[l] + v[r];
        if (sum == s) {
            cout << v[l] << " + " << v[r] << " = " << s << endl;
            found = true;
            break;
        }
        if (sum < s) ++l;   // сумма мала: двигаем левый вправо, чтобы увеличить
        else         --r;   // сумма велика: двигаем правый влево, чтобы уменьшить
    }
    if (!found) cout << "Пары нет" << endl;
    return 0;
}
```

Почему это правильный приём: массив отсортирован, поэтому слева лежат
меньшие числа, справа большие. Сдвиг `l` вправо может только **увеличить**
сумму (берём число побольше), сдвиг `r` влево может только **уменьшить**.
Значит, на каждом шаге мы движемся в нужную сторону и ни одну подходящую
пару не пропускаем.

Важное условие: массив обязательно должен быть отсортирован. Если порядок
произвольный, метод двух указателей в таком виде не сработает, и нужно
либо сначала отсортировать, либо применять другой подход (например, хеш-
таблицу, о которой расскажем в одном из следующих модулей).

[mini-task:1]

## Итоги

- Поиск максимума за один проход работает за O(n). Инициализируй переменную первым элементом массива, а не нулём.
- Метод двух указателей превращает квадрат в линейку, но требует отсортированного массива.
- Привычка пройти массив за один проход и аккуратно обновлять ответ это основа большинства алгоритмов в этом курсе.
"""

QUIZ_3 = [
    {
        'question': 'Какая сложность поиска максимума в неотсортированном массиве?',
        'a': 'O(1)', 'b': 'O(log n)', 'c': 'O(n)', 'd': 'O(n²)',
        'correct': 'c',
        'explanation': 'Чтобы быть уверенным, нужно посмотреть на каждый элемент.',
    },
    {
        'question': 'Чем плох вариант «инициализировать максимум нулём»?',
        'a': 'Это медленно',
        'b': 'Если все элементы отрицательные, ноль ошибочно станет ответом',
        'c': 'Это вообще не скомпилируется',
        'd': 'Ничем, это правильный подход',
        'correct': 'b',
    },
    {
        'question': 'Какое условие обязательно для применения метода двух указателей в задаче «пара с суммой s»?',
        'a': 'Массив должен быть из положительных чисел',
        'b': 'Массив должен быть отсортирован',
        'c': 'Размер должен быть степенью двойки',
        'd': 'Никаких условий',
        'correct': 'b',
    },
    {
        'question': 'Если v отсортирован по возрастанию, а sum = v[l] + v[r] меньше нужного s, что делаем?',
        'a': 'Двигаем правый указатель влево',
        'b': 'Двигаем левый указатель вправо',
        'c': 'Удваиваем sum',
        'd': 'Прерываем поиск',
        'correct': 'b',
        'explanation': 'Сдвиг l вправо берёт большее число и увеличивает сумму.',
    },
    {
        'question': 'Сложность метода двух указателей в задаче «пара с суммой s»?',
        'a': 'O(1)', 'b': 'O(log n)', 'c': 'O(n)', 'd': 'O(n²)',
        'correct': 'c',
    },
]

MINI_TASKS_3 = [
    {
        'title': 'Найдите минимум',
        'description': (
            'Прочитайте `n`, затем `n` целых чисел. Выведите наименьшее из них.\n\n'
            '## Пример\n```\n5\n3 1 4 1 5\n```\n```\n1\n```'
        ),
        'starter_code': (
            '#include <iostream>\n'
            '#include <vector>\n'
            'using namespace std;\n\n'
            'int main() {\n'
            '    int n;\n'
            '    cin >> n;\n'
            '    // Ваш код\n'
            '\n'
            '    return 0;\n'
            '}\n'
        ),
        'tests': [
            {'input': '5\n3 1 4 1 5', 'expected': '1'},
            {'input': '1\n42', 'expected': '42'},
            {'input': '4\n-3 -1 -8 -2', 'expected': '-8'},
        ],
        'hint': 'Идея похожа на поиск максимума, только сравнение в другую сторону.',
    },
    {
        'title': 'Пара с суммой',
        'description': (
            'Прочитайте `n` и `s`, затем `n` целых чисел в неубывающем порядке. '
            'Выведите `YES`, если в массиве есть два разных индекса i < j '
            'с суммой `a[i] + a[j] == s`, иначе `NO`. Сделайте это за один проход.\n\n'
            '## Пример\n```\n5 9\n1 3 4 5 7\n```\n```\nYES\n```'
        ),
        'starter_code': (
            '#include <iostream>\n'
            '#include <vector>\n'
            'using namespace std;\n\n'
            'int main() {\n'
            '    int n, s;\n'
            '    cin >> n >> s;\n'
            '    vector<int> v(n);\n'
            '    for (int i = 0; i < n; ++i) cin >> v[i];\n'
            '    // Ваш код\n'
            '\n'
            '    return 0;\n'
            '}\n'
        ),
        'tests': [
            {'input': '5 9\n1 3 4 5 7', 'expected': 'YES'},
            {'input': '5 100\n1 3 4 5 7', 'expected': 'NO'},
            {'input': '4 6\n1 2 3 4', 'expected': 'YES'},
            {'input': '3 10\n1 2 3', 'expected': 'NO'},
        ],
        'hint': 'Два указателя по краям массива двигаются навстречу. Цикл заканчивается, когда они встретились.',
    },
]

# ============================================================================
MODULE = {
    'title': 'Массивы и вектора',
    'description': (
        'Учимся хранить много значений сразу с помощью массивов и векторов. '
        'Перебор элементов, поиск максимума, обработка пар значений. Это базовая '
        'структура данных, на которой строится почти весь дальнейший материал курса.'
    ),
    'concepts': (
        'статические массивы int a[N]; std::vector<int>, vector<int>(n), '
        'vector<int>(n, val), v[i], v.size(), v.empty(), v.push_back, v.pop_back, '
        'v.back(), v.front(), v.clear(); перебор обычным for через индексы и '
        'range-based for; поиск максимума/минимума в один проход; '
        'метод двух указателей на отсортированном массиве'
    ),
    'order': 2,
    'theories': [
        {'title': 'Массивы: как хранить много значений', 'content': THEORY_1, 'order': 0,
         'mini_tasks': MINI_TASKS_1, 'quiz': QUIZ_1},
        {'title': 'std::vector — массив, который растёт', 'content': THEORY_2, 'order': 1,
         'mini_tasks': MINI_TASKS_2, 'quiz': QUIZ_2},
        {'title': 'Базовые алгоритмы на массивах', 'content': THEORY_3, 'order': 2,
         'mini_tasks': MINI_TASKS_3, 'quiz': QUIZ_3},
    ],
    'tasks': [
        # Уровень 1 — чтение и перебор массива
        {
            'title': 'Сумма массива',
            'description': (
                'Прочитайте `n`, затем `n` целых чисел. Выведите их сумму.\n\n'
                '## Пример\n```\n5\n1 2 3 4 5\n```\n```\n15\n```'
            ),
            'difficulty': 1,
            'tests': [
                {'input': '5\n1 2 3 4 5', 'expected': '15'},
                {'input': '1\n42', 'expected': '42'},
                {'input': '4\n-1 -2 -3 -4', 'expected': '-10'},
                {'input': '3\n1000000000 1000000000 1000000000', 'expected': '3000000000'},
            ],
            'hint': 'Аккумулируй сумму в long long, чтобы избежать переполнения.',
            'tags': ['array', 'vector', 'loops'],
            'order': 0,
            'variations': [
                {
                    'title': 'Произведение массива',
                    'description': (
                        'Прочитайте `n` и `n` целых чисел. Выведите их произведение.\n\n'
                        '## Пример\n```\n4\n1 2 3 4\n```\n```\n24\n```'
                    ),
                    'tests': [
                        {'input': '4\n1 2 3 4', 'expected': '24'},
                        {'input': '3\n5 6 0', 'expected': '0'},
                        {'input': '1\n7', 'expected': '7'},
                        {'input': '3\n-2 3 -4', 'expected': '24'},
                    ],
                    'hint': 'Тот же приём, что и для суммы: заводим аккумулятор и обновляем его в цикле. Только начинать стоит с 1, а не с 0.',
                },
            ],
        },

        # Уровень 2 — аккумулятор/условие в цикле
        {
            'title': 'Максимум массива',
            'description': (
                'Прочитайте `n` и `n` целых чисел. Выведите наибольшее из них.\n\n'
                '## Пример\n```\n5\n3 1 4 1 5\n```\n```\n5\n```'
            ),
            'difficulty': 2,
            'tests': [
                {'input': '5\n3 1 4 1 5', 'expected': '5'},
                {'input': '1\n-100', 'expected': '-100'},
                {'input': '4\n-5 -3 -8 -1', 'expected': '-1'},
            ],
            'hint': 'Инициализируй максимум первым элементом, потом сравнивай с остальными.',
            'tags': ['array', 'vector', 'loops'],
            'order': 1,
            'variations': [
                {
                    'title': 'Минимум массива',
                    'description': (
                        'Прочитайте `n` и `n` целых чисел. Выведите наименьшее из них.\n\n'
                        '## Пример\n```\n5\n3 1 4 1 5\n```\n```\n1\n```'
                    ),
                    'tests': [
                        {'input': '5\n3 1 4 1 5', 'expected': '1'},
                        {'input': '1\n42', 'expected': '42'},
                        {'input': '4\n-3 -1 -8 -2', 'expected': '-8'},
                    ],
                    'hint': 'Идея та же, что у максимума, только сравнение в другую сторону.',
                },
            ],
        },

        # Уровень 3 — два указателя
        {
            'title': 'Пара с заданной суммой',
            'description': (
                'Дан отсортированный по неубыванию массив из `n` чисел и значение `s`. '
                'Определите, есть ли в массиве два элемента, сумма которых равна `s`. '
                'Выведите `да` или `нет`. Решение должно работать за O(n).\n\n'
                '## Пример\n```\n5 9\n1 3 4 5 7\n```\n```\nда\n```'
            ),
            'difficulty': 3,
            'tests': [
                {'input': '5 9\n1 3 4 5 7', 'expected': 'да'},
                {'input': '5 100\n1 3 4 5 7', 'expected': 'нет'},
                {'input': '2 0\n-5 5', 'expected': 'да'},
                {'input': '4 6\n1 2 3 4', 'expected': 'да'},
                {'input': '3 10\n1 2 3', 'expected': 'нет'},
            ],
            'hint': 'Поставь левый указатель в начало, правый в конец. Если сумма меньше s, двигай левый, а если больше, двигай правый.',
            'tags': ['array', 'vector', 'two-pointers'],
            'order': 2,
            'variations': [
                {
                    'title': 'Пара с заданной разностью',
                    'description': (
                        'Дан отсортированный по неубыванию массив из `n` чисел и значение `d` (d ≥ 0). '
                        'Определите, есть ли в массиве два разных элемента, разность которых равна `d` '
                        '(больший минус меньший). Выведите `да` или `нет`. Решение должно работать за O(n).\n\n'
                        '## Пример\n```\n5 3\n1 3 4 6 7\n```\n```\nда\n```'
                    ),
                    'tests': [
                        {'input': '5 3\n1 3 4 6 7', 'expected': 'да'},
                        {'input': '5 10\n1 3 4 6 7', 'expected': 'нет'},
                        {'input': '4 0\n1 1 2 3', 'expected': 'да'},
                        {'input': '3 5\n1 2 3', 'expected': 'нет'},
                    ],
                    'hint': 'Идея похожа на «пару с суммой», но оба указателя идут в одном направлении: правый ускоряет, левый догоняет, поддерживая разность.',
                    'tags': ['array', 'vector', 'two-pointers'],
                },
            ],
        },
    ],
}
