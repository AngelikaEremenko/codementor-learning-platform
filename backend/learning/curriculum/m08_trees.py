# -*- coding: utf-8 -*-
"""Модуль 8: Деревья и обходы."""

# ============================================================================
# ТЕОРИЯ 1 — Что такое дерево
# ============================================================================

THEORY_1 = """\
# Деревья: иерархия данных

## Что такое дерево

Дерево это структура данных, которая описывает **иерархию**: один главный
элемент сверху, под ним подчинённые, под ними свои подчинённые, и так
далее.

Аналогии вокруг нас:

- Семейное древо: прадед, дед, отец, дети, внуки.
- Папки в файловой системе: один корневой диск, в нём папки, внутри них опять папки и файлы.
- Структура компании: генеральный директор, под ним руководители отделов, под ними сотрудники.

Графически дерево обычно рисуют так:

```
        10
       /  \\
      5    20
     / \\     \\
    3   7    25
```

Сверху сидит **корень** (`10`). От него идут стрелки вниз к его **детям**
(потомкам). У каждого ребёнка могут быть свои дети, и так до самых
нижних узлов, у которых детей нет. Такие узлы без детей называются
**листьями**.

Главное правило, отличающее дерево от любой другой структуры: **никаких
циклов**. От корня к любому узлу есть **ровно один** путь, не больше и
не меньше. Если бы в структуре был цикл (можно было бы вернуться к
предку), она бы называлась графом, а не деревом.

Ещё немного словарика:

- **Узел** это место, где лежит значение и от которого идут связи.
- У каждого узла, кроме корня, есть **родитель** (только один). У корня родителя нет.
- **Поддерево узла** это сам узел и всё, что висит под ним.
- **Высота дерева** это длина самого длинного пути от корня до листа.

## Двоичное дерево

Самый частый и удобный случай это **двоичное дерево**, в котором у каждого
узла не больше двух потомков: левый и правый. Если у узла только один
потомок, второй просто отсутствует. Если оба отсутствуют, узел лист.

В коде двоичный узел выглядит точно так же, как узел связного списка из
прошлого модуля, только указателей теперь два:

```cpp-run
// hint: создаём дерево вручную и обращаемся к узлам
#include <iostream>
using namespace std;

struct Node {
    int value;     // значение в узле
    Node* left;    // указатель на левого ребёнка (или nullptr)
    Node* right;   // указатель на правого ребёнка (или nullptr)
};

int main() {
    // строим дерево: корень 10, слева 5 (с детьми 3 и 7), справа 20
    Node* root = new Node{10, nullptr, nullptr};
    root->left  = new Node{5,  nullptr, nullptr};
    root->right = new Node{20, nullptr, nullptr};
    root->left->left  = new Node{3, nullptr, nullptr};
    root->left->right = new Node{7, nullptr, nullptr};
    // поэкспериментируй: добавь ребёнка справа от 20, поменяй значения

    cout << "Корень: " << root->value << endl;
    cout << "Левый потомок корня: " << root->left->value << endl;
    cout << "Правый потомок корня: " << root->right->value << endl;
    cout << "Левый внук слева: " << root->left->left->value << endl;
    return 0;
}
```

Это та же логика «узел + указатели», что и у связного списка, просто у
каждого узла теперь два «следующих»: лево и право. Дальше всё строится из
этой одной идеи.

## Двоичное дерево поиска (BST)

Если на обычное двоичное дерево наложить одно простое правило, получается
очень полезная структура **двоичное дерево поиска**, или **BST** (Binary
Search Tree). Правило такое:

> Для любого узла все значения в **левом** поддереве **меньше** его
> значения, а все значения в **правом** поддереве **больше**.

```
        10           ← корень
       /  \\
      5    20        ← всё слева меньше 10, всё справа больше 10
     / \\
    3   7
```

Что это даёт. Когда мы ищем число `x` в BST, мы каждый раз решаем, идти
влево или вправо, просто сравнивая `x` со значением в текущем узле. Это
очень похоже на двоичный поиск из пятого модуля. В среднем для
сбалансированного дерева получается O(log n) шагов. Это очень быстро.

Маленькая предостерегающая деталь. Если вставлять в BST уже
отсортированные числа (например, 1, 2, 3, 4, 5), дерево вырождается в
длинную цепочку:

```
1 → 2 → 3 → 4 → 5
```

И тогда поиск превращается в линейный, O(n). Эту проблему решают
балансированные деревья (AVL, красно-чёрные), о которых рассказывают
позже. В этом курсе нам хватит обычного BST с разумными данными.

[mini-task:0]

## Высота и размер

С деревьями постоянно нужно считать два свойства:

- **Размер дерева** это общее количество узлов. Формула проста и красива: `size(v) = 1 + size(v->left) + size(v->right)`. Сам узел плюс размер левого поддерева плюс размер правого.
- **Высота дерева** это длина самого длинного пути от корня до листа в рёбрах. Считается рекурсивно: `height(v) = 1 + max(height(left), height(right))`. У пустого поддерева высота обычно считается `-1` (или `0`, единого стандарта нет, главное определиться и не путаться).

Обрати внимание на одну общую идею: и размер, и высота вычисляются
**рекурсивно** через те же значения у детей. Это лейтмотив всей работы с
деревьями. Почти любую задачу про дерево решают рекурсивной функцией с
базой «если узел `nullptr`, вернуть что-то простое».

```cpp-run
// hint: считаем высоту рекурсивно
#include <iostream>
using namespace std;

struct Node { int value; Node* left; Node* right; };

int height(Node* v) {
    if (v == nullptr) return -1;             // база: пустого поддерева как бы нет
    int hl = height(v->left);                // высота левого
    int hr = height(v->right);               // высота правого
    return 1 + (hl > hr ? hl : hr);          // плюс один за текущий узел
}

int main() {
    Node* root = new Node{1,
        new Node{2, new Node{4, nullptr, nullptr}, nullptr},
        new Node{3, nullptr, nullptr}};
    // поэкспериментируй: добавь ещё один уровень и посмотри, как меняется высота
    cout << "Высота: " << height(root) << endl;   // 2
    return 0;
}
```

[mini-task:1]

## Итоги

- Дерево это узлы с детьми и одним родителем. Циклов в дереве нет: к каждому узлу ведёт ровно один путь от корня.
- Двоичное дерево это дерево, где у узла не больше двух детей (левый и правый).
- BST добавляет правило «слева меньше, справа больше», благодаря которому поиск в нём в среднем работает за O(log n).
- Большинство характеристик дерева (размер, высота, сумма, количество листьев) считаются простой рекурсивной функцией с базой при `nullptr`.
"""

QUIZ_1 = [
    {
        'question': 'Что такое лист в дереве?',
        'a': 'Корневой узел',
        'b': 'Узел без потомков',
        'c': 'Узел с двумя потомками',
        'd': 'Любой внутренний узел',
        'correct': 'b',
    },
    {
        'question': 'Какое правило отличает BST от обычного двоичного дерева?',
        'a': 'У узла не больше двух потомков',
        'b': 'В левом поддереве лежат меньшие значения, а в правом лежат бо́льшие',
        'c': 'Все значения уникальны',
        'd': 'В корне всегда лежит наибольшее значение',
        'correct': 'b',
    },
    {
        'question': 'Какова средняя сложность поиска в сбалансированном BST?',
        'a': 'O(1)', 'b': 'O(log n)', 'c': 'O(n)', 'd': 'O(n²)',
        'correct': 'b',
    },
    {
        'question': 'Что произойдёт с поиском, если BST выродился в длинную цепочку?',
        'a': 'Всё равно O(log n)',
        'b': 'Сложность станет O(n)',
        'c': 'Программа упадёт',
        'd': 'Ничего',
        'correct': 'b',
    },
    {
        'question': 'Как обычно пишут код «обхода» дерева?',
        'a': 'Тремя вложенными циклами',
        'b': 'Через рекурсию (или явный стек)',
        'c': 'Бинарным поиском',
        'd': 'Только итеративно',
        'correct': 'b',
    },
]

MINI_TASKS_1 = [
    {
        'title': 'Сумма всех значений',
        'description': (
            'В стартовом коде уже создано небольшое дерево. Допишите рекурсивную '
            'функцию `tree_sum`, которая возвращает сумму значений всех узлов, '
            'и выведите её результат.'
        ),
        'starter_code': (
            '#include <iostream>\n'
            'using namespace std;\n\n'
            'struct Node { int value; Node* left; Node* right; };\n\n'
            'int tree_sum(Node* v) {\n'
            '    // Ваш код\n'
            '\n'
            '}\n\n'
            'int main() {\n'
            '    Node* root = new Node{1,\n'
            '        new Node{2, new Node{4, nullptr, nullptr}, nullptr},\n'
            '        new Node{3, nullptr, new Node{5, nullptr, nullptr}}};\n'
            '    cout << tree_sum(root);\n'
            '    return 0;\n'
            '}\n'
        ),
        'tests': [{'input': '', 'expected': '15'}],
        'hint': 'Любой обход дерева начинается с базового случая «нулевой узел». На шаге комбинируйте значение узла и результаты для детей.',
    },
]

# ============================================================================
# ТЕОРИЯ 2 — Обходы дерева (DFS и BFS)
# ============================================================================

THEORY_2 = """\
# Обходы дерева: DFS и BFS

С массивами всё было просто: пробежал циклом от `0` до `n − 1` и
посмотрел на каждый элемент. С деревом так нельзя: у узлов нет
порядкового номера, и «индекс следующего» не определён. Поэтому для
дерева есть два главных способа обхода: **DFS** (поиск в глубину) и
**BFS** (поиск в ширину). Они появляются в десятках задач, в обходах
графов, в поиске путей и так далее. Эти два слова стоит знать наизусть.

## DFS: поиск в глубину

Идея простая: от корня идём максимально глубоко вниз, пока есть куда.
Только когда упёрлись в `nullptr` или в лист, возвращаемся и идём в
следующего непосещённого ребёнка. Естественнее всего записывать DFS
рекурсивной функцией.

Аналогия. Представь, что ты в лабиринте с фонариком и идёшь по правилу
«всегда сворачивай налево». Дошёл до тупика, развернулся, отступил и
пошёл по следующему повороту. Получится глубокое погружение в каждый
коридор перед переходом к следующему.

У DFS есть **три варианта порядка обработки** узла. Они отличаются
только тем, **когда** мы выводим значение самого узла: до или после
обхода детей.

| Обход      | Порядок                  | Где применяется                              |
|------------|---------------------------|-----------------------------------------------|
| Pre-order  | узел → лево → право       | копирование дерева, печать «сверху вниз»     |
| In-order   | лево → узел → право       | для BST выводит элементы по возрастанию      |
| Post-order | лево → право → узел       | удаление узлов, подсчёт от листьев           |

```cpp-run
// hint: in-order обход BST выдаёт значения по возрастанию
#include <iostream>
using namespace std;

struct Node { int value; Node* left; Node* right; };

void inorder(Node* v) {
    if (v == nullptr) return;       // база рекурсии: пустое поддерево
    inorder(v->left);               // сначала левый
    cout << v->value << ' ';        // потом сам узел
    inorder(v->right);              // потом правый
    // поэкспериментируй: переставь эти три строки местами и посмотри, как меняется вывод
}

int main() {
    Node* root = new Node{4,
        new Node{2, new Node{1, nullptr, nullptr}, new Node{3, nullptr, nullptr}},
        new Node{6, new Node{5, nullptr, nullptr}, new Node{7, nullptr, nullptr}}};
    inorder(root);                  // 1 2 3 4 5 6 7
    cout << endl;
    return 0;
}
```

Эту функцию очень полезно прочувствовать. Если поменять три строки внутри
местами, получится **другой** обход. Сначала узел и потом дети это
pre-order. Сначала оба ребёнка и в конце узел это post-order. На таком
маленьком примере хорошо видна вся механика DFS.

Важно: у любой рекурсивной функции на дереве есть **база** при `nullptr`.
Без неё функция уйдёт в бесконечную рекурсию и программа упадёт. Это
правило номер один.

[mini-task:0]

## BFS: поиск в ширину

Идея противоположная: сначала смотрим на корень, потом на всех его
детей, потом на всех «внуков», и так уровень за уровнем. Дерево
обходится горизонтальными слоями, сверху вниз.

Аналогия. Представь, что в комнате стоит толпа людей. Ты называешь
имя, человек выходит вперёд, потом он называет имена своих друзей, они
выходят следом, и так далее. Получается, что ты «рассказываешь
новость» сначала ближайшим, потом их соседям, потом соседям их соседей.

BFS естественнее всего реализуется через **очередь**. Модуль про стек
и очередь был не зря.

```cpp-run
// hint: BFS обходит дерево уровнями, используя очередь
#include <iostream>
#include <queue>
using namespace std;

struct Node { int value; Node* left; Node* right; };

void bfs(Node* root) {
    if (!root) return;
    queue<Node*> q;
    q.push(root);                              // начали с корня
    while (!q.empty()) {
        Node* v = q.front(); q.pop();          // берём очередной узел из головы
        cout << v->value << ' ';
        if (v->left)  q.push(v->left);         // в хвост складываем непустых детей
        if (v->right) q.push(v->right);
    }
}

int main() {
    Node* root = new Node{1,
        new Node{2, new Node{4, nullptr, nullptr}, new Node{5, nullptr, nullptr}},
        new Node{3, nullptr, nullptr}};
    // поэкспериментируй: добавь ещё узлы и посмотри, какой получается порядок
    bfs(root);                                 // 1 2 3 4 5
    cout << endl;
    return 0;
}
```

Главное достоинство BFS в том, что узлы посещаются строго в порядке
**возрастания расстояния** от корня. Поэтому BFS очень удобен в задачах
«найти кратчайший путь по числу шагов» (например, в графах).

[mini-task:1]

## DFS или BFS

Когда какой обход выбирать.

| Если нужно...                                     | Бери                  |
|---------------------------------------------------|------------------------|
| Углубиться, перебрать все варианты до конца       | DFS                    |
| Пройти по уровням, найти ближайшее                | BFS                    |
| Простоту записи и компактный код                  | DFS (рекурсия)         |
| Контролировать порядок узлов вручную              | BFS (через очередь)    |

Маленькая практическая мысль. На очень глубоких деревьях рекурсивный DFS
может «съесть» весь стек вызовов и упасть с переполнением стека. Это
встречается в задачах на длинные цепочки. В таких случаях DFS
переписывают через явный `std::stack`. Логика та же, просто стек теперь
управляется руками.

## Итоги

- DFS это рекурсивный спуск в глубину. У него три варианта порядка обработки: pre-order, in-order, post-order.
- BFS это обход по уровням через очередь. Полезен для задач на расстояние и кратчайший путь.
- Для BST in-order выводит значения по возрастанию.
- У любой рекурсивной функции на дереве должна быть база при `nullptr`. Это правило номер один.
- DFS и BFS это фундаментальные обходы. Они встретятся ещё много раз в задачах на графы и сложные структуры.
"""

QUIZ_2 = [
    {
        'question': 'Какой обход BST даёт элементы в порядке возрастания?',
        'a': 'Pre-order', 'b': 'In-order', 'c': 'Post-order', 'd': 'BFS',
        'correct': 'b',
    },
    {
        'question': 'Какая структура данных используется для BFS?',
        'a': 'Стек', 'b': 'Очередь', 'c': 'Хеш-таблица', 'd': 'Массив',
        'correct': 'b',
    },
    {
        'question': 'Как обычно реализуют DFS на дереве?',
        'a': 'Через очередь',
        'b': 'Через рекурсию (или явный стек)',
        'c': 'Через двоичный поиск',
        'd': 'Через сортировку',
        'correct': 'b',
    },
    {
        'question': 'Когда BFS особенно удобен?',
        'a': 'Для подсчёта высоты дерева',
        'b': 'Для поиска кратчайшего пути по числу шагов',
        'c': 'Для удаления листьев',
        'd': 'Никогда',
        'correct': 'b',
    },
    {
        'question': 'Что обязательно должно быть в рекурсивном обходе, чтобы он не зациклился?',
        'a': 'Цикл for',
        'b': 'Базовый случай (выход из рекурсии при nullptr)',
        'c': 'Тип возврата void',
        'd': 'Ничего',
        'correct': 'b',
    },
]

MINI_TASKS_2 = [
    {
        'title': 'Pre-order обход',
        'description': (
            'В стартовом коде уже создано дерево. Реализуйте рекурсивную функцию '
            '`preorder`, которая печатает значения узлов в порядке: '
            '**узел → левое поддерево → правое поддерево**, через пробел.'
        ),
        'starter_code': (
            '#include <iostream>\n'
            'using namespace std;\n\n'
            'struct Node { int value; Node* left; Node* right; };\n\n'
            'void preorder(Node* v) {\n'
            '    // Ваш код\n'
            '\n'
            '}\n\n'
            'int main() {\n'
            '    Node* root = new Node{1,\n'
            '        new Node{2, new Node{4, nullptr, nullptr}, new Node{5, nullptr, nullptr}},\n'
            '        new Node{3, nullptr, new Node{6, nullptr, nullptr}}};\n'
            '    preorder(root);\n'
            '    return 0;\n'
            '}\n'
        ),
        'tests': [{'input': '', 'expected': '1 2 4 5 3 6'}],
        'hint': 'Сначала обработайте сам узел, потом рекурсивно обойдите левое и правое поддерево.',
    },
    {
        'title': 'BFS: печать по уровням',
        'description': (
            'В стартовом коде уже создано дерево. Реализуйте функцию `bfs`, '
            'которая печатает значения узлов в порядке обхода в ширину, '
            'через пробел.'
        ),
        'starter_code': (
            '#include <iostream>\n'
            '#include <queue>\n'
            'using namespace std;\n\n'
            'struct Node { int value; Node* left; Node* right; };\n\n'
            'void bfs(Node* root) {\n'
            '    // Ваш код\n'
            '\n'
            '}\n\n'
            'int main() {\n'
            '    Node* root = new Node{1,\n'
            '        new Node{2, new Node{4, nullptr, nullptr}, new Node{5, nullptr, nullptr}},\n'
            '        new Node{3, nullptr, new Node{6, nullptr, nullptr}}};\n'
            '    bfs(root);\n'
            '    return 0;\n'
            '}\n'
        ),
        'tests': [{'input': '', 'expected': '1 2 3 4 5 6'}],
        'hint': 'Очередь хранит узлы, которых ещё нужно обработать. Из неё всегда забираем голову, печатаем, добавляем непустых детей.',
    },
]

# ============================================================================
MODULE = {
    'title': 'Деревья и обходы',
    'description': (
        'Двоичные деревья и деревья поиска. Обходы дерева разными способами, '
        'измерение его высоты и размера, базовые операции поиска и вставки значений.'
    ),
    'concepts': (
        'struct Node { int value; Node* left; Node* right; }; '
        'двоичное дерево поиска (BST): свойство «слева меньше, справа больше», '
        'вставка через указатели; '
        'обходы DFS через рекурсию: прямой (pre-order: узел → лево → право), '
        'симметричный (in-order: лево → узел → право), обратный (post-order); '
        'обход BFS через std::queue<Node*>; '
        'высота дерева, количество листьев и размер вычисляются рекурсивными функциями '
        'с базой при nullptr'
    ),
    'order': 8,
    'theories': [
        {'title': 'Что такое дерево', 'content': THEORY_1, 'order': 0,
         'mini_tasks': MINI_TASKS_1, 'quiz': QUIZ_1},
        {'title': 'Обходы дерева: DFS и BFS', 'content': THEORY_2, 'order': 1,
         'mini_tasks': MINI_TASKS_2, 'quiz': QUIZ_2},
    ],
    'tasks': [
        # Уровень 1 — рекурсия как разминка
        {
            'title': 'Факториал через рекурсию',
            'description': (
                'Прочитайте целое n (0 ≤ n ≤ 12). Реализуйте рекурсивную функцию '
                '`factorial(n)` и выведите её результат.\n\n'
                'Это разминка перед обходами деревьев. Рекурсия будет нашим основным '
                'инструментом.\n\n'
                '## Пример\n```\n5\n```\n```\n120\n```'
            ),
            'difficulty': 1,
            'tests': [
                {'input': '0', 'expected': '1'},
                {'input': '1', 'expected': '1'},
                {'input': '5', 'expected': '120'},
                {'input': '10', 'expected': '3628800'},
            ],
            'hint': 'База: factorial(0) == 1. Шаг: factorial(n) = n * factorial(n-1).',
            'tags': ['recursion', 'functions'],
            'order': 0,
            'variations': [
                {
                    'title': 'Числа Фибоначчи (рекурсия)',
                    'description': (
                        'Прочитайте целое n (0 ≤ n ≤ 30). Реализуйте рекурсивную функцию '
                        '`fib(n)` и выведите n-е число Фибоначчи: F(0) = 0, F(1) = 1, '
                        'F(n) = F(n-1) + F(n-2).\n\n'
                        '## Пример\n```\n10\n```\n```\n55\n```'
                    ),
                    'tests': [
                        {'input': '0', 'expected': '0'},
                        {'input': '1', 'expected': '1'},
                        {'input': '10', 'expected': '55'},
                        {'input': '20', 'expected': '6765'},
                    ],
                    'hint': 'База: fib(0) = 0, fib(1) = 1. Шаг: fib(n) = fib(n-1) + fib(n-2).',
                    'tags': ['recursion', 'functions'],
                },
            ],
        },

        # Уровень 2 — BST и обход
        {
            'title': 'BST: вставка и симметричный обход',
            'description': (
                'Прочитайте `n` и `n` различных целых чисел. Вставляйте их по очереди '
                'в двоичное дерево поиска. Затем выведите элементы в **симметричном** '
                'обходе (узел между поддеревьями: left → узел → right) через пробел.\n\n'
                '## Пример\n```\n5\n10 5 20 3 7\n```\n```\n3 5 7 10 20\n```'
            ),
            'difficulty': 2,
            'tests': [
                {'input': '5\n10 5 20 3 7', 'expected': '3 5 7 10 20'},
                {'input': '1\n42', 'expected': '42'},
                {'input': '4\n4 3 2 1', 'expected': '1 2 3 4'},
                {'input': '4\n1 2 3 4', 'expected': '1 2 3 4'},
            ],
            'hint': 'Вставка: если значение меньше, идём в левое поддерево, иначе в правое. Вставляем там, где встретили nullptr.',
            'tags': ['tree', 'recursion'],
            'order': 1,
            'variations': [
                {
                    'title': 'BST: прямой обход',
                    'description': (
                        'Прочитайте `n` и `n` различных целых чисел. Вставляйте их по очереди '
                        'в двоичное дерево поиска. Затем выведите элементы в **прямом** '
                        'обходе (узел → left → right) через пробел.\n\n'
                        '## Пример\n```\n5\n10 5 20 3 7\n```\n```\n10 5 3 7 20\n```'
                    ),
                    'tests': [
                        {'input': '5\n10 5 20 3 7', 'expected': '10 5 3 7 20'},
                        {'input': '1\n42', 'expected': '42'},
                        {'input': '3\n5 3 7', 'expected': '5 3 7'},
                    ],
                    'hint': 'То же дерево, что и в основной задаче. Меняется только порядок: сначала выводим значение узла, потом обход левого поддерева, потом правого.',
                    'tags': ['tree', 'recursion'],
                },
            ],
        },

        # Уровень 3 — DFS на дереве
        {
            'title': 'Высота дерева',
            'description': (
                'Дерево задано как `n` и затем `n - 1` пар (родитель, ребёнок). '
                'Корнем считается узел с номером 1. Найдите высоту дерева (число рёбер на самом '
                'длинном пути от корня до листа). Высота из одного узла = 0.\n\n'
                '## Пример\n```\n5\n1 2\n1 3\n2 4\n2 5\n```\n```\n2\n```'
            ),
            'difficulty': 3,
            'tests': [
                {'input': '5\n1 2\n1 3\n2 4\n2 5', 'expected': '2'},
                {'input': '1\n', 'expected': '0'},
                {'input': '3\n1 2\n2 3', 'expected': '2'},
                {'input': '4\n1 2\n1 3\n1 4', 'expected': '1'},
            ],
            'hint': 'Постройте список потомков для каждого узла, потом рекурсивно считайте высоту: 1 + максимум среди детей.',
            'tags': ['tree', 'dfs', 'recursion'],
            'order': 2,
            'variations': [
                {
                    'title': 'Количество листьев дерева',
                    'description': (
                        'Дерево задано как `n` и `n - 1` пар (родитель, ребёнок). '
                        'Корнем считается узел с номером 1. Найдите количество листьев, '
                        'то есть узлов, у которых нет потомков. Дерево из одного узла даёт ответ 1.\n\n'
                        '## Пример\n```\n5\n1 2\n1 3\n2 4\n2 5\n```\n```\n3\n```'
                    ),
                    'tests': [
                        {'input': '5\n1 2\n1 3\n2 4\n2 5', 'expected': '3'},
                        {'input': '1\n', 'expected': '1'},
                        {'input': '3\n1 2\n2 3', 'expected': '1'},
                        {'input': '4\n1 2\n1 3\n1 4', 'expected': '3'},
                    ],
                    'hint': 'Постройте список потомков. Дальше рекурсивно: если у узла нет детей, это лист (+1), иначе берём сумму по поддеревьям.',
                    'tags': ['tree', 'dfs', 'recursion'],
                },
            ],
        },
    ],
}
