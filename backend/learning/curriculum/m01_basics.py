# -*- coding: utf-8 -*-
"""Модуль 1: Основы C++.

Подробный вводный модуль для людей, которые программируют впервые.
Каждая теория содержит:
- развёрнутые объяснения с метафорами;
- запускаемые примеры (```cpp-run```);
- мини-задания после ключевых подразделов ([mini-task:N]);
- тест на знание материала.
"""

# ============================================================================
# ТЕОРИЯ 1 — Первая программа: что такое C++ и из чего состоит код
# ============================================================================

THEORY_1 = """\
# Первые шаги в C++: устройство простой программы

> **Памятка: как читать материал курса**
>
> **Жирным шрифтом** выделены ключевые слова и термины, на которые
> стоит обратить внимание и которые стоит запомнить.
>
> Моноширинным шрифтом и акцентным цветом выделены элементы языка C++
> (например, `int`, `cout`, `main()`): ключевые слова, имена функций
> и кусочки кода.

## Что такое язык программирования и зачем он нужен?

Компьютер — это машина, которая умеет очень быстро выполнять простые действия:
складывать числа, сравнивать их, читать символы, выводить буквы на экран.
Но сам компьютер не «понимает», что вы хотите сделать. Чтобы он построил
вам приложение, веб-сайт или игру, нужно записать инструкции на специальном
языке. Эти инструкции называются **программой**, а язык — **языком
программирования**.

C++ — один из самых популярных языков программирования. На C++ написаны 
операционные системы, игровые движки, банковские системы и большинство программ, 
требующих максимальной производительности.
Выбор C++ в качестве основного языка курса обусловлен тем, что он требует от 
обучающегося осознанного отношения к управлению памятью и типам данных, что 
способствует лучшему пониманию принципов работы алгоритмов.

## Как код на C++ становится работающей программой?

Код на C++ — это обычный текст, написанный по определённым правилам. 
Чтобы этот текст превратился в работающую программу, его нужно **скомпилировать** 
— пропустить через специальную программу-компилятор (для C++ часто используют g++).
Компилятор переводит текст в **машинный код** — набор команд, понятный процессору.

Если вы написали код с ошибкой (пропустили точку с запятой, забыли
закрыть скобку), компилятор откажется собирать программу и подскажет,
в какой строке ошибка. Сообщения компилятора стоит читать внимательно,
ведь они подсказывают, где именно нужно что-то исправить.

## Твоя первая программа

Традиционно первая программа на любом языке выводит на экран фразу
«Hello, World!». Вот как она выглядит на C++:

```cpp-run
// hint: вывод строки на экран
#include <iostream>
using namespace std;

int main() {
    cout << "Hello, World!" << endl;
    return 0;
}
```

Нажмите кнопку «▶ Запустить» прямо в этом блоке и увидите, что напечатает
программа. Дальше мы разберём, что значит каждая строчка.

### Разбор строчка за строчкой

```cpp
#include <iostream>
```

Эта строка подключает к программе **библиотеку** `<iostream>`. Библиотека —
это набор готового кода, который кто-то уже написал за вас. В `<iostream>`
лежит всё, что нужно для ввода с клавиатуры и вывода на экран
(io = input/output = ввод/вывод, stream = поток). Без неё мы не сможем напечатать ни буквы.

```cpp
using namespace std;
```

Многие имена (например, `cout`) живут в **пространстве имён** (англ. *namespace*)
с названием `std` — сокращение от «standard». Если бы мы не написали эту строку,
нам пришлось бы каждый раз писать `std::cout` вместо 
`cout`. Эта строка как бы говорит: «всё, что лежит в `std`, используем без
приставки».

Зачем вообще нужны пространства имён? Представьте две библиотеки, в каждой
есть функция `print`. Без пространств имён компилятор не понял бы, чью
именно функцию вы вызываете. Пространства имён решают эту путаницу.

```cpp
int main() {
    ...
}
```

Это **функция** с именем `main`. Любая программа на C++ начинает выполнение
именно с неё. Слово `int` означает, что функция возвращает
целое число (мы поговорим о возврате чуть позже). Фигурные скобки `{ }`
обозначают границы тела функции, внутри которого расположен код, и именно
он будет выполняться.

```cpp
cout << "Hello, World!" << endl;
```

`cout` —  это объект стандартного потока вывода.
Программа отправляет в него данные с помощью оператора `<<`.
В нашем примере мы сначала отправляем строку `"Hello, World!"`,
а затем `endl` — манипулятор, который переводит курсор на следующую строку.

Перевести курсор на новую строку можно и без `endl`. Для этого внутри
самой строки используют специальный символ `\\n`. Он называется
**escape-последовательностью** и понимается компилятором как «здесь нужен
перевод строки»:

```cpp
cout << "Привет!\\nКак дела?\\n";
```

Этот код выведет на экран:

```
Привет!
Как дела?
```

Преимущество `\\n` в том, что его можно ставить прямо внутри строки,
в любом месте. С `endl` так не получится, потому что его всегда отправляют
отдельно через `<<`.

Главное техническое различие: `endl` после перевода строки сбрасывает
**буфер вывода** (заставляет операционную систему немедленно показать
накопленный текст), а `\\n` просто переводит строку, не трогая буфер.
В учебных программах разница незаметна, но в больших проектах `\\n`
работает чуть быстрее.

```cpp
return 0;
```

Эта инструкция завершает функцию `main` и возвращает операционной системе
**код завершения** программы. По договорённости `0` означает «всё прошло
без ошибок», а другие числа используют, чтобы сообщить о разных видах
ошибок (например, не удалось открыть файл). Само по себе число `0` ничем
не примечательно, и стоит за ним лишь общепринятое соглашение, которому
следуют почти все программы.

### Точка с запятой в конце строки

Возможно, вы заметили, что почти каждая строка кода заканчивается
точкой с запятой `;`. В C++ это обязательно. Точка с запятой играет
ту же роль, что и точка в обычном тексте, и отделяет одну инструкцию от
другой, подобно тому как точка отделяет одно предложение от другого.

Если `;` забыть, компилятор не поймёт, где заканчивается одна инструкция
и начинается другая, и откажется собирать программу. Пропущенная точка
с запятой — самая частая ошибка начинающих программистов на C++.

[mini-task:0]

## Что такое комментарии

В коде на C++ можно оставлять **комментарии** — заметки для человека,
читающего код. Компилятор их полностью игнорирует, поэтому на работу
программы они никак не влияют.

В C++ есть два вида комментариев:

- **Однострочные** начинаются с двух косых черт `//` и действуют до конца
  текущей строки. Всё, что написано после `//`, компилятор игнорирует.
- **Многострочные** заключают в специальные ограничители `/*` и `*/`.
  Всё, что находится между ними, считается комментарием, даже если занимает
  несколько строк.

```cpp-run
// hint: однострочные и многострочные комментарии
#include <iostream>
using namespace std;

int main() {
    // Это однострочный комментарий, он не влияет на работу программы
    cout << "Привет!" << endl;

    /* А это многострочный комментарий.
       Он может занимать сколько угодно строк
       и обычно используется для длинных пояснений
       или временного отключения куска кода. */
    return 0;
}
```

[mini-task:1]

## Итоги

Мы написали самую простую программу на C++ и разобрали её по частям.
Главное, что нужно запомнить:

1. **`#include <...>`** подключает библиотеки.
2. **`using namespace std;`** избавляет от длинных приставок `std::`.
3. **`int main() { ... return 0; }`** — точка входа в программу.
4. **`cout << ...`** выводит текст на экран.
5. **`;`** ставится после каждой инструкции.
6. **`{``}`** обозначают начало и конец блока кода.
"""

QUIZ_1 = [
    {
        'question': 'С какой функции всегда начинается выполнение программы на C++?',
        'a': 'start()', 'b': 'init()', 'c': 'main()', 'd': 'run()',
        'correct': 'c',
        'explanation': 'Стандарт C++ требует, чтобы выполнение начиналось с функции main.',
    },
    {
        'question': 'Зачем нужна строка #include <iostream>?',
        'a': 'Чтобы программа работала быстрее',
        'b': 'Чтобы подключить функции для ввода и вывода (cin, cout)',
        'c': 'Чтобы создать новую переменную',
        'd': 'Это просто комментарий',
        'correct': 'b',
        'explanation': 'iostream содержит cin/cout; без него нельзя ничего напечатать на экран.',
    },
    {
        'question': 'Что делает символ ; в конце строки?',
        'a': 'Завершает программу',
        'b': 'Это знак комментария',
        'c': 'Завершает инструкцию',
        'd': 'Создаёт пустую строку',
        'correct': 'c',
        'explanation': 'Точка с запятой обязательно ставится после каждой инструкции в C++.',
    },
    {
        'question': 'Зачем нужно using namespace std;?',
        'a': 'Чтобы можно было использовать имена (cout, cin, endl и др.) без приставки std::',
        'b': 'Чтобы компилятор работал быстрее',
        'c': 'Это обязательная часть любой программы',
        'd': 'Чтобы программа печатала текст',
        'correct': 'a',
        'explanation': 'Большинство имён стандартной библиотеки (cout, cin, endl, string и многие другие) находятся в пространстве имён std. Без using namespace std пришлось бы каждый раз писать std::cout, std::cin и так далее.',
    },
    {
        'question': 'Что выведет программа: cout << "abc" << endl; ?',
        'a': '"abc"',
        'b': 'abc',
        'c': 'abc << endl',
        'd': 'Ошибку компиляции',
        'correct': 'b',
        'explanation': 'Кавычки — это синтаксис C++, они в вывод не попадают; endl лишь переводит строку.',
    },
    {
        'question': 'Что компилятор сделает с комментарием //это тест?',
        'a': 'Выведет его на экран',
        'b': 'Полностью проигнорирует',
        'c': 'Превратит в команду',
        'd': 'Выдаст ошибку',
        'correct': 'b',
        'explanation': 'Комментарии нужны только человеку; компилятор полностью их игнорирует и не учитывает в работе программы.',
    },
]

MINI_TASKS_1 = [
    {
        'title': 'Первая программа',
        'description': (
            'Допишите код так, чтобы он выводил на экран строку `Привет, CodeMentor!` '
            '(точно так, без кавычек, с восклицательным знаком).'
        ),
        'starter_code': (
            '#include <iostream>\n'
            'using namespace std;\n\n'
            'int main() {\n'
            '    // Ваш код\n'
            '\n'
            '    return 0;\n'
            '}\n'
        ),
        'tests': [{'input': '', 'expected': 'Привет, CodeMentor!'}],
        'hint': 'Структура программы здесь та же, что и в самом первом примере «Hello, World!». Просто замените текст внутри кавычек и обязательно поставьте `;` в конце инструкции.',
    },
    {
        'title': 'Две строки',
        'description': (
            'Напишите программу, которая выведет две строки одну под другой:\n\n'
            '```\nПривет!\nДобро пожаловать.\n```'
        ),
        'starter_code': (
            '#include <iostream>\n'
            'using namespace std;\n\n'
            'int main() {\n'
            '    // Ваш код\n'
            '\n'
            '    return 0;\n'
            '}\n'
        ),
        'tests': [{'input': '', 'expected': 'Привет!\nДобро пожаловать.'}],
        'hint': 'В теории мы видели два способа перейти на новую строку: `endl` и символ `\\n`. Используйте любой из них между двумя выводами `cout` и не забудьте про `;`.',
    },
]

# ============================================================================
# ТЕОРИЯ 2 — Переменные и типы данных
# ============================================================================

THEORY_2 = """\
# Переменные и типы данных

## Что такое переменная?

Когда программа выполняется, ей нужно где-то хранить данные: имя пользователя,
счёт игры, температуру воздуха. Для этого мы используем **переменные**.

Представьте переменную как коробку с именем. В коробке лежит какое-то
значение, а имя нужно нам, чтобы знать, в какую именно коробку положить
или из какой достать.

```cpp-run
// hint: создаём переменную и меняем её значение
#include <iostream>
using namespace std;

int main() {
    int score = 100;             // создали коробку score, положили 100
    cout << score << endl;       // достали значение и напечатали
    score = score + 50;          // изменили значение на 150
    cout << score << endl;
    return 0;
}
```

Запустите пример и убедитесь, что программа напечатает сначала 100, а затем 150.
Обратите внимание на знак `=`. Здесь это не математическое равенство,
а **присваивание**: то, что справа от него (значение или результат выражения),
сохраняется в переменную слева. Поэтому, например, запись `score = score + 50`
сначала вычисляет правую часть (старое значение + 50), а потом кладёт результат
обратно в `score`.

## Типы данных

Перед именем переменной всегда стоит **тип**. Он говорит компилятору, какие
именно данные мы будем хранить. Это помогает выделить нужное количество
памяти и поймать ошибки заранее.

| Тип         | Что хранит                                  | Пример объявления                  |
|-------------|---------------------------------------------|------------------------------------|
| `int`       | целое число (≈ ±2·10⁹)                       | `int age = 18;`                    |
| `long long` | очень большое целое (±9·10¹⁸)                | `long long big = 100000000000LL;`  |
| `double`    | дробное число (около 15 значащих цифр)        | `double pi = 3.14;`                |
| `float`     | дробное число, точность ниже (≈ 7 цифр)       | `float ratio = 0.5f;`              |
| `char`      | один символ                                   | `char letter = 'A';`               |
| `bool`      | true или false (правда/ложь)                  | `bool ok = true;`                  |
| `string`    | строка (нужен `#include <string>`)            | `string name = "Анна";`            |

Заметьте: одиночный символ записывают в одинарных кавычках (`'A'`). Строки же заключают в двойные (`"Анна"`).

Обратите внимание на необычные хвостики в примерах: `0.5f` и `100000000000LL`.
Это **суффиксы числовых литералов**. По умолчанию любая дробная константа в коде
(например, `0.5` или `3.14`) воспринимается компилятором как `double`, а любая
целая (например, `42`) как `int`. Суффикс явно указывает другой тип:

- `f` после дробного числа означает `float` (например, `0.5f`);
- `LL` после целого числа означает `long long` (например, `100000000000LL`).

Если суффикс не поставить, компилятор сначала создаст значение «по умолчанию»,
а потом приведёт его к нужному типу. Обычно это работает, но иногда вызывает
предупреждение о возможной потере точности.

В учебных задачах для дробных чисел используем `double`: он хранит больше цифр
после запятой и реже даёт ошибки округления. Тип `float` нужен в основном там,
где важно экономить память (он занимает вдвое меньше места). Это бывает полезно,
например, в массивах из миллионов чисел или в графике и видеокартах.

По умолчанию значения типа `bool` выводятся через `cout` как числа: `true`
превращается в `1`, а `false` в `0`. Чтобы получить вывод словами, используйте
манипулятор `boolalpha` из стандартной библиотеки. После него все значения
`bool` будут показываться буквами (`true`/`false`). Отменить такое поведение
можно манипулятором `noboolalpha`.

```cpp-run
// hint: вывод bool как числа и как слова
#include <iostream>
using namespace std;

int main() {
    bool ok = true;
    cout << ok << endl;                  // 1

    cout << boolalpha << ok << endl;     // true

    cout << noboolalpha << ok << endl;   // 1, снова числовой режим
    return 0;
}
```

Сам тип `bool` особенно пригодится в условиях `if` и циклах `while`/`for`.
Об этом будет рассказано позже.

> **Интересно:** как числа хранятся внутри компьютера.
>
> Внутри компьютера каждая ячейка памяти умеет находиться только в двух
> состояниях: «есть сигнал» и «нет сигнала». Эти состояния обозначают
> цифрами `1` и `0`, а одна такая ячейка называется **битом**.
>
> Любое число компьютер превращает в последовательность таких битов
> (**двоичный код**). Например, число `5` в двоичной системе записывается
> как `101`, а число `100` как `1100100`.
>
> Под обычный `int` отводится 32 бита, что даёт около 4 миллиардов
> возможных комбинаций. Половина из них уходит на отрицательные числа,
> поэтому «потолок» получается примерно ±2 миллиарда.
>
> Знать это во всех деталях сейчас необязательно. Подробно темой битов и
> двоичной системы мы займёмся, когда она реально понадобится: например,
> в задачах на битовые операции. Сейчас же достаточно помнить, что в коде
> мы пишем привычные десятичные числа, а компьютер сам разбирается, как
> их хранить.

```cpp-run
// hint: разные типы переменных
#include <iostream>
#include <string>
using namespace std;

int main() {
    int age = 25;
    double height = 1.75;
    char grade = 'A';
    bool is_student = true;
    string name = "Алёна";

    cout << name << ", возраст " << age << ", рост " << height << endl;
    cout << "Оценка: " << grade << endl;
    cout << "Студент: " << is_student << endl;   // 1 = true, 0 = false
    return 0;
}
```

[mini-task:0]

## Чтение данных с клавиатуры

Пока что наши программы знают только то, что мы сами вписали в код. Чтобы
программа могла работать с любыми числами, ей нужно уметь читать их извне.
Для этого в C++ есть `cin` (от англ. *console input*,
то есть «ввод из консоли»). Рядом с ним работает оператор `>>`: он берёт
очередное значение из ввода и кладёт его в переменную.

```cpp-run
// hint: чтение числа с клавиатуры через cin
// stdin: 7
#include <iostream>
using namespace std;

int main() {
    int x;                  // объявили переменную (без значения)
    cin >> x;               // прочитали число с клавиатуры
    cout << "Удвоенное: " << x * 2 << endl;
    return 0;
}
```

Под редактором появилось поле «Ввод данных». В нём уже стоит число `7`,
которое программа прочитает через `cin`. Запустите пример и убедитесь, что
он выведет `Удвоенное: 14`. Попробуйте поменять число в поле ввода и снова
нажать «▶ Запустить»: программа отреагирует на новое значение.

## Арифметические операции

| Операция | Что делает         | Пример       | Результат |
|----------|---------------------|--------------|-----------|
| `+`      | сложение            | `5 + 3`      | 8         |
| `-`      | вычитание           | `10 - 4`     | 6         |
| `*`      | умножение           | `6 * 7`      | 42        |
| `/`      | деление             | `15 / 4`     | 3 (целое!) |
| `%`      | остаток от деления  | `15 % 4`     | 3         |

Внимание: деление целых чисел в C++ отбрасывает дробную часть.
Если хотите получить дробь, хотя бы одно из чисел должно быть `double`:

```cpp-run
// hint: целочисленное деление и остаток
#include <iostream>
using namespace std;

int main() {
    cout << 7 / 2 << endl;        // 3
    cout << 7 / 2.0 << endl;      // 3.5
    cout << 7 % 2 << endl;        // 1 (остаток)
    return 0;
}
```

Оператор `%` (остаток) часто встречается в алгоритмических задачах. Например,
`n % 2 == 0` означает «n — чётное число».

[mini-task:1]

## Сокращённые операции

Часто нужно изменить переменную относительно её текущего значения. Для этого
в C++ есть короткие записи.

### Составные присваивания

Самый частый случай — прибавить, отнять, умножить или поделить переменную
на какое-то значение. Для этого подходит **составное присваивание**

| Длинная запись   | Короткая запись |
|------------------|-----------------|
| `x = x + 5;`     | `x += 5;`       |
| `x = x - 3;`     | `x -= 3;`       |
| `x = x * 2;`     | `x *= 2;`       |
| `x = x / 2;`     | `x /= 2;`       |
| `x = x % 10;`    | `x %= 10;`      |

Обе формы делают абсолютно одно и то же, короткая просто компактнее.

### Инкремент и декремент

Если нужно изменить переменную ровно на единицу, в C++ есть ещё более
короткие операторы:

- **Инкремент** (`++`) увеличивает переменную на 1.
- **Декремент** (`--`) уменьшает переменную на 1.

```cpp-run
// hint: четыре способа прибавить единицу
#include <iostream>
using namespace std;

int main() {
    int x = 10;
    x = x + 1;   // 11, длинная форма
    x += 1;      // 12, составное присваивание
    ++x;         // 13, префиксный инкремент
    x++;         // 14, постфиксный инкремент
    cout << x << endl;
    return 0;
}
```

### Префиксная и постфиксная формы

Инкремент и декремент бывают двух видов:

- **Префиксная** форма — оператор стоит перед переменной: `++x`, `--x`.
- **Постфиксная** форма — оператор стоит после переменной: `x++`, `x--`.

Когда вы пишете их как отдельную инструкцию (например, `++x;` или `x++;`), разницы нет: переменная просто увеличивается на 1.

Разница появляется, когда `++` или `--` используется внутри выражения
вместе с другими действиями. Правило такое:

- `++x` сначала увеличивает `x`, и в выражение подставляется новое
  значение.
- `x++` подставляет в выражение старое значение `x`, и только потом
  увеличивает переменную.

```cpp-run
// hint: разница префикса и постфикса
#include <iostream>
using namespace std;

int main() {
    int a = 5;
    int b = ++a;    // a стало 6, и в b записалось 6
    cout << "a=" << a << " b=" << b << endl;   // a=6 b=6

    int c = 5;
    int d = c++;    // в d записалось старое значение 5, потом c стало 6
    cout << "c=" << c << " d=" << d << endl;   // c=6 d=5
    return 0;
}
```

### Где это используется

Сокращённые формы пригодятся почти везде, где переменную нужно постепенно
менять: подсчёт количества чего-либо, накопление суммы, ведение счёта.
Самый простой пример — счётчик.

```cpp-run
// hint: счётчик «пройденных шагов»
#include <iostream>
using namespace std;

int main() {
    int steps = 0;
    ++steps;        // прошли первый шаг, steps = 1
    ++steps;        // прошли второй шаг,  steps = 2
    steps += 5;     // прошли сразу пять,  steps = 7
    cout << "Всего шагов: " << steps << endl;
    return 0;
}
```

Чуть позже, в теории про **циклы**, инкремент будет встречаться буквально
в каждом примере: с его помощью счётчик цикла увеличивается после каждой
итерации. Так что эти короткие записи стоит запомнить уже сейчас.

> **Совет новичку**
>
> Пока вы только начинаете, лучше не смешивать `++` или `--` с другими
> действиями в одной строке (`b = ++a;`, `cout << x++;`). Сначала измените
> переменную отдельной инструкцией, а потом используйте её значение. Это
> избавит от трудноуловимых ошибок.

## Итоги

- **Переменная** объявляется как `тип имя = значение;`.
- Основные типы: `int`, `double`, `char`, `bool`, `string`.
- `cin >> x;` читает значение  переменной `x`с клавиатуры.
- Деление двух целых отбрасывает дробь (`7 / 2 = 3`); `%` даёт остаток
  от деления (`7 % 2 = 1`).
- Короткие записи арифметических операций: `x += 5`, `x *= 2`, `++x`, `--x`.
"""

QUIZ_2 = [
    {
        'question': 'Какой тип лучше всего подойдёт для роста человека в метрах (например, 1.78)?',
        'a': 'int', 'b': 'char', 'c': 'double', 'd': 'bool',
        'correct': 'c',
        'explanation': 'Рост 1.78 является дробным числом, поэтому нужен тип, который хранит дроби: double (или float). int хранит только целые числа, char предназначен для одного символа, а bool принимает только true и false.',
    },
    {
        'question': 'Чему будет равно значение x после выполнения: int x = 10 / 3;?',
        'a': '3.33', 'b': '3', 'c': '4', 'd': 'Ошибка',
        'correct': 'b',
        'explanation': 'При делении двух int результат тоже int: дробная часть отбрасывается. Математически 10 / 3 = 3.33…, но в переменную int сохраняется только целая часть, то есть 3.',
    },
    {
        'question': 'Что выведет cout << (15 % 4);?',
        'a': '3', 'b': '4', 'c': '15', 'd': '3.75',
        'correct': 'a',
        'explanation': 'Оператор `%` возвращает остаток от целочисленного деления. 15 делится на 4 с остатком: 15 = 3·4 + 3, поэтому 15 % 4 = 3.',
    },
    {
        'question': 'Как можно короче записать `x = x + 1;`?',
        'a': '`x++;` или `x += 1;`',
        'b': '`x = ++;`',
        'c': '`x ++ 1;`',
        'd': '`x := x + 1;`',
        'correct': 'a',
        'explanation': 'Прибавить единицу можно тремя короткими способами: инкрементом `x++` (или `++x`) и составным присваиванием `x += 1`. Все три записи делают то же самое, что `x = x + 1`.',
    },
    {
        'question': 'Какие кавычки нужно использовать для одиночного символа: `char c = ?`',
        'a': 'двойные `"A"`',
        'b': 'одинарные `\'A\'`',
        'c': 'без кавычек',
        'd': 'обратные `` `A` ``',
        'correct': 'b',
        'explanation': 'Тип `char` хранит ровно один символ, такие литералы записывают в одинарных кавычках: `\'A\'`. В двойных кавычках `"A"` уже получается строка (`string`), а не одиночный символ.',
    },
    {
        'question': 'В чём разница между типами `int` и `long long`?',
        'a': '`long long` хранит дробные, а `int` нет',
        'b': '`long long` вмещает гораздо большие целые числа, чем `int`',
        'c': '`long long` работает медленнее в любых операциях',
        'd': 'Разницы нет',
        'correct': 'b',
        'explanation': 'Оба типа хранят только целые числа, дроби живут в double и float. Разница лишь в размере: int вмещает примерно ±2·10⁹, а long long вмещает около ±9·10¹⁸. Long long нужен, когда числа выходят за пределы int (например, факториал большого числа).',
    },
]

MINI_TASKS_2 = [
    {
        'title': 'Прибавьте 10',
        'description': (
            'Прочитайте целое число с клавиатуры, прибавьте к нему 10 и выведите результат.\n\n'
            '## Пример\nВвод: `5` → Вывод: `15`'
        ),
        'starter_code': (
            '#include <iostream>\n'
            'using namespace std;\n\n'
            'int main() {\n'
            '    int x;\n'
            '    cin >> x;\n'
            '    // Ваш код\n'
            '\n'
            '    return 0;\n'
            '}\n'
        ),
        'tests': [
            {'input': '5', 'expected': '15'},
            {'input': '0', 'expected': '10'},
            {'input': '-3', 'expected': '7'},
        ],
        'hint': 'В теории мы видели, как использовать арифметические операции с переменными, например `score + 50`. Здесь работает тот же приём: прибавьте 10 к прочитанному числу и выведите результат через `cout`.',
    },
    {
        'title': 'Чётное или нечётное',
        'description': (
            'Прочитайте неотрицательное целое число `n` (то есть `n ≥ 0`). '
            'Выведите `0`, если оно чётное, и `1`, если нечётное.'
        ),
        'starter_code': (
            '#include <iostream>\n'
            'using namespace std;\n\n'
            'int main() {\n'
            '    int n;\n'
            '    cin >> n;\n'
            '    // Ваш код\n'
            '\n'
            '    return 0;\n'
            '}\n'
        ),
        'tests': [
            {'input': '4', 'expected': '0'},
            {'input': '7', 'expected': '1'},
            {'input': '0', 'expected': '0'},
            {'input': '13', 'expected': '1'},
            {'input': '100', 'expected': '0'},
        ],
        'hint': 'Вспомните оператор `%` из таблицы арифметики: он возвращает остаток от деления. Для неотрицательного `n` результат `n % 2` уже равен нужному ответу (0 для чётных, 1 для нечётных). Просто выведите `n % 2` через `cout`.',
    },
]

# ============================================================================
# ТЕОРИЯ 3 — Условия и циклы
# ============================================================================

THEORY_3 = """\
# Условия и циклы: как программа принимает решения и повторяет действия

## Зачем нужны условия?

Пока что наши программы делали ровно одно и то же при любом вводе. Но обычно
от данных зависит, что должна делать программа: если число чётное, нужно
сделать одно, иначе другое. Для этого есть конструкция `if` (англ. *если*).

Представь банкомат. Алгоритм у него один и тот же, но ответ зависит от
ситуации: на счёте хватает денег, тогда он выдаёт наличные, иначе
показывает сообщение об ошибке. Такая развилка в программе и описывается
оператором `if`.

Идея простая. Программа задаёт вопрос вида «верно ли, что x > 0?». Если
ответ «да» (значение `true`), выполняется один блок кода. Если ответ «нет»
(значение `false`), можно указать запасной блок через `else` или добавить
дополнительные проверки с помощью `else if`. Так из обычной
последовательности команд получается дерево решений, по которому программа
каждый раз спускается своим путём.

```cpp-run
// hint: программа реагирует на знак числа
#include <iostream>
using namespace std;

int main() {
    // попробуй заменить 7 на -3, 0 или 100 и нажать «Запустить»
    int x = 7;
    if (x > 0) {
        cout << "Положительное" << endl;
    } else if (x < 0) {
        cout << "Отрицательное" << endl;
    } else {
        cout << "Ноль" << endl;
    }
    return 0;
}
```

Разберём этот пример по шагам. В скобках после `if` стоит **условие**:
выражение, которое можно проверить на истинность. Программа подставляет
текущее значение `x` (сейчас 7) в `x > 0` и получает `true`. Раз условие
истинно, выполняется блок в фигурных скобках сразу за `if`, и в консоль
выводится «Положительное». После этого программа пропускает все остальные
ветки `else if` и `else` и переходит к коду под закрывающей фигурной
скобкой.

Если бы `x` был, например, `-5`, проверка `x > 0` дала бы `false`,
программа спустилась бы к `else if (x < 0)`, увидела бы `true` и напечатала
«Отрицательное». При `x = 0` обе верхние проверки дают `false`, и
срабатывает запасной блок `else`. Главное помнить: из всей цепочки
`if`/`else if`/`else` выполняется ровно один блок, ни больше, ни
меньше.

## Операторы сравнения и логики

Внутри условия программа что-то сравнивает: число с числом, переменную с
переменной, переменную с числом. Результат любого сравнения это специальное
значение типа `bool`: либо `true` (истина), либо `false` (ложь). Именно это
значение и проверяет `if`, чтобы решить, выполнять блок кода или пропустить.

| Оператор | Смысл                | Пример      |
|----------|----------------------|-------------|
| `==`     | равно                | `x == 5`    |
| `!=`     | не равно             | `x != 0`    |
| `<`      | меньше               | `x < 10`    |
| `>`      | больше               | `x > 0`     |
| `<=`     | меньше или равно     | `x <= 100`  |
| `>=`     | больше или равно     | `x >= 18`   |

Самая частая ошибка новичка: написать `=` вместо `==`. Один знак `=`
означает **присваивание** (положить значение в переменную), а два знака `==`
означают **сравнение** (проверить, равны ли значения). Запись `if (x = 5)`
ничего не сравнивает: она молча кладёт в `x` число 5, и условие после этого
всегда считается истинным. Привыкай читать про себя: `==` это «равно ли»,
а `=` это «положи».

Иногда одной проверки не хватает. Например, «возраст не меньше 18 И меньше
65» это сразу два условия. Чтобы соединить их в одно, есть логические
операторы:

| Оператор | Смысл  | Пример                 |
|----------|--------|------------------------|
| `&&`     | И      | `x > 0 && x < 100`     |
| `\\|\\|` | ИЛИ    | `x == 1 \\|\\| x == 2` |
| `!`      | НЕ     | `!is_ready`            |

На словах они работают так:

- `A && B` истинно только тогда, когда **оба** условия `A` и `B` истинны. Если хотя бы одно ложно, весь результат равен `false`.
- `A || B` истинно, когда истинно **хотя бы одно** из условий. Ложь получается, только если оба условия ложны.
- `!A` переворачивает значение наоборот: из `true` делает `false`, из `false` делает `true`.

Маленькая, но полезная деталь: C++ проверяет условия слева направо и
останавливается, как только ответ уже понятен. В `A && B`, если `A` оказалось
ложным, второе условие даже не вычисляется (общий результат всё равно будет
`false`). В `A || B`, если `A` истинно, проверку тоже можно не продолжать.

```cpp-run
// hint: объединение двух условий через И (&&)
#include <iostream>
using namespace std;

int main() {
    int age = 20;
    // поэкспериментируй: поменяй age (например, на 15, 17, 30, 70),
    // замени && на || и сравни результат,
    // или поставь ! перед скобкой:  if (!(age >= 18 && age < 65))
    if (age >= 18 && age < 65) {
        cout << "Взрослый трудоспособный" << endl;
    }
    return 0;
}
```

Совет: перед тем как нажать «Запустить», постарайся сам предсказать, что
выведет программа. А потом сравни с реальным результатом. Так логические
операторы запоминаются заметно быстрее, чем при простом чтении правил.

[mini-task:0]

## Зачем нужны циклы?

Представь, что нужно поздороваться с каждым из тридцати учеников в классе
по имени. Можно написать тридцать одинаковых строк `cout << "Привет, ..."`,
но это и долго, и легко ошибиться. А если учеников триста? А три тысячи?

Цикл это блок кода, который программа выполняет несколько раз подряд. Один
раз пишешь действие, а компьютер сам повторяет его столько, сколько нужно:
десять, сто, миллион. Без циклов почти ни одна реальная программа не
обходится: список товаров, поиск максимума, чтение файла построчно, обход
всех пикселей картинки.

В C++ есть два основных цикла: `for` и `while`. По возможностям они равны,
но читаются по-разному. Выбирают тот, который понятнее в конкретной
ситуации.

### Цикл `for`

Аналогия: ты делаешь зарядку и считаешь приседания вслух: «один, два, три,
…, десять, всё». У тебя есть стартовое число (один), точка остановки
(десять) и шаг счёта (плюс один за приседание). `for` устроен ровно так
же.

```cpp-run
// hint: распечатать числа от 1 до 5
#include <iostream>
using namespace std;

int main() {
    // поэкспериментируй: поменяй 1 на 0, 5 на 10 или ++i на i += 2
    for (int i = 1; i <= 5; ++i) {
        cout << i << ' ';
    }
    cout << endl;
    return 0;
}
```

Скобки `for` делятся точками с запятой на три части:

1. **Инициализация** `int i = 1`. Здесь рождается переменная-счётчик `i`. Эта строка выполняется один раз, перед первой итерацией.
2. **Условие** `i <= 5`. Перед каждой итерацией программа проверяет его. Если истина, выполняется тело цикла. Если ложь, цикл заканчивается.
3. **Шаг** `++i`. Эта команда выполняется в конце каждой итерации, прямо перед очередной проверкой условия. Запись `++i` это короткое «увеличить `i` на 1», и она равнозначна `i = i + 1`.

Развернём, что происходит при запуске примера выше:

| Итерация | `i` до тела | Проверка `i <= 5` | Что выведет | `i` после шага |
|----------|-------------|-------------------|-------------|----------------|
| 1        | 1           | `true`            | `1`         | 2              |
| 2        | 2           | `true`            | `2`         | 3              |
| 3        | 3           | `true`            | `3`         | 4              |
| 4        | 4           | `true`            | `4`         | 5              |
| 5        | 5           | `true`            | `5`         | 6              |
| 6        | 6           | `false`           | (выход)     | (не считается) |

Шаг можно делать любым: `++i` идёт по одному, `i += 2` сразу через одного,
`i--` назад. Главное, чтобы счётчик действительно двигался к условию
остановки. Поменяй числа и шаг в примере выше и посмотри, как меняется
вывод. Это самый быстрый способ почувствовать, что делает каждая из трёх
частей `for`.

### Цикл `while`

`while` переводится с английского как «пока». Используют его, когда заранее
не знаешь, сколько раз надо повторять, но знаешь условие, при котором пора
остановиться.

Аналогия: ты ешь конфеты из коробки и хочешь продолжать, *пока в коробке
есть конфеты*. Сколько именно их там, ты не считал. `while` работает по
такому же принципу: «проверь условие, если истина, выполни тело и проверь
снова». Когда условие станет ложным, цикл сам остановится.

```cpp-run
// hint: цикл while: делим число пополам, пока оно больше 1
#include <iostream>
using namespace std;

int main() {
    // поэкспериментируй: поставь n = 7, n = 1, n = 1000
    int n = 100;
    while (n > 1) {
        n /= 2;            // запись n /= 2 означает n = n / 2
        cout << n << ' ';  // печатаем, что получилось на этом шаге
    }
    cout << endl;
    return 0;
}
```

При `n = 100` программа напечатает `50 25 12 6 3 1`. На последнем шаге `n`
стало равно `1`, условие `n > 1` дало `false`, и цикл больше не запустился.

Очень важная мысль: в теле цикла нужно обязательно делать что-то, что
приближает условие к ложному состоянию. Иначе цикл будет крутиться
бесконечно. Пример плохого цикла: `int n = 10; while (n > 0) cout << n;`.
Тут `n` никто не меняет, и программа никогда не остановится. Если такое
случилось, окно с кодом «зависнет», и его придётся перезапускать.

Маленькая подсказка по выбору. Если знаешь точное число повторов («сделать
ровно десять раз», «обойти все числа от 1 до `n`»), бери `for`. Если
останавливаешься по условию из реального мира («пока пользователь не введёт
ноль», «пока число больше единицы»), удобнее `while`.

[mini-task:1]

## break и continue

Внутри цикла иногда нужно сказать: «всё, дальше не надо, выходим» или
«пропусти этот шаг и переходи к следующему». Для этого в C++ есть две
специальные команды.

- `break` ломает цикл целиком. Программа сразу выпрыгивает наружу и продолжает выполнение со строки после цикла.
- `continue` пропускает остаток **текущей** итерации. Сам цикл при этом не ломается, программа просто перескакивает к шагу и проверке условия.

Аналогия. Представь, что у тебя список из десяти дел на день.

- `break` это «всё, забиваю, ухожу домой». Остальные дела сегодня делать никто не будет.
- `continue` это «это дело пропущу, оно сейчас не подходит, перехожу к следующему».

```cpp-run
// hint: пропускаем нечётные, останавливаемся на 8
#include <iostream>
using namespace std;

int main() {
    for (int i = 1; i <= 10; ++i) {
        // поэкспериментируй: убери continue или поменяй 8 на 6
        if (i % 2 == 1) continue;   // i нечётное: пропускаем
        if (i == 8) break;          // дошли до 8: выходим из цикла
        cout << i << ' ';
    }
    cout << endl;
    return 0;
}
```

Что напечатает программа? Пройдём по значениям `i`:

- `i = 1`: нечётное, срабатывает `continue`, ничего не выводим.
- `i = 2`: чётное, до восьми не дошли, печатаем `2`.
- `i = 3`: нечётное, пропуск.
- `i = 4`: печатаем `4`.
- `i = 5`: пропуск.
- `i = 6`: печатаем `6`.
- `i = 7`: пропуск.
- `i = 8`: чётное, но срабатывает `break`. Выходим, ничего не печатаем.

Итог: `2 4 6`. Запусти код и проверь себя. Потом попробуй убрать строку с
`continue` или строку с `break` и снова запусти. Так быстро вырабатывается
интуиция, что и когда срабатывает.

Полезно помнить: `break` и `continue` действуют только на **тот цикл,
внутри которого стоят**. Если один цикл вложен в другой, `break` выйдет
только из внутреннего цикла, а внешний продолжит работу как ни в чём не
бывало.

[mini-task:2]

## Итоги

- `if (условие) { ... } else { ... }` выбирает один из двух путей.
- `==`, `!=`, `<`, `<=`, `>`, `>=` это операторы сравнения. Не путай `=` (присваивание) и `==` (сравнение).
- `&&`, `||`, `!` соединяют и отрицают условия.
- `for (init; cond; step)` подходит, когда заранее знаешь число повторов.
- `while (cond)` подходит, когда повторяешь до выполнения условия.
- `break` выходит из цикла, `continue` пропускает одну итерацию.
- Главная опасность цикла это бесконечное повторение. Всегда проверяй, меняется ли в теле цикла то, что упомянуто в условии.
"""

QUIZ_3 = [
    {
        'question': 'Что произойдёт при выполнении break внутри цикла?',
        'a': 'Цикл начнётся заново',
        'b': 'Программа завершится',
        'c': 'Цикл прервётся, выполнится код после цикла',
        'd': 'Будет выведена ошибка',
        'correct': 'c',
        'explanation': 'break вызывает немедленный выход из ближайшего цикла. Программа продолжит выполнение со следующей за циклом инструкции.',
    },
    {
        'question': 'В чём разница между = и ==?',
        'a': 'Никакой',
        'b': '= это присваивание, == это сравнение',
        'c': '= для чисел, == для строк',
        'd': '== работает только в if',
        'correct': 'b',
        'explanation': 'Один знак кладёт значение в переменную, а два проверяют на равенство.',
    },
    {
        'question': 'Сколько раз выполнится цикл: for (int i = 0; i < 5; ++i) { ... } ?',
        'a': '4', 'b': '5', 'c': '6', 'd': 'Бесконечно',
        'correct': 'b',
        'explanation': 'i принимает значения 0, 1, 2, 3, 4, пока выполняется условие i < 5. Это ровно 5 итераций.',
    },
    {
        'question': 'Какой оператор означает «логическое И»?',
        'a': '&&', 'b': '||', 'c': '!', 'd': 'and',
        'correct': 'a',
        'explanation': 'Оператор && означает логическое И (оба условия истинны), || означает ИЛИ, ! означает НЕ. В C++ ключевое слово and не используется.',
    },
    {
        'question': 'Как правильно записать «x от 1 до 10 включительно»?',
        'a': '1 < x > 10',
        'b': '1 <= x <= 10',
        'c': 'x >= 1 && x <= 10',
        'd': 'x == 1 || x == 10',
        'correct': 'c',
        'explanation': 'C++ не понимает 1 <= x <= 10 как математик: нужно явно соединить два сравнения через &&.',
    },
    {
        'question': 'Что делает continue внутри цикла?',
        'a': 'Выходит из цикла',
        'b': 'Пропускает остаток итерации, переходит к следующей',
        'c': 'Завершает программу',
        'd': 'Ничего не делает',
        'correct': 'b',
        'explanation': 'continue прерывает только текущую итерацию: следующие инструкции в теле цикла пропускаются, и сразу начинается следующая итерация.',
    },
]

MINI_TASKS_3 = [
    {
        'title': 'Возраст и совершеннолетие',
        'description': (
            'Прочитайте целое число (возраст человека). Если он 18 или больше, '
            'выведите `совершеннолетний`, иначе `несовершеннолетний`.'
        ),
        'starter_code': (
            '#include <iostream>\n'
            'using namespace std;\n\n'
            'int main() {\n'
            '    int age;\n'
            '    cin >> age;\n'
            '    // Ваш код\n'
            '\n'
            '    return 0;\n'
            '}\n'
        ),
        'tests': [
            {'input': '18', 'expected': 'совершеннолетний'},
            {'input': '17', 'expected': 'несовершеннолетний'},
            {'input': '5', 'expected': 'несовершеннолетний'},
            {'input': '99', 'expected': 'совершеннолетний'},
        ],
        'hint': 'Структура такая же, как в примере про знак числа в теории: там тоже использовались `if` и `else`. Здесь нужны те же ветки, только условие сравнивает возраст с числом 18.',
    },
    {
        'title': 'Сумма от 1 до n',
        'description': (
            'Прочитайте натуральное число `n`. Выведите сумму всех целых чисел от 1 '
            'до n включительно.\n\n'
            '## Пример\nВвод: `5` → Вывод: `15` (т.к. 1+2+3+4+5).'
        ),
        'starter_code': (
            '#include <iostream>\n'
            'using namespace std;\n\n'
            'int main() {\n'
            '    int n;\n'
            '    cin >> n;\n'
            '    int sum = 0;\n'
            '    // Ваш код\n'
            '\n'
            '    cout << sum;\n'
            '    return 0;\n'
            '}\n'
        ),
        'tests': [
            {'input': '1', 'expected': '1'},
            {'input': '5', 'expected': '15'},
            {'input': '10', 'expected': '55'},
            {'input': '100', 'expected': '5050'},
        ],
        'hint': 'В теории мы видели пример с циклом `for`, который выводил числа от 1 до 5. Здесь нужен такой же цикл, только вместо вывода числа прибавляйте его к переменной `sum`.',
    },
    {
        'title': 'Только чётные',
        'description': (
            'Прочитайте число `n`. Выведите через пробел все **чётные** числа '
            'от 1 до n включительно.\n\n'
            '## Пример\nВвод: `10` → Вывод: `2 4 6 8 10`'
        ),
        'starter_code': (
            '#include <iostream>\n'
            'using namespace std;\n\n'
            'int main() {\n'
            '    int n;\n'
            '    cin >> n;\n'
            '    for (int i = 1; i <= n; ++i) {\n'
            '        // Ваш код\n'
            '\n'
            '    }\n'
            '    return 0;\n'
            '}\n'
        ),
        'tests': [
            {'input': '10', 'expected': '2 4 6 8 10'},
            {'input': '5', 'expected': '2 4'},
            {'input': '1', 'expected': ''},
            {'input': '2', 'expected': '2'},
        ],
        'hint': 'В теории мы видели пример с `continue`, где цикл пропускал нечётные числа. Здесь нужно сделать то же самое: пройти от 1 до n, через `continue` пропускать нечётные, а остальные выводить.',
    },
]

# ============================================================================
# ТЕОРИЯ 4 — Функции
# ============================================================================

THEORY_4 = """\
# Функции: как разделить программу на блоки

## Зачем нужны функции?

Когда программа становится больше, в ней появляются куски, которые повторяются:
например, мы пять раз считаем максимум двух чисел, или трижды печатаем
красивый разделитель. Чтобы не дублировать код, его упаковывают в **функцию** —
именованный блок, который можно вызывать сколько угодно раз.

Функцию можно представить как кулинарный рецепт: один раз записал, потом
просто говоришь «приготовь блюдо номер 3», и не пересказываешь весь рецепт.

## Объявление функции

```cpp-run
// hint: своя функция для квадрата
#include <iostream>
using namespace std;

// Функция, возвращающая квадрат числа
int square(int x) {
    return x * x;
}

int main() {
    cout << square(5) << endl;   // 25
    cout << square(7) << endl;   // 49
    return 0;
}
```

Структура объявления функции:

```
тип_возвращаемого_значения  имя  ( список_параметров ) {
    тело
    return значение;
}
```

Разбираем:

- `int` перед именем указывает, что вернёт функция. Здесь `int`, потому что
  квадрат целого числа тоже целый.
- `square` — имя функции, по нему мы её и вызываем.
- `(int x)` — **параметр**: переменная, в которую попадёт переданное
  значение при вызове.
- `return x * x;` возвращает результат. Сразу после `return` функция
  завершается.

## Функции без возвращаемого значения

Иногда функция ничего не возвращает: она просто делает что-то (например,
печатает разделитель). Тогда тип возврата записывают как `void` («ничто»).

```cpp-run
// hint: функция без возврата (void): печатает разделитель
#include <iostream>
using namespace std;

void print_line() {
    cout << "----------" << endl;
}

int main() {
    print_line();
    cout << "Заголовок" << endl;
    print_line();
    return 0;
}
```

Заметьте: внутри `void`-функции мы не пишем `return значение;`. Можно
написать просто `return;` (это означает «выйти из функции прямо сейчас»),
но обычно и этого не нужно.

[mini-task:0]

## Несколько параметров

Функция может принимать сколько угодно параметров; они перечисляются через
запятую:

```cpp-run
// hint: функция с двумя параметрами: возвращает максимум
#include <iostream>
using namespace std;

int max_of_two(int a, int b) {
    if (a > b) return a;
    else      return b;
}

int main() {
    cout << max_of_two(10, 20) << endl;   // 20
    cout << max_of_two(7, 3)   << endl;   // 7
    return 0;
}
```

При вызове `max_of_two(10, 20)` значение `10` попадает в `a`, а `20` в `b`.
Порядок аргументов очень важен.

[mini-task:1]

## Что такое область видимости

Переменная, объявленная **внутри функции**, существует только внутри неё.
Снаружи её не видно. Это сделано специально, чтобы функции не «портили»
друг другу данные.

```cpp
int square(int x) {
    int result = x * x;     // result живёт только внутри square
    return result;
}

int main() {
    cout << result;         // ОШИБКА: result в main не видно
}
```

Если нужно поделиться значением, функция возвращает его через `return`,
а вызывающая сторона его принимает.

## Зачем разбивать программу на функции

1. **Меньше повторов.** Один и тот же блок не нужно писать дважды.
2. **Понятнее.** Читая `int m = max_of_two(a, b);`, легко понять смысл,
   даже не глядя в код функции.
3. **Легче тестировать.** Можно проверить функцию отдельно, прежде чем
   встраивать её в большую программу.
4. **Рекурсия.** Функция может вызывать саму себя; это мощный инструмент,
   который мы будем активно использовать в задачах про деревья и поиск.

```cpp-run
// hint: рекурсивный факториал
#include <iostream>
using namespace std;

int factorial(int n) {
    if (n <= 1) return 1;        // база рекурсии
    return n * factorial(n - 1); // шаг: выражаем через себя
}

int main() {
    cout << factorial(5) << endl;   // 120
    return 0;
}
```

[mini-task:2]

## Итоги

- Функция — повторно используемый именованный блок кода.
- `тип имя(параметры) { тело; return ... }`.
- `void` — функция без возвращаемого значения.
- Переменные внутри функции живут только пока она работает.
- Функция может вызывать саму себя (рекурсия). Главное, чтобы был случай
  выхода (база), иначе будет бесконечный вызов.
"""

QUIZ_4 = [
    {
        'question': 'Что вернёт функция, у которой указан тип возврата void?',
        'a': '0', 'b': 'Ничего', 'c': 'NULL', 'd': 'Строку',
        'correct': 'b',
        'explanation': 'void буквально означает «ничто»: функция не возвращает никакого значения. Внутри неё используют просто return; либо вообще обходятся без него.',
    },
    {
        'question': 'Что произойдёт после выполнения return в функции?',
        'a': 'Программа завершится',
        'b': 'Цикл прервётся',
        'c': 'Функция немедленно завершит работу и вернёт значение',
        'd': 'Будет выведена ошибка',
        'correct': 'c',
        'explanation': 'return сразу прекращает выполнение функции и передаёт указанное значение тому, кто её вызвал. Код после return уже не выполнится.',
    },
    {
        'question': 'Можно ли использовать переменную, объявленную в одной функции, внутри другой?',
        'a': 'Да, всегда',
        'b': 'Нет, переменная видна только внутри своей функции',
        'c': 'Только если она имеет тип int',
        'd': 'Только в main',
        'correct': 'b',
        'explanation': 'Переменная имеет локальную область видимости: она существует только пока работает «своя» функция и недоступна из других функций.',
    },
    {
        'question': 'Что обязательно для рекурсивной функции, чтобы она не зациклилась?',
        'a': 'Параметр типа int',
        'b': 'Базовый случай (условие выхода без рекурсивного вызова)',
        'c': 'Цикл for',
        'd': 'Тип возврата void',
        'correct': 'b',
        'explanation': 'Без базового случая функция будет вызывать сама себя бесконечно.',
    },
    {
        'question': 'Что напечатает программа?\n\n```\nint mystery(int x) { return x + 1; }\nint main() { cout << mystery(mystery(5)); }\n```',
        'a': '5', 'b': '6', 'c': '7', 'd': 'Ошибка',
        'correct': 'c',
        'explanation': 'mystery(5) == 6, потом mystery(6) == 7.',
    },
    {
        'question': 'Зачем выделять часть кода в отдельную функцию?',
        'a': 'Чтобы её можно было повторно использовать и читать яснее',
        'b': 'Это требование компилятора',
        'c': 'Чтобы программа работала быстрее',
        'd': 'Чтобы освободить память',
        'correct': 'a',
        'explanation': 'Главная цель функций состоит в переиспользовании и читаемости: вызов одной функции по имени гораздо понятнее, чем десять строк дублированного кода в каждом месте.',
    },
]

MINI_TASKS_4 = [
    {
        'title': 'Функция приветствия',
        'description': (
            'Реализуйте функцию `void greet()`, которая выводит на экран строку '
            '`Привет!`. В `main` вызовите её **три раза подряд** (каждый раз с новой строки).\n\n'
            '## Ожидаемый вывод\n```\nПривет!\nПривет!\nПривет!\n```'
        ),
        'starter_code': (
            '#include <iostream>\n'
            'using namespace std;\n\n'
            'void greet() {\n'
            '    // Ваш код\n'
            '\n'
            '}\n\n'
            'int main() {\n'
            '    // Ваш код\n'
            '\n'
            '    return 0;\n'
            '}\n'
        ),
        'tests': [{'input': '', 'expected': 'Привет!\nПривет!\nПривет!'}],
        'hint': 'Структура такая же, как у функции `square` из теории, только без параметров и без возврата (`void` вместо `int`). В `main` вызовите `greet()` со скобками так же, как `square(5)`, но без аргумента, и сделайте это три раза подряд.',
    },
    {
        'title': 'Функция min_of_two',
        'description': (
            'Реализуйте функцию `int min_of_two(int a, int b)`, возвращающую '
            'наименьшее из двух чисел. В `main` прочитайте два числа и выведите '
            'результат вызова функции.\n\n'
            '## Пример\nВвод: `7 3` → Вывод: `3`'
        ),
        'starter_code': (
            '#include <iostream>\n'
            'using namespace std;\n\n'
            'int min_of_two(int a, int b) {\n'
            '    // Ваш код\n'
            '\n'
            '}\n\n'
            'int main() {\n'
            '    int x, y;\n'
            '    cin >> x >> y;\n'
            '    cout << min_of_two(x, y);\n'
            '    return 0;\n'
            '}\n'
        ),
        'tests': [
            {'input': '7 3', 'expected': '3'},
            {'input': '5 5', 'expected': '5'},
            {'input': '-10 0', 'expected': '-10'},
            {'input': '100 99', 'expected': '99'},
        ],
        'hint': 'Идея такая же, как у функции `max_of_two` из теории, только сравнение в другую сторону. Используйте `if/else` внутри функции и верните меньшее значение через `return`.',
    },
    {
        'title': 'Сумма цифр (рекурсия)',
        'description': (
            'Реализуйте функцию `int digit_sum(int n)`, которая возвращает сумму '
            'цифр положительного целого числа `n`, **используя рекурсию**.\n\n'
            '## Пример\nВвод: `123` → Вывод: `6` (1+2+3).'
        ),
        'starter_code': (
            '#include <iostream>\n'
            'using namespace std;\n\n'
            'int digit_sum(int n) {\n'
            '    // Ваш код\n'
            '\n'
            '}\n\n'
            'int main() {\n'
            '    int n;\n'
            '    cin >> n;\n'
            '    cout << digit_sum(n);\n'
            '    return 0;\n'
            '}\n'
        ),
        'tests': [
            {'input': '123', 'expected': '6'},
            {'input': '0', 'expected': '0'},
            {'input': '9', 'expected': '9'},
            {'input': '10000', 'expected': '1'},
            {'input': '99999', 'expected': '45'},
        ],
        'hint': 'Структура та же, что у рекурсивной функции `factorial` из теории: база + шаг. База: если `n == 0`, верните 0. Шаг: верните последнюю цифру (`n % 10`) плюс результат `digit_sum(n / 10)`.',
    },
]

# ============================================================================
# Сборка модуля
# ============================================================================

MODULE = {
    'title': 'Основы C++',
    'description': (
        'Самый первый модуль курса. Знакомимся с языком C++ для тех, кто '
        'программирует впервые: учимся писать программы, использовать переменные, '
        'управлять потоком и разбивать код на функции. По окончании модуля '
        'вы спокойно поймёте любой простой код на C++.'
    ),
    'concepts': (
        'структура программы (#include, using namespace std, int main, return); '
        'типы int, long long, double, char, bool, string; cin >> и cout <<, endl, \\n; '
        'арифметика +, -, *, /, %, составные присваивания +=, -=, *=, /=; '
        'условия if/else, операторы сравнения ==, !=, <, >, <=, >=, '
        'логические &&, ||, !; циклы for и while, break, continue; '
        'функции (объявление, параметры, return, void), рекурсия'
    ),
    'order': 1,
    'theories': [
        {
            'title': 'Первые шаги в C++: устройство простой программы',
            'content': THEORY_1,
            'order': 0,
            'mini_tasks': MINI_TASKS_1,
            'quiz': QUIZ_1,
        },
        {
            'title': 'Переменные и типы данных',
            'content': THEORY_2,
            'order': 1,
            'mini_tasks': MINI_TASKS_2,
            'quiz': QUIZ_2,
        },
        {
            'title': 'Условия и циклы',
            'content': THEORY_3,
            'order': 2,
            'mini_tasks': MINI_TASKS_3,
            'quiz': QUIZ_3,
        },
        {
            'title': 'Функции',
            'content': THEORY_4,
            'order': 3,
            'mini_tasks': MINI_TASKS_4,
            'quiz': QUIZ_4,
        },
    ],
    'tasks': [
        # Уровень 1 — переменные, ввод-вывод, арифметика (теории 1–2)
        {
            'title': 'Сумма двух чисел',
            'description': (
                'Прочитайте два целых числа и выведите их сумму.\n\n'
                '## Ввод\nДва целых числа через пробел.\n'
                '## Вывод\nИх сумма.\n\n'
                '## Пример\n```\n3 5\n```\n```\n8\n```'
            ),
            'difficulty': 1,
            'tests': [
                {'input': '3 5', 'expected': '8'},
                {'input': '-10 4', 'expected': '-6'},
                {'input': '0 0', 'expected': '0'},
                {'input': '100 200', 'expected': '300'},
            ],
            'hint': 'Объяви две переменных типа int, прочитай их через cin, выведи сумму.',
            'tags': ['io'],
            'order': 0,
            'variations': [
                {
                    'title': 'Произведение двух чисел',
                    'description': (
                        'Прочитайте два целых числа и выведите их произведение.\n\n'
                        '## Пример\n```\n4 5\n```\n```\n20\n```'
                    ),
                    'tests': [
                        {'input': '4 5', 'expected': '20'},
                        {'input': '0 100', 'expected': '0'},
                        {'input': '-3 7', 'expected': '-21'},
                        {'input': '1 1', 'expected': '1'},
                    ],
                    'hint': 'Принцип такой же, как при сумме, только используется другой арифметический оператор.',
                },
            ],
        },

        # Уровень 2 — условия и циклы (теория 3)
        {
            'title': 'Сумма от 1 до N',
            'description': (
                'Прочитайте натуральное число `n` (1 ≤ n ≤ 10⁶) и выведите '
                'сумму всех целых чисел от 1 до n включительно.\n\n'
                '## Пример\n```\n5\n```\n```\n15\n```'
            ),
            'difficulty': 2,
            'tests': [
                {'input': '5', 'expected': '15'},
                {'input': '1', 'expected': '1'},
                {'input': '100', 'expected': '5050'},
                {'input': '1000000', 'expected': '500000500000'},
            ],
            'hint': 'Заметь, что сумма может не поместиться в int, используй long long.',
            'tags': ['loops'],
            'order': 1,
            'variations': [
                {
                    'title': 'Сумма чётных чисел до N',
                    'description': (
                        'Прочитайте натуральное `n` (1 ≤ n ≤ 10⁶) и выведите сумму всех '
                        'чётных чисел от 1 до n включительно.\n\n'
                        '## Пример\n```\n10\n```\n```\n30\n```'
                    ),
                    'tests': [
                        {'input': '10', 'expected': '30'},
                        {'input': '1', 'expected': '0'},
                        {'input': '2', 'expected': '2'},
                        {'input': '100', 'expected': '2550'},
                    ],
                    'hint': 'Цикл от 1 до n; внутри проверяйте чётность через остаток.',
                },
            ],
        },

        # Уровень 3 — функции и рекурсия (теория 4)
        {
            'title': 'НОД двух чисел',
            'description': (
                'Реализуйте функцию, которая возвращает наибольший общий делитель '
                'двух натуральных чисел (алгоритм Евклида), и используйте её в `main`.\n\n'
                '## Ввод\nДва натуральных числа через пробел.\n'
                '## Вывод\nИх НОД.\n\n'
                '## Пример\n```\n12 18\n```\n```\n6\n```'
            ),
            'difficulty': 3,
            'tests': [
                {'input': '12 18', 'expected': '6'},
                {'input': '7 13', 'expected': '1'},
                {'input': '100 75', 'expected': '25'},
                {'input': '1000000 999999', 'expected': '1'},
            ],
            'hint': 'Алгоритм Евклида: gcd(a, b) = gcd(b, a % b), gcd(a, 0) = a.',
            'tags': ['functions', 'recursion'],
            'order': 2,
            'variations': [
                {
                    'title': 'НОК двух чисел',
                    'description': (
                        'Реализуйте функцию, возвращающую наименьшее общее кратное двух '
                        'натуральных чисел. Используйте формулу: `НОК(a, b) = a * b / НОД(a, b)`.\n\n'
                        '## Пример\n```\n4 6\n```\n```\n12\n```'
                    ),
                    'tests': [
                        {'input': '4 6', 'expected': '12'},
                        {'input': '3 5', 'expected': '15'},
                        {'input': '12 18', 'expected': '36'},
                        {'input': '7 7', 'expected': '7'},
                    ],
                    'hint': 'Сначала найдите НОД (как в основной задаче), затем поделите произведение на него.',
                },
            ],
        },
    ],
}
