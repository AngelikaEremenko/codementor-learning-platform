# -*- coding: utf-8 -*-
"""Модуль 6: Стек и очередь."""

# ============================================================================
# ТЕОРИЯ 1 — Стек
# ============================================================================

THEORY_1 = """\
# Стек: последний пришёл, первый ушёл

## Что такое стек

Стек (англ. *stack*, «стопка») это структура данных с очень простым, но
строгим правилом: **видно и можно трогать только верхний элемент**. Когда
ты кладёшь что-то новое, оно ложится сверху. Когда забираешь, забираешь
тоже сверху. До нижних или средних элементов добраться нельзя.

Аналогия. Представь стопку тарелок в кафе. Новую тарелку моют и кладут на
самый верх. Когда официант берёт тарелку, он берёт верхнюю. Никто не лезет
в середину стопки, иначе она просто рассыпется. Стек устроен ровно так
же.

Это поведение принято называть **LIFO**, от Last In, First Out: «последний
пришёл, первый ушёл». Кто положил последним, того первым и заберут.

## std::stack

В стандартной библиотеке C++ стек уже готов: подключаешь заголовок
`<stack>` и пользуешься.

```cpp-run
// hint: добавляем и забираем элементы
#include <iostream>
#include <stack>
using namespace std;

int main() {
    stack<int> s;
    // поэкспериментируй: добавь ещё push, выведи top после нескольких pop
    s.push(1);                                // содержимое: [1]
    s.push(2);                                // содержимое: [1, 2]
    s.push(3);                                // содержимое: [1, 2, 3]

    cout << "Сверху: " << s.top() << endl;    // 3
    s.pop();                                  // удалили верхний (тройку)
    cout << "Сверху: " << s.top() << endl;    // 2
    cout << "Размер: " << s.size() << endl;   // 2
    return 0;
}
```

| Метод       | Что делает                                |
|-------------|--------------------------------------------|
| `push(x)`   | положить `x` наверх                       |
| `pop()`     | убрать верхний элемент (без возврата!)    |
| `top()`     | посмотреть верхний (не удаляя)            |
| `size()`    | количество элементов в стеке              |
| `empty()`   | `true`, если стек пуст                    |

Очень важная мелочь, на которой новички часто спотыкаются: `pop()` **не
возвращает** удалённый элемент. Если значение тебе нужно, сначала смотри
его через `top()`, потом убирай через `pop()`. Это распространённый шаблон
из двух строк:

```cpp
int x = s.top();
s.pop();
```

Все эти операции работают за O(1): сколько бы элементов ни лежало в стеке,
положить или забрать с верхушки занимает одно и то же короткое время.

[mini-task:0]

## Зачем нужен стек

Стек кажется ограниченной структурой («ну я же могу с любого индекса
обратиться, что мне даст этот ваш стек?»), но именно из-за своих
ограничений он невероятно полезен в задачах, где важен **порядок**
действий и **возврат назад**.

Несколько классических применений:

1. **Проверка скобок.** Открывающую скобку кладём в стек. Когда встречаем закрывающую, смотрим на верхушку: если там парная открывающая, всё хорошо, делаем `pop`. Если нет или стек пуст, последовательность неправильная.
2. **Отмена действий (Undo).** Каждое действие пользователя кладётся в стек через `push`. Ctrl+Z это просто `pop` верхушки и применение обратного действия.
3. **Поиск в глубину (DFS).** Подробно рассмотрим в модуле «Деревья», но идея та же: стек хранит «куда ещё нужно вернуться».
4. **Вычисление арифметических выражений** в обратной польской записи (постфиксной). На число делаем `push`, на оператор берём два числа сверху, считаем и кладём результат назад.

```cpp-run
// hint: проверяем правильность скобочной последовательности
#include <iostream>
#include <stack>
#include <string>
using namespace std;

int main() {
    string s = "(()(()))";
    // поэкспериментируй: поменяй s на "(()", "(()))(", "((()))"
    stack<char> st;
    bool ok = true;
    for (char c : s) {
        if (c == '(') st.push(c);
        else {
            if (st.empty()) { ok = false; break; }   // закрыли то, что не открывали
            st.pop();
        }
    }
    if (!st.empty()) ok = false;                     // остались незакрытые скобки
    cout << (ok ? "Правильно" : "Неправильно") << endl;
    return 0;
}
```

Этот пример хорошо иллюстрирует природу стека. Каждая открывающая скобка
ждёт «свою» закрывающую, и нам нужно знать, какая открывалась последней. В
этом стек и помогает: верхушка это всегда «последняя не закрытая скобка».

[mini-task:1]

## Стек на векторе

Если по какой-то причине не хочется подключать `<stack>`, ту же логику
можно эмулировать обычным `std::vector`. Соответствие методов простое:
`push_back` это `push`, `back()` это `top`, `pop_back` это `pop`. На
производительность это никак не влияет, и бонус в том, что у вектора есть
ещё и доступ по индексу. Иногда это удобно для отладки.

## Итоги

- Стек работает по принципу LIFO: последний пришёл, первый ушёл. Доступен только верхний элемент.
- Все основные операции (`push`, `pop`, `top`, `size`, `empty`) выполняются за O(1).
- `pop` не возвращает значение. Сначала `top`, потом `pop`.
- Применяется при разборе скобок, отмене действий, обходах графов в глубину, вычислении выражений в постфиксной записи.
"""

QUIZ_1 = [
    {
        'question': 'Какой принцип у стека?',
        'a': 'FIFO', 'b': 'LIFO', 'c': 'Random', 'd': 'Sorted',
        'correct': 'b',
    },
    {
        'question': 'Какая сложность операций push/pop у std::stack?',
        'a': 'O(1)', 'b': 'O(log n)', 'c': 'O(n)', 'd': 'O(n log n)',
        'correct': 'a',
    },
    {
        'question': 'Что возвращает s.pop()?',
        'a': 'Удалённый элемент',
        'b': 'Ничего, просто удаляет верхний',
        'c': 'Размер стека',
        'd': 'true / false',
        'correct': 'b',
        'explanation': 'Если хотите получить элемент, сначала вызовите top(), потом pop().',
    },
    {
        'question': 'Какая задача типично решается с помощью стека?',
        'a': 'Сортировка',
        'b': 'Проверка правильности скобочной последовательности',
        'c': 'Двоичный поиск',
        'd': 'Подсчёт частот',
        'correct': 'b',
    },
    {
        'question': 'Какое поведение НЕЛЬЗЯ ожидать от std::stack?',
        'a': 'Доступ к произвольному элементу по индексу',
        'b': 'Размер через size()',
        'c': 'Проверку пустоты через empty()',
        'd': 'Доступ к верхнему элементу через top()',
        'correct': 'a',
        'explanation': 'У стека намеренно нет произвольного доступа, и это часть его дизайна.',
    },
]

MINI_TASKS_1 = [
    {
        'title': 'Развернуть последовательность',
        'description': (
            'Прочитайте `n` и `n` целых чисел. Выведите их в обратном порядке, '
            'через пробел. Используйте стек.\n\n'
            '## Пример\n```\n5\n1 2 3 4 5\n```\n```\n5 4 3 2 1\n```'
        ),
        'starter_code': (
            '#include <iostream>\n'
            '#include <stack>\n'
            'using namespace std;\n\n'
            'int main() {\n'
            '    int n;\n'
            '    cin >> n;\n'
            '    // Ваш код\n'
            '\n'
            '    return 0;\n'
            '}\n'
        ),
        'tests': [
            {'input': '5\n1 2 3 4 5', 'expected': '5 4 3 2 1'},
            {'input': '1\n42', 'expected': '42'},
            {'input': '3\n7 7 7', 'expected': '7 7 7'},
        ],
        'hint': 'LIFO-природа стека идеально подходит для разворота: что положили последним, заберём первым.',
    },
    {
        'title': 'Парные скобки только из (',
        'description': (
            'Прочитайте строку из символов `(` и `)`. Выведите `YES`, если '
            'это правильная скобочная последовательность, иначе `NO`.\n\n'
            '## Пример\n```\n(())\n```\n```\nYES\n```'
        ),
        'starter_code': (
            '#include <iostream>\n'
            '#include <stack>\n'
            '#include <string>\n'
            'using namespace std;\n\n'
            'int main() {\n'
            '    string s;\n'
            '    cin >> s;\n'
            '    // Ваш код\n'
            '\n'
            '    return 0;\n'
            '}\n'
        ),
        'tests': [
            {'input': '(())', 'expected': 'YES'},
            {'input': '()()', 'expected': 'YES'},
            {'input': '(', 'expected': 'NO'},
            {'input': ')(', 'expected': 'NO'},
            {'input': '((()))', 'expected': 'YES'},
        ],
        'hint': 'На каждое `(` делайте push, а на каждое `)` проверяйте вершину и pop. В конце стек должен быть пуст.',
    },
]

# ============================================================================
# ТЕОРИЯ 2 — Очередь и дек
# ============================================================================

THEORY_2 = """\
# Очередь и дек: «первый пришёл, первый ушёл»

## Очередь (FIFO)

Очередь работает прямо как живая очередь у кассы в магазине: кто встал
первым, того первым и обслужат. Никто не пускают вперёд, никто не прыгает
через головы.

Этот принцип называется **FIFO**, от First In, First Out: «первый пришёл,
первый ушёл». В стеке было наоборот, и это вторая важная структура,
которую нужно держать в голове.

Аналогия. Очередь это труба, в которую с одного конца кладут шарики, а с
другого они выкатываются. У трубы есть **хвост** (туда добавляют) и
**голова** (оттуда забирают).

```cpp-run
// hint: добавляем в хвост, забираем из головы
#include <iostream>
#include <queue>
using namespace std;

int main() {
    queue<int> q;
    // поэкспериментируй: добавь больше элементов, чередуй push и pop
    q.push(1);                                  // содержимое: [1]
    q.push(2);                                  // содержимое: [1, 2]
    q.push(3);                                  // содержимое: [1, 2, 3]

    cout << "Голова: " << q.front() << endl;    // 1
    q.pop();                                    // удалили голову (единицу)
    cout << "Голова: " << q.front() << endl;    // 2
    cout << "Хвост:  " << q.back()  << endl;    // 3
    return 0;
}
```

| Метод     | Что делает                              |
|-----------|------------------------------------------|
| `push(x)` | добавить `x` в хвост                    |
| `pop()`   | удалить голову (без возврата значения)  |
| `front()` | посмотреть голову (не удаляя)           |
| `back()`  | посмотреть хвост (не удаляя)            |
| `size()`  | количество элементов                    |
| `empty()` | `true`, если очередь пуста              |

Все эти операции тоже выполняются за O(1). Логика та же, что у стека: чтобы
получить значение из головы, сначала `front()`, потом `pop()`.

## Где встречается очередь

Очередь это сердце многих алгоритмов и систем, в которых порядок
обработки имеет значение:

- **Поиск в ширину (BFS)** на графах и деревьях: складываем в очередь то, что нужно посетить, и обрабатываем по порядку.
- **Симуляция реальных процессов**: «пришёл клиент, обработали, вызвали следующего». На очереди строятся очереди в банке, печать документов, обработка заявок.
- **Конвейер задач**: одна часть программы что-то производит и кладёт в очередь, другая часть забирает и обрабатывает. Так устроены многие веб-серверы.

[mini-task:0]

## Дек (deque)

`std::deque` это **дек**, или двусторонняя очередь. Название происходит
от Double-Ended Queue. От обычной очереди отличается тем, что добавлять и
удалять можно с **любого** конца, а не только с одного.

Аналогия. Если обычная очередь это труба, то дек это коридор с двумя
выходами. Можно зайти и с одной стороны, и с другой, и так же выйти. Это
самая гибкая из трёх изученных структур.

```cpp-run
// hint: дек умеет добавлять и удалять с обоих концов
#include <iostream>
#include <deque>
using namespace std;

int main() {
    deque<int> dq;
    // поэкспериментируй: попробуй разные комбинации push_front/push_back/pop_*
    dq.push_back(1);                            // [1]
    dq.push_back(2);                            // [1, 2]
    dq.push_front(0);                           // [0, 1, 2]

    cout << "Спереди: " << dq.front() << endl;  // 0
    cout << "Сзади:   " << dq.back()  << endl;  // 2

    dq.pop_front();                             // [1, 2]
    dq.pop_back();                              // [1]
    cout << "Размер: " << dq.size() << endl;
    return 0;
}
```

У дека есть ещё одна приятная мелочь: индексный доступ `dq[i]`, как у
вектора. По производительности дек чуть медленнее вектора, но все основные
операции (добавление, удаление, доступ) у него тоже O(1).

[mini-task:1]

## Что выбирать

| Нужно...                                | Бери                  |
|-----------------------------------------|------------------------|
| LIFO: важна только верхушка             | `stack`                |
| FIFO: разные концы для добавления и удаления | `queue`           |
| Свобода с обеих сторон                  | `deque`                |
| Произвольный доступ по индексу          | `vector` или `deque`   |

Маленький практический совет: если задача формулируется как «обработай
элементы в порядке поступления», скорее всего, нужна очередь. Если в
формулировке есть слово «откатить», «отменить» или «вернуться к
предыдущему», скорее всего, стек.

## Итоги

- Очередь работает по принципу FIFO: первый пришёл, первый ушёл.
- Все операции (`push`, `pop`, `front`, `back`) выполняются за O(1).
- Дек это двусторонняя очередь: можно добавлять и удалять с обоих концов одинаково удобно.
- Очередь используется в BFS, симуляциях процессов и конвейерах задач.
"""

QUIZ_2 = [
    {
        'question': 'Какой принцип у очереди?',
        'a': 'FIFO', 'b': 'LIFO', 'c': 'Random', 'd': 'Sorted',
        'correct': 'a',
    },
    {
        'question': 'Какой метод queue возвращает голову (не удаляя)?',
        'a': 'top()', 'b': 'front()', 'c': 'first()', 'd': 'head()',
        'correct': 'b',
    },
    {
        'question': 'Какой алгоритм типично использует очередь как основной инструмент?',
        'a': 'DFS (поиск в глубину)',
        'b': 'BFS (поиск в ширину)',
        'c': 'Бинарный поиск',
        'd': 'Сортировка слиянием',
        'correct': 'b',
    },
    {
        'question': 'Что умеет deque, чего не умеет queue?',
        'a': 'Добавлять и удалять элементы с обоих концов',
        'b': 'Хранить больше элементов',
        'c': 'Сортироваться',
        'd': 'Ничего нового',
        'correct': 'a',
    },
    {
        'question': 'Какова сложность push_front и push_back у deque?',
        'a': 'O(1)', 'b': 'O(log n)', 'c': 'O(n)', 'd': 'O(n²)',
        'correct': 'a',
    },
]

MINI_TASKS_2 = [
    {
        'title': 'Симуляция очереди',
        'description': (
            'Прочитайте число команд `q`, затем `q` строк. Каждая строка содержит '
            'либо `+ x` (добавить число x в конец очереди), либо `-` '
            '(удалить голову и вывести её значение).\n\n'
            'Гарантируется, что `-` приходит только при непустой очереди.\n\n'
            '## Пример\n```\n5\n+ 1\n+ 2\n-\n+ 3\n-\n```\n```\n1\n2\n```'
        ),
        'starter_code': (
            '#include <iostream>\n'
            '#include <queue>\n'
            'using namespace std;\n\n'
            'int main() {\n'
            '    int q;\n'
            '    cin >> q;\n'
            '    // Ваш код\n'
            '\n'
            '    return 0;\n'
            '}\n'
        ),
        'tests': [
            {'input': '5\n+ 1\n+ 2\n-\n+ 3\n-', 'expected': '1\n2'},
            {'input': '3\n+ 10\n+ 20\n-', 'expected': '10'},
            {'input': '4\n+ 5\n-\n+ 7\n-', 'expected': '5\n7'},
        ],
        'hint': 'Прочитайте символ команды и при необходимости прочитайте следующее число.',
    },
    {
        'title': 'Дек: добавь и удали',
        'description': (
            'Прочитайте `n` пар команд. Каждая пара состоит из типа команды '
            '(`F` для front, `B` для back) и числа `x`. Каждая команда '
            '**добавляет** x в '
            'соответствующий конец дека.\n\n'
            'После всех команд выведите содержимое дека от головы к хвосту через пробел.\n\n'
            '## Пример\n```\n4\nB 1\nB 2\nF 0\nB 3\n```\n```\n0 1 2 3\n```'
        ),
        'starter_code': (
            '#include <iostream>\n'
            '#include <deque>\n'
            'using namespace std;\n\n'
            'int main() {\n'
            '    int n;\n'
            '    cin >> n;\n'
            '    deque<int> dq;\n'
            '    // Ваш код\n'
            '\n'
            '    for (int x : dq) cout << x << \' \';\n'
            '    return 0;\n'
            '}\n'
        ),
        'tests': [
            {'input': '4\nB 1\nB 2\nF 0\nB 3', 'expected': '0 1 2 3'},
            {'input': '3\nF 1\nF 2\nF 3', 'expected': '3 2 1'},
            {'input': '2\nB 5\nB 5', 'expected': '5 5'},
        ],
        'hint': 'Соответствие очевидное: F → push_front, B → push_back.',
    },
]

# ============================================================================
MODULE = {
    'title': 'Стек и очередь',
    'description': (
        'Структуры данных с ограниченным доступом: стек, очередь и дек. '
        'Применяются при разборе скобок, моделировании процессов и обходах графов.'
    ),
    'concepts': (
        'std::stack<T>: push, pop, top, size, empty; '
        'std::queue<T>: push, pop, front, back, size, empty; '
        'std::deque<T>: push_front, push_back, pop_front, pop_back, front, back, dq[i]; '
        'применение стека для проверки скобочной последовательности и '
        'вычисления выражения в обратной польской записи'
    ),
    'order': 6,
    'theories': [
        {'title': 'Стек: LIFO', 'content': THEORY_1, 'order': 0,
         'mini_tasks': MINI_TASKS_1, 'quiz': QUIZ_1},
        {'title': 'Очередь и дек: FIFO и двусторонний', 'content': THEORY_2, 'order': 1,
         'mini_tasks': MINI_TASKS_2, 'quiz': QUIZ_2},
    ],
    'tasks': [
        # Уровень 1 — использование std::queue / std::stack
        {
            'title': 'Эмуляция очереди',
            'description': (
                'Прочитайте `q` команд. Каждая команда имеет вид `push x` (добавить '
                'число в очередь), `pop` (удалить голову и вывести её) или '
                '`front` (вывести голову, не удаляя её).\n\n'
                '## Пример\n```\n5\npush 1\npush 2\nfront\npop\nfront\n```\n```\n1\n1\n2\n```'
            ),
            'difficulty': 1,
            'tests': [
                {'input': '5\npush 1\npush 2\nfront\npop\nfront', 'expected': '1\n1\n2'},
                {'input': '3\npush 10\nfront\npop', 'expected': '10\n10'},
            ],
            'hint': 'Используй std::queue<int>. Команда push вызывает push, front вызывает front(), а pop сначала выводит front(), затем делает pop().',
            'tags': ['queue'],
            'order': 0,
            'variations': [
                {
                    'title': 'Эмуляция стека',
                    'description': (
                        'Прочитайте `q` команд. Каждая команда имеет вид `push x` (положить '
                        'число в стек), `pop` (удалить вершину и вывести её) или '
                        '`top` (вывести вершину, не удаляя её).\n\n'
                        '## Пример\n```\n5\npush 1\npush 2\ntop\npop\ntop\n```\n```\n2\n2\n1\n```'
                    ),
                    'tests': [
                        {'input': '5\npush 1\npush 2\ntop\npop\ntop', 'expected': '2\n2\n1'},
                        {'input': '4\npush 5\ntop\npop\npush 9', 'expected': '5\n5'},
                    ],
                    'hint': 'Принцип тот же, что у очереди, только вместо `queue` берём `stack`, а вместо `front` зовём `top`. Из стека «выходит» последний пришедший.',
                    'tags': ['stack'],
                },
            ],
        },

        # Уровень 2 — применение стека
        {
            'title': 'Правильная скобочная последовательность (только круглые)',
            'description': (
                'Прочитайте строку из символов `(` и `)`. Определите, является ли она '
                'правильной скобочной последовательностью. Выведите `да` или `нет`.\n\n'
                '## Пример\n```\n(())\n```\n```\nда\n```'
            ),
            'difficulty': 2,
            'tests': [
                {'input': '(())', 'expected': 'да'},
                {'input': '()()', 'expected': 'да'},
                {'input': '(', 'expected': 'нет'},
                {'input': ')(', 'expected': 'нет'},
                {'input': '((()))', 'expected': 'да'},
            ],
            'hint': 'На каждое `(` делайте push в стек, а на каждое `)` выполняйте проверку и pop. В конце стек должен быть пуст.',
            'tags': ['stack', 'string'],
            'order': 1,
            'variations': [
                {
                    'title': 'Правильная скобочная последовательность из (, [, {',
                    'description': (
                        'Прочитайте строку из символов `()[]{}` (без других символов). '
                        'Определите, является ли она правильной скобочной последовательностью. '
                        'Выведите `да` или `нет`.\n\n'
                        '## Пример\n```\n([{}])\n```\n```\nда\n```'
                    ),
                    'tests': [
                        {'input': '([{}])', 'expected': 'да'},
                        {'input': '([)]', 'expected': 'нет'},
                        {'input': '(((())))', 'expected': 'да'},
                        {'input': '(', 'expected': 'нет'},
                        {'input': ')', 'expected': 'нет'},
                        {'input': '{[()]}', 'expected': 'да'},
                    ],
                    'hint': 'Идея та же, но при `)` нужно проверить, что на вершине стека лежит именно `(`. Аналогично для `]` и `}`.',
                    'tags': ['stack', 'string'],
                },
            ],
        },

        # Уровень 3 — нестандартное применение стека
        {
            'title': 'Обратная польская запись',
            'description': (
                'Прочитайте строку, содержащую арифметическое выражение в обратной польской '
                'записи (постфиксной). Числа и операторы (+, -, *) разделены пробелами. '
                'Выведите результат вычисления.\n\n'
                '## Пример\n```\n3 4 + 2 *\n```\n```\n14\n```\n\n(то есть (3+4)*2 = 14)'
            ),
            'difficulty': 3,
            'tests': [
                {'input': '3 4 + 2 *', 'expected': '14'},
                {'input': '5 1 -', 'expected': '4'},
                {'input': '2 3 4 * +', 'expected': '14'},
                {'input': '7', 'expected': '7'},
            ],
            'hint': 'Заведите стек чисел. На число делайте push, а на оператор снимайте со стека два числа, применяйте оператор и кладите результат обратно.',
            'tags': ['stack', 'parsing'],
            'order': 2,
            'variations': [
                {
                    'title': 'Минимум стека за O(1)',
                    'description': (
                        'Прочитайте число операций `q`, затем `q` строк. Каждая строка содержит '
                        'либо `push x` (положить число в стек), либо `pop` (удалить вершину), '
                        'либо `min` (вывести минимум среди элементов стека).\n\n'
                        'Все операции должны работать за O(1).\n\n'
                        '## Пример\n```\n6\npush 5\npush 3\nmin\npush 7\npop\nmin\n```\n```\n3\n3\n```'
                    ),
                    'tests': [
                        {'input': '6\npush 5\npush 3\nmin\npush 7\npop\nmin', 'expected': '3\n3'},
                        {'input': '4\npush 10\nmin\npush 5\nmin', 'expected': '10\n5'},
                        {'input': '5\npush 3\npush 1\npush 4\npop\nmin', 'expected': '1'},
                    ],
                    'hint': 'Веди два стека: основной и стек минимумов. При push клади min(x, текущий минимум) во второй.',
                    'tags': ['stack'],
                },
            ],
        },
    ],
}
