# -*- coding: utf-8 -*-
"""Модуль 9: Хеш-таблицы."""

# ============================================================================
# ТЕОРИЯ 1 — Что такое хеш-таблица
# ============================================================================

THEORY_1 = """\
# Хеш-таблицы: «сразу найди по ключу»

## Зачем нужна ещё одна структура

В задачах постоянно встречаются вопросы вида:

- «Есть ли это значение в большой коллекции?»
- «Сколько раз встретилось каждое значение?»
- «По какому имени какая запись?»

Линейный поиск отвечает на «есть ли?» за O(n), и для миллиона элементов
это становится медленно. Двоичный поиск даёт O(log n), но он требует
**отсортированных** данных. Если ключи это строки или сложные
структуры, поддерживать сортировку тоже неудобно.

**Хеш-таблица** это структура данных, которая отвечает на такие вопросы
за **O(1) в среднем**. То есть «практически мгновенно», независимо от
размера коллекции. Это магия, ради которой её и придумали.

## Как это работает

Аналогия. Представь огромную камеру хранения на вокзале, где у каждой
ячейки есть свой номер. Тебе нужно найти своего знакомого, но ты не
помнишь, в какую ячейку он положил вещи. Перебирать все ячейки подряд
долго. А теперь представь, что номер ячейки можно **вычислить** из
имени человека: например, сложить коды букв и взять остаток от деления
на количество ячеек. Тогда по имени сразу понятно, в какую ячейку идти.

Хеш-таблица устроена так же. Внутри лежит большой массив. Когда ты
кладёшь ключ, специальная **хеш-функция** превращает этот ключ в число
(индекс в массиве). Туда же кладётся значение. Поиск работает зеркально:
по ключу считаем хеш, прыгаем в нужный индекс.

Понимать устройство во всех деталях для обычного использования не
обязательно. Главное запомнить: вставка, поиск и удаление в хеш-таблице
работают за **O(1) в среднем**, что и делает её одной из самых
востребованных структур в задачах.

Есть нюанс. В худшем случае (когда много ключей попадают в одну и ту же
ячейку, это называется **коллизия**) операции могут деградировать до
O(n). На практике это бывает редко и решается доработками внутри
библиотеки. На уровне нашего курса можно считать, что O(1) выполняется
всегда.

## std::unordered_set

Самая простая хеш-структура из стандартной библиотеки это **множество**
уникальных значений. Подключается через заголовок `<unordered_set>`.

```cpp-run
// hint: множество автоматически отбрасывает повторы
#include <iostream>
#include <unordered_set>
using namespace std;

int main() {
    unordered_set<int> s;
    // поэкспериментируй: добавь больше элементов, проверь count для разных
    s.insert(10);
    s.insert(20);
    s.insert(10);                                    // повтор игнорируется

    cout << "Размер: " << s.size() << endl;          // 2
    cout << "Есть 10? " << s.count(10) << endl;      // 1
    cout << "Есть 15? " << s.count(15) << endl;      // 0
    return 0;
}
```

Главные методы: `insert(x)` добавляет, `count(x)` проверяет наличие
(возвращает 1 или 0), `erase(x)` удаляет, `size()` возвращает количество
уникальных элементов, `empty()` проверяет, пусто ли.

## std::unordered_map

То же самое, но с парами **ключ → значение**. Подключается через
`<unordered_map>`. Это как телефонная книга: имя (ключ) указывает на
номер телефона (значение).

```cpp-run
// hint: словарь имя -> возраст
#include <iostream>
#include <unordered_map>
#include <string>
using namespace std;

int main() {
    unordered_map<string, int> ages;
    // поэкспериментируй: добавь ещё имена, обнови существующее значение
    ages["Анна"] = 25;
    ages["Борис"] = 30;
    ages["Анна"] = 26;                                // обновили существующий ключ

    cout << "Анне: " << ages["Анна"] << endl;
    cout << "Размер: " << ages.size() << endl;
    return 0;
}
```

[mini-task:0]

## Главное про оператор `[]`

У оператора `m[key]` есть одна **очень коварная** особенность, на которой
постоянно спотыкаются новички.

- Если ключа в таблице **нет**, оператор `[]` его молча **создаёт** со значением по умолчанию. Для `int` это 0, для `string` это пустая строка, для своих структур это значение, заданное конструктором по умолчанию.
- Если ключ уже есть, оператор возвращает **ссылку** на значение, и через неё можно как читать, так и менять.

Что это значит на практике: написать `if (m[key] != 0)` для проверки
«есть ли ключ» **нельзя**. Такая проверка сама создаст этот ключ как
побочный эффект. Через несколько таких проверок в таблице окажется куча
мусора, а ты даже не поймёшь, как он туда попал.

Безопасные способы проверить наличие ключа:

```cpp-run
// hint: count и find безопасны, оператор [] нет
#include <iostream>
#include <unordered_map>
#include <string>
using namespace std;

int main() {
    unordered_map<string, int> m;
    m["one"] = 1;
    // поэкспериментируй: добавь больше ключей, проверь find и count для них

    if (m.count("two") == 0) {                       // count: безопасная проверка
        cout << "two нет" << endl;
    }

    auto it = m.find("one");                         // find: вернёт итератор
    if (it != m.end()) {
        cout << "one найдено, значение: " << it->second << endl;
    }
    return 0;
}
```

`m.count(key)` возвращает `0` или `1` и **не создаёт** ключей. `m.find(key)`
возвращает итератор на пару (ключ, значение) или `m.end()`, если ключа
нет. Через итератор `it->first` это ключ, `it->second` это значение.

[mini-task:1]

## Итоги

- Хеш-таблица отвечает на вопросы «есть ли?» и «что за значение по ключу?» за O(1) в среднем.
- `unordered_set` хранит множество уникальных значений. `unordered_map` хранит пары «ключ → значение».
- Оператор `[]` **создаёт** ключи, если их нет. Для проверки наличия используй `count(key)` или `find(key)`.
- В худшем случае (много коллизий) операции могут стать O(n), но на практике это бывает редко.
"""

QUIZ_1 = [
    {
        'question': 'Какая средняя сложность вставки/поиска в std::unordered_map?',
        'a': 'O(1)', 'b': 'O(log n)', 'c': 'O(n)', 'd': 'O(n log n)',
        'correct': 'a',
    },
    {
        'question': 'Что произойдёт при m["new_key"] для unordered_map<string,int>, если ключа ещё не было?',
        'a': 'Программа упадёт',
        'b': 'Будет создана запись со значением 0 (значение по умолчанию)',
        'c': 'Вернёт -1',
        'd': 'Бросит исключение',
        'correct': 'b',
    },
    {
        'question': 'Как безопасно проверить, есть ли ключ в map, без создания записи?',
        'a': 'm[key] != 0',
        'b': 'm.count(key) > 0 или m.find(key) != m.end()',
        'c': 'isset(m, key)',
        'd': 'm.has(key)',
        'correct': 'b',
    },
    {
        'question': 'В чём разница между unordered_set и std::set?',
        'a': 'Никакой',
        'b': 'set построен на дереве, упорядочен и работает за O(log n); unordered_set построен на хеше, не упорядочен и работает за O(1)',
        'c': 'set хранит пары, а unordered_set хранит одиночные значения',
        'd': 'unordered_set нельзя обходить',
        'correct': 'b',
    },
    {
        'question': 'Какая сложность операций у unordered_map в худшем случае?',
        'a': 'Тоже O(1)',
        'b': 'O(n), если возникает много коллизий',
        'c': 'O(log n)',
        'd': 'Это не определено',
        'correct': 'b',
    },
]

MINI_TASKS_1 = [
    {
        'title': 'Уникальных значений',
        'description': (
            'Прочитайте `n`, затем `n` целых чисел. Выведите количество '
            '**различных** значений среди них.\n\n'
            '## Пример\n```\n7\n3 1 4 1 5 9 5\n```\n```\n5\n```'
        ),
        'starter_code': (
            '#include <iostream>\n'
            '#include <unordered_set>\n'
            'using namespace std;\n\n'
            'int main() {\n'
            '    int n;\n'
            '    cin >> n;\n'
            '    // Ваш код\n'
            '\n'
            '    return 0;\n'
            '}\n'
        ),
        'tests': [
            {'input': '7\n3 1 4 1 5 9 5', 'expected': '5'},
            {'input': '5\n1 1 1 1 1', 'expected': '1'},
            {'input': '4\n1 2 3 4', 'expected': '4'},
        ],
        'hint': 'Множество автоматически отбрасывает повторы. Какой метод вернёт количество элементов?',
    },
    {
        'title': 'Есть ли в наборе?',
        'description': (
            'Прочитайте `n` и `n` целых чисел, образующих «набор». Затем `q` запросов: '
            'для каждого запроса прочитайте число и выведите `YES` или `NO`, '
            'смотря по тому, есть ли оно в наборе.\n\n'
            '## Пример\n```\n5\n10 20 30 40 50\n3\n20\n25\n50\n```\n```\nYES\nNO\nYES\n```'
        ),
        'starter_code': (
            '#include <iostream>\n'
            '#include <unordered_set>\n'
            'using namespace std;\n\n'
            'int main() {\n'
            '    int n;\n'
            '    cin >> n;\n'
            '    unordered_set<int> s;\n'
            '    for (int i = 0; i < n; ++i) { int v; cin >> v; s.insert(v); }\n'
            '    int q;\n'
            '    cin >> q;\n'
            '    // Ваш код\n'
            '\n'
            '    return 0;\n'
            '}\n'
        ),
        'tests': [
            {'input': '5\n10 20 30 40 50\n3\n20\n25\n50', 'expected': 'YES\nNO\nYES'},
            {'input': '3\n1 2 3\n4\n1\n4\n3\n5', 'expected': 'YES\nNO\nYES\nNO'},
        ],
        'hint': 'Для каждого запроса проверьте наличие через метод, который не модифицирует множество.',
    },
]

# ============================================================================
# ТЕОРИЯ 2 — Применения хеш-таблиц
# ============================================================================

THEORY_2 = """\
# Применения хеш-таблиц: частоты, дедупликация, Two Sum

Хеш-таблицы кажутся «просто ещё одной структурой», но за этим простым
интерфейсом скрывается несколько очень мощных шаблонов решения задач.
Если знать их, многие задачи, которые на первый взгляд требуют сложного
алгоритма, решаются в десять строк.

## Подсчёт частот

Это, пожалуй, самый частый паттерн на собеседованиях и в учебных
задачах. Идея простая: пройти по элементам и считать, сколько раз
встретился каждый из них.

Аналогия. Представь, что ты считаешь голоса на выборах. У тебя в руках
бюллетени, а перед тобой лежат коробки, по одной на каждого кандидата.
Берёшь бюллетень, читаешь имя, кидаешь в нужную коробку. Если коробки
ещё нет (кандидат не встречался раньше), достаёшь новую и ставишь.
В конце смотришь, в какой коробке сколько голосов.

```cpp-run
// hint: считаем, сколько раз встретилась каждая буква
#include <iostream>
#include <unordered_map>
#include <string>
using namespace std;

int main() {
    string s = "abracadabra";
    // поэкспериментируй: поменяй s, попробуй посчитать гласные/согласные
    unordered_map<char, int> freq;
    for (char c : s) {
        ++freq[c];                                   // если ключа не было, он создастся со значением 0
    }
    for (auto& p : freq) {
        cout << p.first << ": " << p.second << endl;
    }
    return 0;
}
```

Здесь поведение оператора `[]` (он создаёт ключ при первом обращении)
оказывается ровно тем, что нужно. Если буквы ещё не было в таблице, она
создаётся со значением 0, и сразу же увеличивается на 1. Получается
чистый и короткий код.

При обходе по таблице используется конструкция `for (auto& p : freq)`.
Здесь `p` это пара (ключ, значение), `p.first` это ключ, `p.second`
значение. Знак `&` нужен, чтобы не копировать пары без нужды.

## Когда нужен порядок: `std::map`

`unordered_map` быстрый, но ключи в нём лежат в **случайном** порядке.
Когда нужно, чтобы при обходе ключи шли по возрастанию (например, печать
частот в отсортированном виде), вместо хеш-таблицы берут `std::map`. Он
построен не на хеше, а на сбалансированном дереве. Операции работают за
O(log n), но взамен ключи всегда упорядочены.

```cpp-run
// hint: map обходит ключи в отсортированном порядке
#include <iostream>
#include <map>
using namespace std;

int main() {
    map<int, int> m;
    // поэкспериментируй: вставь в другом порядке, добавь больше пар
    m[3] = 30;
    m[1] = 10;
    m[2] = 20;
    for (auto& p : m) {
        cout << p.first << " -> " << p.second << endl;   // в порядке 1, 2, 3
    }
    return 0;
}
```

То же самое есть для множеств: `std::set` вместо `std::unordered_set`.
Правило выбора: нужна скорость и порядок не важен, бери unordered. Нужен
порядок, бери обычный set/map.

[mini-task:0]

## Two Sum: классика собеседований

Задача звучит так. Дан массив и число `s`. Нужно найти два индекса, сумма
элементов на которых равна `s`. Тебе уже встречался этот сюжет в модуле
про массивы, и там мы решали его методом двух указателей. Но метод двух
указателей требует **отсортированного** массива.

Хеш-таблица позволяет решить задачу за O(n) **без сортировки**. Идея
следующая: идём по массиву и для каждого `a[i]` спрашиваем себя
«встречалось ли уже число `s − a[i]`?». Если да, пара найдена. Если нет,
запоминаем `a[i]` в таблице и идём дальше.

Аналогия. Представь, что ты на дне рождения и хочешь найти двух гостей,
сумма возрастов которых даёт ровно сто. Идёшь по списку и для каждого
человека прикидываешь: «сколько лет должен быть напарник, чтобы вышло
сто?». Заглядываешь в записную книжку. Если такой возраст там уже
записан, ответ найден. Если нет, добавляешь текущего человека в книжку
и идёшь к следующему.

```cpp-run
// hint: Two Sum за один проход через хеш-таблицу
#include <iostream>
#include <vector>
#include <unordered_map>
using namespace std;

int main() {
    vector<int> a = {2, 7, 11, 15};
    int target = 9;
    // поэкспериментируй: поменяй target на 18 (11+7) или на 100

    unordered_map<int, int> seen;                     // значение -> индекс, где видели
    for (int i = 0; i < (int)a.size(); ++i) {
        int need = target - a[i];                     // какого числа нам не хватает
        auto it = seen.find(need);
        if (it != seen.end()) {
            cout << it->second << ' ' << i << endl;   // нашли: 0 1
            return 0;
        }
        seen[a[i]] = i;                               // запоминаем себя в таблице
    }
    cout << "Не нашли" << endl;
    return 0;
}
```

Магия здесь не в каком-то хитром алгоритме, а в том, что хеш-таблица
позволяет проверять «было ли уже такое число» за O(1). Поэтому общая
сложность O(n), хотя без хеш-таблицы пришлось бы перебирать все пары и
получать O(n²).

[mini-task:1]

## Когда хеш-таблица не нужна

Хеш-таблица это очень мощная вещь, но не серебряная пуля. В некоторых
случаях она избыточна или даже вредна:

- Если массив маленький (несколько десятков элементов), линейного поиска вполне хватит и хеш-таблица не даст ощутимого выигрыша. А памяти на саму таблицу уйдёт больше.
- Если нужно обходить ключи в отсортированном порядке, бери `std::map` или `std::set`. У `unordered_*` порядка нет, и если попытаться его получить, придётся отдельно сортировать ключи.
- Если поиск нужно сделать ровно один раз, стоимость заполнения таблицы может «съесть» весь выигрыш.

## Итоги

- Подсчёт частот через `unordered_map` и шаблон `++freq[x]` это самый частый паттерн в задачах на хеш.
- Если важна сортировка ключей, бери `std::map` или `std::set`. Они работают за O(log n) и хранят ключи упорядоченно.
- Two Sum за O(n) без сортировки это классический пример того, как хеш-таблица заменяет «отсортируй и используй два указателя».
- Хеш-таблица отличный инструмент в большинстве задач, но не всегда нужна. На очень маленьких данных линейный поиск часто проще и читабельнее.
"""

QUIZ_2 = [
    {
        'question': 'Что делает ++freq[c] для unordered_map<char,int> при отсутствующем ключе c?',
        'a': 'Бросит ошибку',
        'b': 'Создаст ключ со значением 0 и увеличит его до 1',
        'c': 'Игнорирует операцию',
        'd': 'Удалит ключ',
        'correct': 'b',
    },
    {
        'question': 'Когда стоит выбрать std::map вместо std::unordered_map?',
        'a': 'Никогда', 'b': 'Когда нужен обход ключей по возрастанию', 'c': 'Когда ключей мало', 'd': 'Когда ключами являются числа',
        'correct': 'b',
    },
    {
        'question': 'Какой выигрыш даёт хеш-таблица в задаче Two Sum?',
        'a': 'Решение за O(n) без сортировки',
        'b': 'Уменьшение памяти',
        'c': 'Возможность работать с отрицательными числами',
        'd': 'Никакого',
        'correct': 'a',
    },
    {
        'question': 'Что вернёт std::map по умолчанию для отсутствующего ключа при обращении через []?',
        'a': '-1',
        'b': 'Значение по умолчанию (0 для int) и создаст запись',
        'c': 'Ошибку',
        'd': 'Размер карты',
        'correct': 'b',
        'explanation': 'Поведение оператора [] идентично у map и unordered_map.',
    },
    {
        'question': 'Какая сложность операций у std::map (на дереве)?',
        'a': 'O(1)', 'b': 'O(log n)', 'c': 'O(n)', 'd': 'O(n log n)',
        'correct': 'b',
    },
]

MINI_TASKS_2 = [
    {
        'title': 'Самое частое',
        'description': (
            'Прочитайте `n` и `n` целых чисел. Выведите значение, которое '
            'встретилось наибольшее число раз. Если таких несколько, '
            'выведите наименьшее из них.\n\n'
            '## Пример\n```\n7\n3 1 4 1 5 9 5\n```\n```\n1\n```'
        ),
        'starter_code': (
            '#include <iostream>\n'
            '#include <map>\n'
            'using namespace std;\n\n'
            'int main() {\n'
            '    int n;\n'
            '    cin >> n;\n'
            '    // Ваш код\n'
            '\n'
            '    return 0;\n'
            '}\n'
        ),
        'tests': [
            {'input': '7\n3 1 4 1 5 9 5', 'expected': '1'},
            {'input': '5\n5 5 5 5 5', 'expected': '5'},
            {'input': '4\n10 20 10 20', 'expected': '10'},
            {'input': '1\n42', 'expected': '42'},
        ],
        'hint': 'Подсчитайте частоты. При равенстве частот нужен наименьший ключ, поэтому выбор map вместо unordered_map подсказан.',
    },
    {
        'title': 'Two Sum',
        'description': (
            'Прочитайте `n` и `s`, затем `n` целых чисел. Найдите два разных '
            'индекса i < j (0-индексация) с условием `a[i] + a[j] == s`. '
            'Выведите их через пробел. Если такой пары нет, выведите `-1 -1`.\n\n'
            'Решение должно работать за O(n).\n\n'
            '## Пример\n```\n4 9\n2 7 11 15\n```\n```\n0 1\n```'
        ),
        'starter_code': (
            '#include <iostream>\n'
            '#include <vector>\n'
            '#include <unordered_map>\n'
            'using namespace std;\n\n'
            'int main() {\n'
            '    int n, s;\n'
            '    cin >> n >> s;\n'
            '    vector<int> a(n);\n'
            '    for (int i = 0; i < n; ++i) cin >> a[i];\n'
            '    // Ваш код\n'
            '\n'
            '    return 0;\n'
            '}\n'
        ),
        'tests': [
            {'input': '4 9\n2 7 11 15', 'expected': '0 1'},
            {'input': '3 6\n3 2 4', 'expected': '1 2'},
            {'input': '3 100\n1 2 3', 'expected': '-1 -1'},
        ],
        'hint': 'Идя по массиву, держите в таблице «уже виденные» значения с их индексами. Для каждого нового проверяйте, не было ли уже нужного дополнения.',
    },
]

# ============================================================================
MODULE = {
    'title': 'Хеш-таблицы',
    'description': (
        'Хеш-таблицы как быстрая структура для поиска по ключу. Применяются для '
        'подсчёта частот, удаления повторов и проверки того, встречалось ли значение '
        'раньше. Сравниваются с упорядоченными аналогами на основе дерева.'
    ),
    'concepts': (
        'std::unordered_set<T>: insert, erase, count, find, size, empty; '
        'std::unordered_map<K, V>: оператор [], at(), count, find, size; '
        'разница между unordered_map (O(1), без порядка) и std::map '
        '(O(log n), упорядочена по ключу); '
        'паттерны: подсчёт частот через ++freq[x], проверка наличия через '
        'count() и find(), Two Sum через unordered_map за O(n)'
    ),
    'order': 9,
    'theories': [
        {'title': 'Что такое хеш-таблица', 'content': THEORY_1, 'order': 0,
         'mini_tasks': MINI_TASKS_1, 'quiz': QUIZ_1},
        {'title': 'Применения хеш-таблиц', 'content': THEORY_2, 'order': 1,
         'mini_tasks': MINI_TASKS_2, 'quiz': QUIZ_2},
    ],
    'tasks': [
        # Уровень 1 — unordered_set
        {
            'title': 'Количество уникальных значений',
            'description': (
                'Прочитайте `n` и `n` чисел. Выведите количество **различных** '
                'значений среди них.\n\n'
                '## Пример\n```\n7\n3 1 4 1 5 9 5\n```\n```\n5\n```'
            ),
            'difficulty': 1,
            'tests': [
                {'input': '7\n3 1 4 1 5 9 5', 'expected': '5'},
                {'input': '1\n42', 'expected': '1'},
                {'input': '5\n1 1 1 1 1', 'expected': '1'},
                {'input': '4\n1 2 3 4', 'expected': '4'},
            ],
            'hint': 'unordered_set<int> и .size() в конце.',
            'tags': ['hash', 'array'],
            'order': 0,
            'variations': [
                {
                    'title': 'Проверка вхождения в множество',
                    'description': (
                        'Прочитайте `n` и `n` целых чисел, образующих «набор». Затем `q` запросов: '
                        'для каждого запроса прочитайте число и выведите `да` или `нет`, '
                        'смотря по тому, есть ли оно в наборе.\n\n'
                        '## Пример\n```\n5\n10 20 30 40 50\n3\n20\n25\n50\n```\n```\nда\nнет\nда\n```'
                    ),
                    'tests': [
                        {'input': '5\n10 20 30 40 50\n3\n20\n25\n50', 'expected': 'да\nнет\nда'},
                        {'input': '3\n1 2 3\n4\n1\n4\n3\n5', 'expected': 'да\nнет\nда\nнет'},
                    ],
                    'hint': 'Положите все значения в `unordered_set`, потом для каждого запроса проверяйте через `count`.',
                    'tags': ['hash', 'array'],
                },
            ],
        },

        # Уровень 2 — unordered_map / map
        {
            'title': 'Подсчёт частот',
            'description': (
                'Прочитайте `n` и `n` целых чисел. Выведите все различные значения '
                'и сколько раз каждое встречается, по возрастанию значения, по '
                'одной паре `значение количество` на строку.\n\n'
                '## Пример\n```\n7\n3 1 4 1 5 9 5\n```\n```\n1 2\n3 1\n4 1\n5 2\n9 1\n```'
            ),
            'difficulty': 2,
            'tests': [
                {'input': '7\n3 1 4 1 5 9 5', 'expected': '1 2\n3 1\n4 1\n5 2\n9 1'},
                {'input': '5\n5 5 5 5 5', 'expected': '5 5'},
                {'input': '3\n3 1 2', 'expected': '1 1\n2 1\n3 1'},
            ],
            'hint': 'Раз нужен отсортированный вывод, используй std::map<int,int>: он автоматически упорядочивает по ключу.',
            'tags': ['hash', 'sorting'],
            'order': 1,
            'variations': [
                {
                    'title': 'Самое частое значение',
                    'description': (
                        'Прочитайте `n` и `n` целых чисел. Выведите значение, которое '
                        'встречается наибольшее число раз. Если таких несколько, '
                        'выведите наименьшее из них.\n\n'
                        '## Пример\n```\n7\n3 1 4 1 5 9 5\n```\n```\n1\n```'
                    ),
                    'tests': [
                        {'input': '7\n3 1 4 1 5 9 5', 'expected': '1'},
                        {'input': '5\n5 5 5 5 5', 'expected': '5'},
                        {'input': '4\n10 20 10 20', 'expected': '10'},
                        {'input': '1\n42', 'expected': '42'},
                    ],
                    'hint': 'Подсчитайте частоты в `map`, потом пройдитесь по нему: ключ с наибольшим count и есть ответ; при равных count меньший ключ встретится первым.',
                    'tags': ['hash'],
                },
            ],
        },

        # Уровень 3 — хеш-таблица вместо двух указателей
        {
            'title': 'Two Sum (пара с суммой за O(n))',
            'description': (
                'Прочитайте `n` и `s`, затем `n` целых чисел. Найдите два **разных** '
                'индекса `i < j` (0-индексация), таких что `a[i] + a[j] == s`. '
                'Выведите их через пробел. Если такой пары нет, выведите `-1 -1`.\n\n'
                'Решение должно работать за O(n) без сортировки.\n\n'
                '## Пример\n```\n4 9\n2 7 11 15\n```\n```\n0 1\n```'
            ),
            'difficulty': 3,
            'tests': [
                {'input': '4 9\n2 7 11 15', 'expected': '0 1'},
                {'input': '3 6\n3 2 4', 'expected': '1 2'},
                {'input': '3 100\n1 2 3', 'expected': '-1 -1'},
                {'input': '4 4\n1 2 3 1', 'expected': '0 2'},
            ],
            'hint': 'Идя по массиву, держи в unordered_map значение → индекс. Для каждого a[i] проверь, был ли уже s - a[i].',
            'tags': ['hash', 'array'],
            'order': 2,
            'variations': [
                {
                    'title': 'Первый дубликат',
                    'description': (
                        'Прочитайте `n` и `n` целых чисел. Найдите первое число, которое '
                        'встречается **второй раз** в массиве (по индексу второго вхождения). '
                        'Выведите его. Если все числа уникальны, выведите `-1`.\n\n'
                        '## Пример\n```\n6\n3 1 4 1 5 9\n```\n```\n1\n```'
                    ),
                    'tests': [
                        {'input': '6\n3 1 4 1 5 9', 'expected': '1'},
                        {'input': '5\n1 2 3 4 5', 'expected': '-1'},
                        {'input': '4\n7 7 7 7', 'expected': '7'},
                        {'input': '4\n1 2 1 2', 'expected': '1'},
                    ],
                    'hint': 'Идите по массиву и держите в `unordered_set` уже встретившиеся значения. Первое, которое в нём уже есть, и будет ответом.',
                    'tags': ['hash', 'array'],
                },
            ],
        },
    ],
}
