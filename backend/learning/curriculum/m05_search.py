# -*- coding: utf-8 -*-
"""Модуль 5: Поиск."""

# ============================================================================
# ТЕОРИЯ 1 — Линейный поиск
# ============================================================================

THEORY_1 = """\
# Линейный поиск

## Что значит «искать»

В большинстве задач рано или поздно появляется вопрос: «есть ли в массиве
такой элемент?», «на какой позиции стоит число `x`?», «сколько раз
встретилось значение `y`?». Все эти задачи объединяет одна идея: посмотреть
на элементы и сравнить их с тем, что мы ищем.

Аналогия. Представь телефонную книжку, в которой записи идут как попало,
без всякого порядка. Тебе нужно найти Васю. Что делать? Открываешь
первую страницу, читаешь имена, потом следующую, и так пока не наткнёшься
на Васю или не дойдёшь до конца. Это и есть линейный поиск: «просто
посмотреть всё подряд».

## Линейный поиск

Самый простой и универсальный способ это пройти весь массив по порядку и
проверить каждый элемент:

```cpp-run
// hint: ищем число 4 в массиве
#include <iostream>
#include <vector>
using namespace std;

int main() {
    vector<int> a = {3, 1, 4, 1, 5, 9, 2};
    int target = 4;
    // поэкспериментируй: поменяй target на 9, 100, на любое из чисел массива

    int found_at = -1;                            // -1 значит «пока не нашли»
    for (int i = 0; i < (int)a.size(); ++i) {
        if (a[i] == target) {
            found_at = i;
            break;                                // нашли, дальше искать не нужно
        }
    }

    if (found_at >= 0) {
        cout << "Найдено на позиции " << found_at << endl;
    } else {
        cout << "Не найдено" << endl;
    }
    return 0;
}
```

Сложность такого поиска это O(n): в худшем случае придётся посмотреть на
все `n` элементов. Главное достоинство в том, что он работает на **любом**
массиве: ни сортировка, ни какие-то особые свойства от данных не требуются.

Зачем здесь `break`. Как только мы нашли нужный элемент, продолжать цикл
смысла нет. `break` сразу выходит из цикла и экономит время. Если же нам
нужно посчитать количество вхождений или найти все позиции, тогда `break`
наоборот не пишут.

Часто вместо отдельной переменной `found_at` пишут просто `return i;` (или
выходят флагом), но идея остаётся той же: пройти и сравнить.

## Когда линейного поиска достаточно

Линейный поиск это самый честный и универсальный приём. Он подходит,
когда:

- Массив небольшой. Даже тысяча или десять тысяч элементов это мгновение для процессора.
- Массив не отсортирован, а сортировать его дорого (например, мы будем искать всего один раз).
- Нужно посчитать, **сколько** раз встречается значение. Тогда `break` не пишем и идём до конца массива, накапливая счётчик.

```cpp-run
// hint: считаем, сколько раз встретилась единица
#include <iostream>
#include <vector>
using namespace std;

int main() {
    vector<int> a = {3, 1, 4, 1, 5, 1, 2};
    int target = 1, count = 0;
    // поэкспериментируй: поменяй target, поменяй элементы массива
    for (int x : a) {
        if (x == target) ++count;
    }
    cout << "Встретилось раз: " << count << endl;
    return 0;
}
```

[mini-task:0]

## Поиск по условию

Искать можно не только конкретное значение, но и любой элемент,
удовлетворяющий какому-то условию. Например, первое отрицательное число
в массиве:

```cpp-run
// hint: ищем первый элемент, который меньше нуля
#include <iostream>
#include <vector>
using namespace std;

int main() {
    vector<int> a = {3, 5, 1, -2, 7, -8};
    // поэкспериментируй: ищи первое чётное (x % 2 == 0), первое большее 5 (x > 5)
    for (int x : a) {
        if (x < 0) {
            cout << "Первое отрицательное: " << x << endl;
            break;
        }
    }
    return 0;
}
```

Тот же самый шаблон («пройти, проверить, остановиться при первом совпадении
или досчитать до конца») встретится в десятках задач. «Найди первый плохой
день», «найди первый дубликат», «найди первое слово длиннее пяти букв»,
«посчитай, сколько чисел больше 100». Везде это будет один и тот же цикл,
меняется только условие внутри.

## Итоги

- Линейный поиск это «пройти все элементы и сравнить с тем, что мы ищем».
- Сложность O(n). Работает на любых массивах, никаких требований к данным.
- `break` останавливает поиск сразу же, как только найден первый ответ.
- Тот же шаблон применяется и для подсчёта вхождений, и для поиска по произвольному условию.
"""

QUIZ_1 = [
    {
        'question': 'Какая сложность линейного поиска?',
        'a': 'O(1)', 'b': 'O(log n)', 'c': 'O(n)', 'd': 'O(n²)',
        'correct': 'c',
    },
    {
        'question': 'Нужно ли сортировать массив для линейного поиска?',
        'a': 'Да, обязательно',
        'b': 'Нет, работает на любом массиве',
        'c': 'Только для чисел',
        'd': 'Только если массив большой',
        'correct': 'b',
    },
    {
        'question': 'Зачем добавлять break при поиске первого вхождения?',
        'a': 'Для красоты',
        'b': 'Чтобы не делать лишних итераций после того, как нашли элемент',
        'c': 'break обязателен в каждом цикле',
        'd': 'Чтобы программа не упала',
        'correct': 'b',
    },
    {
        'question': 'Что обычно возвращают, если элемент не найден?',
        'a': '0', 'b': 'Размер массива', 'c': 'Специальное значение, например -1 или string::npos', 'd': 'Это вызовет ошибку',
        'correct': 'c',
    },
    {
        'question': 'Когда линейный поиск разумно заменить на двоичный?',
        'a': 'Когда массив отсортирован и поисков много',
        'b': 'Когда массив маленький',
        'c': 'Когда ищем строку',
        'd': 'Двоичный никогда не нужен',
        'correct': 'a',
    },
]

MINI_TASKS_1 = [
    {
        'title': 'Найти позицию',
        'description': (
            'Прочитайте `n`, затем `n` целых чисел и значение `x`. '
            'Выведите 0-индексированную позицию первого вхождения `x` '
            'или `-1`, если его нет.\n\n'
            '## Пример\n```\n5\n3 1 4 1 5\n4\n```\n```\n2\n```'
        ),
        'starter_code': (
            '#include <iostream>\n'
            '#include <vector>\n'
            'using namespace std;\n\n'
            'int main() {\n'
            '    int n;\n'
            '    cin >> n;\n'
            '    vector<int> a(n);\n'
            '    for (int i = 0; i < n; ++i) cin >> a[i];\n'
            '    int x;\n'
            '    cin >> x;\n'
            '    // Ваш код\n'
            '\n'
            '    return 0;\n'
            '}\n'
        ),
        'tests': [
            {'input': '5\n3 1 4 1 5\n4', 'expected': '2'},
            {'input': '5\n3 1 4 1 5\n9', 'expected': '-1'},
            {'input': '3\n1 1 1\n1', 'expected': '0'},
        ],
        'hint': 'Запоминайте индекс первого совпадения. Если так и не нашли, выводите -1.',
    },
]

# ============================================================================
# ТЕОРИЯ 2 — Двоичный поиск
# ============================================================================

THEORY_2 = """\
# Двоичный поиск

## Идея

Если массив **отсортирован** по возрастанию, то в нём можно искать гораздо
быстрее, чем за O(n). Идея простая.

Аналогия. Представь, что я загадал число от 1 до 100, а ты его
отгадываешь. Самая глупая стратегия это спрашивать «один?», «два?», «три?»
и так далее по очереди. В среднем ты потратишь 50 вопросов. Умная
стратегия другая: первый вопрос «больше или меньше 50?», второй «больше
или меньше 25 (или 75)?», и так далее. Каждый вопрос **разрезает** оставшийся
диапазон ровно пополам. За 7 шагов ты гарантированно отгадаешь любое число
от 1 до 100, потому что 2⁷ = 128.

Двоичный поиск работает по тому же принципу:

1. Смотрим на **середину** массива.
2. Если там и стоит то, что мы ищем, ура, мы нашли.
3. Если в середине число **меньше** искомого, значит, все элементы слева тем более меньше (массив-то отсортирован), и искать имеет смысл только **в правой половине**.
4. Иначе наоборот: продолжаем в **левой половине**.

На каждом шаге размер «зоны поиска» уменьшается в два раза. Поэтому за
примерно log₂(n) шагов мы либо найдём, либо убедимся, что элемента нет.
Для миллиона элементов это всего около двадцати шагов, для миллиарда
около тридцати. Сравни с миллионом или миллиардом шагов линейного поиска.

## Реализация

```cpp-run
// hint: ищем 7 в отсортированном массиве за log(n)
#include <iostream>
#include <vector>
using namespace std;

int main() {
    vector<int> a = {1, 3, 5, 7, 9, 11, 13};
    int target = 7;
    // поэкспериментируй: поменяй target на 11, на 4, на 100

    int lo = 0, hi = (int)a.size() - 1;
    int found = -1;
    while (lo <= hi) {
        int mid = lo + (hi - lo) / 2;             // середина без переполнения
        if (a[mid] == target) {
            found = mid;
            break;
        }
        if (a[mid] < target) lo = mid + 1;        // искомое правее, отрезаем левую половину
        else                 hi = mid - 1;        // искомое левее, отрезаем правую половину
    }

    cout << (found >= 0 ? "Найдено на позиции " : "Не найдено")
         << (found >= 0 ? to_string(found) : "") << endl;
    return 0;
}
```

Разберём ключевые места:

- `lo` и `hi` это границы текущей «зоны поиска». В начале они охватывают весь массив.
- Цикл крутится, пока зона не пуста (`lo <= hi`). Если в какой-то момент `lo` стало больше `hi`, искать больше негде.
- Маленькая, но важная деталь: `mid = lo + (hi - lo) / 2`, а не `(lo + hi) / 2`. Если `lo` и `hi` очень большие числа, их сумма может не поместиться в `int` и произойти **переполнение**. Запись через разность от этого защищает. Это классическая ловушка.

[mini-task:0]

## std::lower_bound и std::binary_search

В стандартной библиотеке двоичный поиск уже реализован, и пользоваться
готовым проще, чем писать цикл `while` каждый раз. В заголовке
`<algorithm>` есть три полезные функции:

| Функция            | Что возвращает                                   |
|--------------------|---------------------------------------------------|
| `binary_search`    | `true` или `false`: есть ли элемент в массиве     |
| `lower_bound`      | итератор на первый элемент `>= x`                 |
| `upper_bound`      | итератор на первый элемент `> x`                  |

```cpp-run
// hint: три готовых способа двоичного поиска
#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int main() {
    vector<int> a = {1, 3, 5, 7, 9};
    // поэкспериментируй: меняй число в скобках, попробуй 2, 5, 7, 100

    cout << binary_search(a.begin(), a.end(), 5) << endl;   // напечатает 1 (true)
    cout << binary_search(a.begin(), a.end(), 6) << endl;   // напечатает 0 (false)

    auto it = lower_bound(a.begin(), a.end(), 6);
    cout << "Первый >= 6: " << *it << " (индекс " << it - a.begin() << ")" << endl;
    return 0;
}
```

Важная техника: чтобы получить **индекс** найденного элемента, из
итератора вычитают `v.begin()`. Это работает потому, что итератор у
вектора по сути это и есть «расширенный указатель» на ячейку.

Маленький бонус: количество элементов, равных `x`, можно вычислить как
`upper_bound − lower_bound`. Эта формула часто встречается в задачах.

[mini-task:1]

## Когда двоичный поиск не сработает

- Массив **не отсортирован**. Тогда сначала отсортируй, либо используй линейный поиск. Если поисков много, потратиться на сортировку выгоднее.
- Нужно искать «по условию», которое не монотонно. То есть нельзя сказать «правее точно подходит, левее нет». Двоичный поиск опирается именно на это разделение, и без него не работает.

## Итоги

- Двоичный поиск работает за O(log n), но только на отсортированных данных.
- Идея «делим зону поиска пополам» это одна из самых мощных в алгоритмах.
- Считай середину как `lo + (hi - lo) / 2`, чтобы не получить переполнение.
- В стандартной библиотеке есть `binary_search`, `lower_bound` и `upper_bound`. Чаще всего пользуются именно ими.
- Чтобы получить индекс из итератора, вычитаем `v.begin()`.
"""

QUIZ_2 = [
    {
        'question': 'Какое условие обязательно для применения двоичного поиска?',
        'a': 'Массив из чисел',
        'b': 'Массив отсортирован',
        'c': 'Размер является степенью двойки',
        'd': 'Никаких',
        'correct': 'b',
    },
    {
        'question': 'Какова сложность двоичного поиска?',
        'a': 'O(1)', 'b': 'O(log n)', 'c': 'O(n)', 'd': 'O(n log n)',
        'correct': 'b',
    },
    {
        'question': 'Почему mid считают как lo + (hi - lo) / 2?',
        'a': 'Так быстрее',
        'b': 'Чтобы избежать переполнения int',
        'c': 'Это короче',
        'd': 'Нет особой причины',
        'correct': 'b',
    },
    {
        'question': 'Что возвращает std::lower_bound?',
        'a': 'Минимальный элемент',
        'b': 'Итератор на первый элемент, не меньший заданного',
        'c': 'Итератор на самый левый элемент равный x',
        'd': 'true / false',
        'correct': 'b',
    },
    {
        'question': 'Сколько шагов сделает бинарный поиск в массиве из миллиона элементов?',
        'a': 'Около 20', 'b': 'Около 1000', 'c': 'Около 1 000 000', 'd': 'Зависит от типа',
        'correct': 'a',
        'explanation': 'log₂(10⁶) ≈ 20.',
    },
]

MINI_TASKS_2 = [
    {
        'title': 'Есть ли элемент?',
        'description': (
            'Прочитайте `n`, затем `n` чисел в **отсортированном** по возрастанию '
            'порядке, затем число `x`. Выведите `YES`, если `x` есть в массиве, '
            'иначе `NO`. Решение должно работать за O(log n).\n\n'
            '## Пример\n```\n5\n1 3 5 7 9\n5\n```\n```\nYES\n```'
        ),
        'starter_code': (
            '#include <iostream>\n'
            '#include <vector>\n'
            '#include <algorithm>\n'
            'using namespace std;\n\n'
            'int main() {\n'
            '    int n;\n'
            '    cin >> n;\n'
            '    vector<int> a(n);\n'
            '    for (int i = 0; i < n; ++i) cin >> a[i];\n'
            '    int x;\n'
            '    cin >> x;\n'
            '    // Ваш код\n'
            '\n'
            '    return 0;\n'
            '}\n'
        ),
        'tests': [
            {'input': '5\n1 3 5 7 9\n5', 'expected': 'YES'},
            {'input': '5\n1 3 5 7 9\n4', 'expected': 'NO'},
            {'input': '1\n42\n42', 'expected': 'YES'},
            {'input': '1\n42\n0', 'expected': 'NO'},
        ],
        'hint': 'Можно сделать руками через while (lo <= hi), а можно стандартной функцией.',
    },
    {
        'title': 'Минимум, не меньше x',
        'description': (
            'Дан отсортированный по возрастанию массив `n` чисел и значение `x`. '
            'Выведите наименьший элемент, который **не меньше** `x`. Если такого '
            'нет, выведите `-1`.\n\n'
            '## Пример\n```\n5\n1 3 5 7 9\n4\n```\n```\n5\n```'
        ),
        'starter_code': (
            '#include <iostream>\n'
            '#include <vector>\n'
            '#include <algorithm>\n'
            'using namespace std;\n\n'
            'int main() {\n'
            '    int n;\n'
            '    cin >> n;\n'
            '    vector<int> a(n);\n'
            '    for (int i = 0; i < n; ++i) cin >> a[i];\n'
            '    int x;\n'
            '    cin >> x;\n'
            '    // Ваш код\n'
            '\n'
            '    return 0;\n'
            '}\n'
        ),
        'tests': [
            {'input': '5\n1 3 5 7 9\n4', 'expected': '5'},
            {'input': '5\n1 3 5 7 9\n10', 'expected': '-1'},
            {'input': '5\n1 3 5 7 9\n0', 'expected': '1'},
            {'input': '3\n10 20 30\n20', 'expected': '20'},
        ],
        'hint': 'В стандартной библиотеке есть готовая функция, которая возвращает ровно нужный итератор.',
    },
]

# ============================================================================
MODULE = {
    'title': 'Поиск',
    'description': (
        'Как находить нужный элемент в массиве. Линейный поиск работает в любых '
        'массивах, двоичный поиск гораздо быстрее, но требует, чтобы массив был '
        'отсортирован. Также разбирается приём «двоичный поиск по ответу».'
    ),
    'concepts': (
        'линейный поиск циклом for с ранним выходом через break; '
        'двоичный поиск вручную через while (lo <= hi) с mid = lo + (hi - lo) / 2; '
        'std::binary_search, std::lower_bound, std::upper_bound из <algorithm>; '
        'разность итераторов it - v.begin() для получения индекса'
    ),
    'order': 5,
    'theories': [
        {'title': 'Линейный поиск', 'content': THEORY_1, 'order': 0,
         'mini_tasks': MINI_TASKS_1, 'quiz': QUIZ_1},
        {'title': 'Двоичный поиск', 'content': THEORY_2, 'order': 1,
         'mini_tasks': MINI_TASKS_2, 'quiz': QUIZ_2},
    ],
    'tasks': [
        # Уровень 1 — линейный поиск
        {
            'title': 'Линейный поиск',
            'description': (
                'Прочитайте `n`, `n` чисел и значение `x`. Выведите 0-индексированную '
                'позицию первого вхождения `x` в массив или `-1`, если такого числа нет.\n\n'
                '## Пример\n```\n5\n3 1 4 1 5\n4\n```\n```\n2\n```'
            ),
            'difficulty': 1,
            'tests': [
                {'input': '5\n3 1 4 1 5\n4', 'expected': '2'},
                {'input': '5\n3 1 4 1 5\n9', 'expected': '-1'},
                {'input': '3\n1 1 1\n1', 'expected': '0'},
            ],
            'hint': 'Пройди массив циклом и верни первый индекс, где a[i] == x.',
            'tags': ['linear-search', 'array'],
            'order': 0,
            'variations': [
                {
                    'title': 'Количество вхождений',
                    'description': (
                        'Прочитайте `n`, `n` целых чисел и значение `x`. Выведите, сколько '
                        'раз `x` встречается в массиве (массив **не обязательно** отсортирован).\n\n'
                        '## Пример\n```\n6\n3 1 4 1 5 1\n1\n```\n```\n3\n```'
                    ),
                    'tests': [
                        {'input': '6\n3 1 4 1 5 1\n1', 'expected': '3'},
                        {'input': '5\n1 2 3 4 5\n9', 'expected': '0'},
                        {'input': '4\n7 7 7 7\n7', 'expected': '4'},
                    ],
                    'hint': 'Идея та же: один проход циклом. Только не выходим при первом совпадении, а считаем счётчик.',
                    'tags': ['linear-search', 'array'],
                },
            ],
        },

        # Уровень 2 — двоичный поиск
        {
            'title': 'Двоичный поиск',
            'description': (
                'Прочитайте `n`, затем `n` чисел в **отсортированном** по возрастанию '
                'порядке, затем число `x`. Выведите `да`, если `x` есть в массиве, '
                'иначе `нет`. Решение должно работать за O(log n).\n\n'
                '## Пример\n```\n5\n1 3 5 7 9\n5\n```\n```\nда\n```'
            ),
            'difficulty': 2,
            'tests': [
                {'input': '5\n1 3 5 7 9\n5', 'expected': 'да'},
                {'input': '5\n1 3 5 7 9\n4', 'expected': 'нет'},
                {'input': '1\n42\n42', 'expected': 'да'},
                {'input': '1\n42\n0', 'expected': 'нет'},
                {'input': '6\n-10 -5 0 5 10 15\n-10', 'expected': 'да'},
            ],
            'hint': 'Реализуй сам через while (lo <= hi) либо используй std::binary_search.',
            'tags': ['binary-search', 'array'],
            'order': 1,
            'variations': [
                {
                    'title': 'Первый элемент не меньше x',
                    'description': (
                        'Дан отсортированный по возрастанию массив `n` чисел и значение `x`. '
                        'Выведите наименьший элемент, который **не меньше** `x`. Если такого '
                        'нет, выведите `-1`. Решение должно работать за O(log n).\n\n'
                        '## Пример\n```\n5\n1 3 5 7 9\n4\n```\n```\n5\n```'
                    ),
                    'tests': [
                        {'input': '5\n1 3 5 7 9\n4', 'expected': '5'},
                        {'input': '5\n1 3 5 7 9\n10', 'expected': '-1'},
                        {'input': '5\n1 3 5 7 9\n0', 'expected': '1'},
                        {'input': '3\n10 20 30\n20', 'expected': '20'},
                    ],
                    'hint': 'В стандартной библиотеке для этого есть готовая функция, которая возвращает нужный итератор.',
                    'tags': ['binary-search', 'array'],
                },
            ],
        },

        # Уровень 3 — lower_bound / upper_bound
        {
            'title': 'Количество вхождений в отсортированном',
            'description': (
                'Дан отсортированный по возрастанию массив из `n` чисел и значение `x`. '
                'Выведите, сколько раз `x` встречается в массиве. Решение должно работать '
                'за O(log n).\n\n'
                '## Пример\n```\n7\n1 2 3 3 3 4 5\n3\n```\n```\n3\n```'
            ),
            'difficulty': 3,
            'tests': [
                {'input': '7\n1 2 3 3 3 4 5\n3', 'expected': '3'},
                {'input': '7\n1 2 3 3 3 4 5\n6', 'expected': '0'},
                {'input': '5\n2 2 2 2 2\n2', 'expected': '5'},
                {'input': '4\n1 2 3 4\n2', 'expected': '1'},
            ],
            'hint': 'Разность `upper_bound − lower_bound` даёт количество элементов, равных x.',
            'tags': ['binary-search', 'array'],
            'order': 2,
            'variations': [
                {
                    'title': 'Минимум, строго больше x',
                    'description': (
                        'Дан отсортированный по возрастанию массив `n` чисел и значение `x`. '
                        'Выведите наименьший элемент, который **строго больше** `x`. '
                        'Если такого нет, выведите `-1`. Решение должно работать за O(log n).\n\n'
                        '## Пример\n```\n5\n1 3 5 7 9\n4\n```\n```\n5\n```'
                    ),
                    'tests': [
                        {'input': '5\n1 3 5 7 9\n4', 'expected': '5'},
                        {'input': '5\n1 3 5 7 9\n9', 'expected': '-1'},
                        {'input': '5\n1 3 5 7 9\n0', 'expected': '1'},
                        {'input': '3\n10 20 30\n20', 'expected': '30'},
                    ],
                    'hint': 'Это то, что делает `upper_bound`. Проверь, что итератор не равен `end()`.',
                    'tags': ['binary-search', 'array'],
                },
            ],
        },
    ],
}
